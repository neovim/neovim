local bit = require 'bit'

local M = {}

-- Description of the values below is contained in ex_cmds_defs.h file.
-- "EX_" prefix is omitted.
local RANGE = 0x001
local BANG = 0x002
local EXTRA = 0x004
local XFILE = 0x008
local NOSPC = 0x010
local DFLALL = 0x020
local WHOLEFOLD = 0x040
local NEEDARG = 0x080
local TRLBAR = 0x100
local REGSTR = 0x200
local COUNT = 0x400
local NOTRLCOM = 0x800
local ZEROR = 0x1000
local CTRLV = 0x2000
local CMDARG = 0x4000
local BUFNAME = 0x8000
local BUFUNL = 0x10000
local ARGOPT = 0x20000
local SBOXOK = 0x40000
local CMDWIN = 0x80000
local MODIFY = 0x100000
local FLAGS = 0x200000
local LOCK_OK = 0x1000000
local PREVIEW = 0x8000000
local FILES = bit.bor(XFILE, EXTRA)
local WORD1 = bit.bor(EXTRA, NOSPC)
local FILE1 = bit.bor(FILES, NOSPC)

M.flags = {
  RANGE = RANGE,
  DFLALL = DFLALL,
  PREVIEW = PREVIEW,
}

-- The following table is described in ex_cmds_defs.h file.
M.cmds = {
  {
    command = 'append',
    flags = bit.bor(BANG, RANGE, ZEROR, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_append',
    desc = [[Insert several lines of text below the specified line. If the {range} is missing, the text will be inserted after the current line. Adding [!] toggles 'autoindent' for the time this command is executed.]],
    short_desc = 'append text',
  },
  {
    command = 'abbreviate',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = [[list all abbreviations. The character in the first column indicates the mode where the abbreviation is used: 'i' for insert mode, 'c' for Command-line mode, '!' for both. These are the same as for mappings, see map-listing.]],
    short_desc = 'enter abbreviation',
  },
  {
    command = 'abclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abclear',
    desc = 'Remove all abbreviations.',
    short_desc = 'remove all abbreviations',
  },
  {
    command = 'aboveleft',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd}. If it contains a command that splits a window, it will be opened left (vertical split) or above (horizontal split) the current window. Overrules 'splitbelow' and 'splitright'. Doesn't work for :execute and :normal.]],
    short_desc = 'make split window appear left or above',
  },
  {
    command = 'all',
    flags = bit.bor(BANG, RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_all',
    desc = [[Rearrange the screen to open one window for each argument. All other windows are closed. When a count is given, this is the maximum number of windows to open. With the :tab modifier open a tab page for each argument. When there are more arguments than 'tabpagemax' further ones become split windows in the last tab page. When the 'hidden' option is set, all buffers in closed windows become hidden. When 'hidden' is not set, and the 'autowrite' option is set, modified buffers are written.]],
    short_desc = 'open a window for each file in the argument',
  },
  {
    command = 'amenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = 'To make the command work correctly, a character is',
    short_desc = 'enter new menu item for all modes',
  },
  {
    command = 'anoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = 'To make the command work correctly, a character is',
    short_desc = 'enter a new menu for all modes that will not',
  },
  {
    command = 'args',
    flags = bit.bor(BANG, FILES, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_args',
    desc = 'Print the argument list, with the current file in square brackets.',
    short_desc = 'print the argument list',
  },
  {
    command = 'argadd',
    flags = bit.bor(BANG, RANGE, ZEROR, FILES, TRLBAR),
    addr_type = 'ADDR_ARGUMENTS',
    func = 'ex_argadd',
    desc = [[Add the {name}s to the argument list. When {name} is omitted add the current buffer name to the argument list. If [count] is omitted, the {name}s are added just after the current entry in the argument list. Otherwise they are added after the [count]'th file.]],
    short_desc = 'add items to the argument list',
  },
  {
    command = 'argdelete',
    flags = bit.bor(BANG, RANGE, FILES, TRLBAR),
    addr_type = 'ADDR_ARGUMENTS',
    func = 'ex_argdelete',
    desc = [[Delete files from the argument list that match the {pattern}s. {pattern} is used like a file pattern, see file-pattern. "%" can be used to delete the current entry. This command keeps the currently edited file, also when it's deleted from the argument list. Example: > :argdel *.obj]],
    short_desc = 'delete items from the argument list',
  },
  {
    command = 'argdo',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_ARGUMENTS',
    func = 'ex_listdo',
    desc = 'Execute {cmd} for each file in the argument list or, if [range] is specified, only for arguments in that range. It works like doing this: > :rewind :{cmd} :next :{cmd} etc.',
    short_desc = 'do a command on all items in the argument list',
  },
  {
    command = 'argdedupe',
    flags = TRLBAR,
    addr_type = 'ADDR_NONE',
    func = 'ex_argdedupe',
    desc = 'Remove duplicate filenames from the argument list. If your current file is a duplicate, your current file will change to the original file index.',
    short_desc = 'remove duplicates from the argument list',
  },
  {
    command = 'argedit',
    flags = bit.bor(BANG, NEEDARG, RANGE, ZEROR, FILES, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_ARGUMENTS',
    func = 'ex_argedit',
    desc = [[Add {name}s to the argument list and edit it. There is no check for duplicates, it is possible to add a file to the argument list twice :argded. This is like using :argadd and then :edit (with the small exception that :edit does not change the argument list, so the argument list pointer isn't changed). Spaces in filenames have to be escaped with "\". [count] is used like with :argadd. If the current file cannot be abandoned {name}s will still be added to the argument list, but won't be edited.]],
    short_desc = 'add item to the argument list and edit it',
  },
  {
    command = 'argglobal',
    flags = bit.bor(BANG, FILES, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_args',
    desc = [[Use the global argument list for the current window. Doesn't start editing another file. Use the global argument list for the current window. Define a new global argument list like :args_f. All windows using the global argument list will see this new list.]],
    short_desc = 'define the global argument list',
  },
  {
    command = 'arglocal',
    flags = bit.bor(BANG, FILES, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_args',
    desc = [[Make a local copy of the global argument list. Doesn't start editing another file. Define a new argument list, which is local to the current window. Works like :args_f otherwise.]],
    short_desc = 'define a local argument list',
  },
  {
    command = 'argument',
    flags = bit.bor(BANG, RANGE, COUNT, EXTRA, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_ARGUMENTS',
    func = 'ex_argument',
    desc = 'Edit file [count] in the argument list. When [count] is omitted the current entry is used. This fails when changes have been made and Vim does not want to abandon the current buffer. Also see ++opt and +cmd.',
    short_desc = 'go to specific file in the argument list',
  },
  {
    command = 'ascii',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'do_ascii',
    desc = [[Print the ascii value of the character under the cursor in decimal, hexadecimal and octal. Mnemonic: Get Ascii value. For example, when the cursor is on a 'R': <R> 82, Hex 52, Octal 122 ~ When the character is a non-standard ASCII character, but printable according to the 'isprint' option, the non-printable version is also given.]],
    short_desc = 'print ascii value of character under the cursor',
  },
  {
    command = 'autocmd',
    flags = bit.bor(BANG, EXTRA, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_autocmd',
    desc = [[Add {cmd} to the list of commands that Vim will execute automatically on {event} for a file matching {aupat} autocmd-pattern. Here {event} cannot be "*". Note: A quote character is seen as argument to the :autocmd and won't start a comment. Nvim always adds {cmd} after existing autocommands so they execute in the order in which they were defined. See autocmd-nested for [++nested]. If [++once] is supplied the command is executed once, then removed ("one shot").]],
    short_desc = 'enter or show autocommands',
  },
  {
    command = 'augroup',
    flags = bit.bor(BANG, WORD1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_autocmd',
    desc = [[Define the autocmd group name for the following ":autocmd" commands. The name "end" or "END" selects the default group. To avoid confusion, the name should be different from existing {event} names, as this most likely will not do what you intended. Delete the autocmd group {name}. Don't use this if there is still an autocommand using this group! You will get a warning if doing it anyway. When the group is the current group you will get error E936.]],
    short_desc = 'select the autocommand group to use',
  },
  {
    command = 'aunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = 'Eg: > :unmenu! Edit.Paste',
    short_desc = 'remove menu for all modes',
  },
  {
    command = 'buffer',
    flags = bit.bor(BANG, RANGE, BUFNAME, BUFUNL, COUNT, EXTRA, CMDARG, TRLBAR),
    addr_type = 'ADDR_BUFFERS',
    func = 'ex_buffer',
    desc = [[Edit buffer [N] from the buffer list. If [N] is not given, the current buffer remains being edited. See :buffer-! for [!]. This will also edit a buffer that is not in the buffer list, without setting the 'buflisted' flag. Also see +cmd.]],
    short_desc = 'go to specific buffer in the buffer list',
  },
  {
    command = 'bNext',
    flags = bit.bor(BANG, RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bprevious',
    desc = 'Mapped to :bprevious. default-mappings',
    short_desc = 'go to previous buffer in the buffer list',
  },
  {
    command = 'ball',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_buffer_all',
    desc = [[Rearrange the screen to open one window for each buffer in the buffer list. When a count is given, this is the maximum number of windows to open. 'winheight' also limits the number of windows opened ('winwidth' if :vertical was prepended). Buf/Win Enter/Leave autocommands are not executed for the new windows here, that's only done when they are really entered. When the :tab modifier is used new windows are opened in a new tab, up to 'tabpagemax'.]],
    short_desc = 'open a window for each buffer in the buffer list',
  },
  {
    command = 'badd',
    flags = bit.bor(NEEDARG, FILE1, CMDARG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_edit',
    desc = [[[+lnum] {fname} Add file name {fname} to the buffer list, without loading it, if it wasn't listed yet. If the buffer was previously deleted, not wiped, it will be made listed again. If "lnum" is specified, the cursor will be positioned at that line when the buffer is first entered. Note that other commands after the + will be ignored.]],
    short_desc = 'add buffer to the buffer list',
  },
  {
    command = 'balt',
    flags = bit.bor(NEEDARG, FILE1, CMDARG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_edit',
    desc = 'Like `:badd` and also set the alternate file for the current window to {fname}.',
    short_desc = 'like ":badd" but also set the alternate file',
  },
  {
    command = 'bdelete',
    flags = bit.bor(BANG, RANGE, BUFNAME, COUNT, EXTRA, TRLBAR),
    addr_type = 'ADDR_BUFFERS',
    func = 'ex_bunload',
    desc = 'Unload buffer [N] (default: current buffer) and delete it from the buffer list. If the buffer was changed, this fails, unless [!] is specified, in which case changes are lost. The file remains unaffected. Any windows for this buffer are closed. If buffer [N] is the current buffer, another buffer will be displayed instead. This is the most recent entry in the jump list that points into a loaded buffer.',
    short_desc = 'remove a buffer from the buffer list',
  },
  {
    command = 'belowright',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd}. If it contains a command that splits a window, it will be opened right (vertical split) or below (horizontal split) the current window. Overrules 'splitbelow' and 'splitright'. Doesn't work for :execute and :normal.]],
    short_desc = 'make split window appear right or below',
  },
  {
    command = 'bfirst',
    flags = bit.bor(BANG, RANGE, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_brewind',
    desc = 'Same as :brewind. Also see +cmd.',
    short_desc = 'go to first buffer in the buffer list',
  },
  {
    command = 'blast',
    flags = bit.bor(BANG, RANGE, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_blast',
    desc = 'Go to last buffer in buffer list. If the buffer list is empty, go to the last unlisted buffer. See :buffer-! for [!].',
    short_desc = 'go to last buffer in the buffer list',
  },
  {
    command = 'bmodified',
    flags = bit.bor(BANG, RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bmodified',
    desc = 'Go to [N]th next modified buffer. Note: this command also finds unlisted buffers. If there is no modified buffer the command fails.',
    short_desc = 'go to next buffer in the buffer list that has',
  },
  {
    command = 'bnext',
    flags = bit.bor(BANG, RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bnext',
    desc = [[Go to [N]th next buffer in buffer list. [N] defaults to one. Wraps around the end of the buffer list. See :buffer-! for [!]. Also see +cmd. If you are in a help buffer, this takes you to the next help buffer (if there is one). Similarly, if you are in a normal (non-help) buffer, this takes you to the next normal buffer. This is so that if you have invoked help, it doesn't get in the way when you're browsing code/text buffers. The next three commands also work like this.]],
    short_desc = 'go to next buffer in the buffer list',
  },
  {
    command = 'botright',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd}. If it contains a command that splits a window, it will appear at the bottom and occupy the full width of the Vim window. When the split is vertical the window appears at the far right and occupies the full height of the Vim window. Doesn't work for :execute and :normal.]],
    short_desc = 'make split window appear at bottom or far right',
  },
  {
    command = 'bprevious',
    flags = bit.bor(BANG, RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bprevious',
    desc = 'Mapped to :bprevious. default-mappings',
    short_desc = 'go to previous buffer in the buffer list',
  },
  {
    command = 'brewind',
    flags = bit.bor(BANG, RANGE, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_brewind',
    desc = 'Go to first buffer in buffer list. If the buffer list is empty, go to the first unlisted buffer. See :buffer-! for [!].',
    short_desc = 'go to first buffer in the buffer list',
  },
  {
    command = 'break',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_break',
    desc = 'When used inside a `:while` or `:for` loop, skips to the command after the matching `:endwhile` or `:endfor`. If it is used after a `:try` inside the loop but before the matching `:finally` (if present), the commands following the `:finally` up to the matching `:endtry` are executed first. This process applies to all nested `:try`s inside the loop. The outermost `:endtry` then jumps to the command after the loop.',
    short_desc = 'break out of while loop',
  },
  {
    command = 'breakadd',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_breakadd',
    desc = 'Set a breakpoint in a function. Example: > :breakadd func Explore',
    short_desc = 'add a debugger breakpoint',
  },
  {
    command = 'breakdel',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_breakdel',
    desc = 'Delete breakpoint {nr}. Use :breaklist to see the number of each breakpoint. Delete all breakpoints. Delete a breakpoint in a function. Delete a breakpoint in a sourced file. Delete a breakpoint at the current line of the current file.',
    short_desc = 'delete a debugger breakpoint',
  },
  {
    command = 'breaklist',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_breaklist',
    desc = 'List all breakpoints.',
    short_desc = 'list debugger breakpoints',
  },
  {
    command = 'browse',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Open a file selection dialog for an argument to {command}. At present this works for :e, :w, :wall, :wq, :wqall, :x, :xall, :exit, :view, :sview, :r, :saveas, :sp, :mkexrc, :mkvimrc, :mksession, :mkview, :split, :vsplit, :tabe, :tabnew, :cfile, :cgetfile, :caddfile, :lfile, :lgetfile, :laddfile, :diffsplit, :diffpatch, :pedit, :redir, :source, :update, :visual, :vsplit, and :qall if 'confirm' is set.]],
    short_desc = 'use file selection dialog',
  },
  {
    command = 'buffers',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'buflist_list',
    desc = 'Show all buffers. Example: 1 #h "/test/text" line 1 ~ 2u "asdf" line 0 ~ 3 %a + "version.c" line 1 ~ When the [!] is included the list will show unlisted buffers (the term "unlisted" is a bit confusing then...). Each buffer has a unique number. That number will not change, thus you can always go to a specific buffer with ":buffer N" or "N CTRL-^", where N is the buffer number.',
    short_desc = 'list all files in the buffer list',
  },
  {
    command = 'bufdo',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_BUFFERS',
    func = 'ex_listdo',
    desc = 'Execute {cmd} in each buffer in the buffer list or if [range] is given only for buffers for which their buffer number is in the [range]. It works like doing this: > :bfirst :{cmd} :bnext :{cmd} etc.',
    short_desc = 'execute command in each listed buffer',
  },
  {
    command = 'bunload',
    flags = bit.bor(BANG, RANGE, BUFNAME, COUNT, EXTRA, TRLBAR),
    addr_type = 'ADDR_LOADED_BUFFERS',
    func = 'ex_bunload',
    desc = 'Unload buffer [N] (default: current buffer). The memory allocated for this buffer will be freed. The buffer remains in the buffer list. If the buffer was changed, this fails, unless when [!] is specified, in which case the changes are lost. Any windows for this buffer are closed. If buffer [N] is the current buffer, another buffer will be displayed instead. This is the most recent entry in the jump list that points into a loaded buffer. Like ":bunload[!] [N]", but buffer given by name.',
    short_desc = 'unload a specific buffer',
  },
  {
    command = 'bwipeout',
    flags = bit.bor(BANG, RANGE, BUFNAME, BUFUNL, COUNT, EXTRA, TRLBAR),
    addr_type = 'ADDR_BUFFERS',
    func = 'ex_bunload',
    desc = [[Like :bdelete, but really delete the buffer. Everything related to the buffer is lost. All marks in this buffer become invalid, option settings are lost, the jumplist and tagstack data will be purged, etc. Don't use this unless you know what you are doing. Examples: > :.+,$bwipeout " wipe out all buffers after the current " one :%bwipeout " wipe out all buffers]],
    short_desc = 'really delete a buffer',
  },
  {
    command = 'change',
    flags = bit.bor(BANG, WHOLEFOLD, RANGE, COUNT, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_change',
    desc = [[Replace lines of text with some different text. Type a line containing only "." to stop replacing. Without {range}, this command changes only the current line. Adding [!] toggles 'autoindent' for the time this command is executed.]],
    short_desc = 'replace a line or series of lines',
  },
  {
    command = 'cNext',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = [[Display the [count] previous error in the list that includes a file name. If there are no file names at all, go to the [count] previous error. See :cc for [!] and 'switchbuf'.]],
    short_desc = 'go to previous error',
  },
  {
    command = 'cNfile',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = [[Display the last error in the [count] previous file in the list that includes a file name. If there are no file names at all or if there is no next file, go to the [count] previous error. See :cc for [!] and 'switchbuf'.]],
    short_desc = 'go to last error in previous file',
  },
  {
    command = 'cabbrev',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'same as ":ab", but for Command-line mode only.',
    short_desc = 'like ":abbreviate" but for Command-line mode',
  },
  {
    command = 'cabclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abclear',
    desc = 'Remove all abbreviations for Command-line mode.',
    short_desc = 'clear all abbreviations for Command-line mode',
  },
  {
    command = 'cabove',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Go to the [count] error above the current line in the current buffer. If [count] is omitted, then 1 is used. If there are no errors, then an error message is displayed. Assumes that the entries in a quickfix list are sorted by their buffer number and line number. If there are multiple errors on the same line, then only the first entry is used. If [count] exceeds the number of entries above the current line, then the first error in the file is selected.',
    short_desc = 'go to error above current line',
  },
  {
    command = 'caddbuffer',
    flags = bit.bor(RANGE, WORD1, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_cbuffer',
    desc = 'Read the error list from the current buffer and add the errors to the current quickfix list. If a quickfix list is not present, then a new list is created. Otherwise, same as ":cbuffer".',
    short_desc = 'add errors from buffer',
  },
  {
    command = 'caddexpr',
    flags = bit.bor(NEEDARG, WORD1, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_cexpr',
    desc = 'Evaluate {expr} and add the resulting lines to the current quickfix list. If a quickfix list is not present, then a new list is created. The current cursor position will not be changed. See :cexpr for more information. Example: > :g/mypattern/caddexpr expand("%") .. ":" .. line(".") .. ":" .. getline(".")',
    short_desc = 'add errors from expr',
  },
  {
    command = 'caddfile',
    flags = bit.bor(TRLBAR, FILE1),
    addr_type = 'ADDR_NONE',
    func = 'ex_cfile',
    desc = [[Read the error file and add the errors from the errorfile to the current quickfix list. If a quickfix list is not present, then a new list is created. If the encoding of the error file differs from the 'encoding' option, you can use the 'makeencoding' option to specify the encoding.]],
    short_desc = 'add error message to current quickfix list',
  },
  {
    command = 'cafter',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Go to the [count] error after the current cursor position in the current buffer. If [count] is omitted, then 1 is used. If there are no errors, then an error message is displayed. Assumes that the entries in a quickfix list are sorted by their buffer, line and column numbers. If [count] exceeds the number of entries after the current position, then the last error in the file is selected.',
    short_desc = 'go to error after current cursor',
  },
  {
    command = 'call',
    flags = bit.bor(RANGE, NEEDARG, EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_call',
    desc = 'Call a function. The name of the function and its arguments are as specified with `:function`. Up to 20 arguments can be used. The returned value is discarded. Without a range and for functions that accept a range, the function is called once. When a range is given the cursor is positioned at the start of the first line before executing the function.',
    short_desc = 'call a function',
  },
  {
    command = 'catch',
    flags = bit.bor(EXTRA, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_catch',
    desc = 'The following commands until the next `:catch`, `:finally`, or `:endtry` that belongs to the same `:try` as the `:catch` are executed when an exception matching {pattern} is being thrown and has not yet been caught by a previous `:catch`. Otherwise, these commands are skipped. Pattern can start with "Vim({cmd})" to indicate an exception that occurred when executing the Ex command {cmd}. When {pattern} is omitted all errors are caught.',
    short_desc = 'part of a :try command',
  },
  {
    command = 'cbuffer',
    flags = bit.bor(BANG, RANGE, WORD1, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_cbuffer',
    desc = 'Read the error list from the current buffer. When [bufnr] is given it must be the number of a loaded buffer. That buffer will then be used instead of the current buffer. A range can be specified for the lines to be used. Otherwise all lines in the buffer are used. See :cc for [!].',
    short_desc = 'parse error messages and jump to first error',
  },
  {
    command = 'cbefore',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Go to the [count] error before the current cursor position in the current buffer. If [count] is omitted, then 1 is used. If there are no errors, then an error message is displayed. Assumes that the entries in a quickfix list are sorted by their buffer, line and column numbers. If [count] exceeds the number of entries before the current position, then the first error in the file is selected.',
    short_desc = 'go to error before current cursor',
  },
  {
    command = 'cbelow',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Go to the [count] error below the current line in the current buffer. If [count] is omitted, then 1 is used. If there are no errors, then an error message is displayed. Assumes that the entries in a quickfix list are sorted by their buffer number and line number. If there are multiple errors on the same line, then only the first entry is used. If [count] exceeds the number of entries below the current line, then the last error in the file is selected.',
    short_desc = 'go to error below current line',
  },
  {
    command = 'cbottom',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_cbottom',
    desc = 'Put the cursor in the last line of the quickfix window and scroll to make it visible. This is useful for when errors are added by an asynchronous callback. Only call it once in a while if there are many updates to avoid a lot of redrawing.',
    short_desc = 'scroll to the bottom of the quickfix window',
  },
  {
    command = 'cc',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_QUICKFIX',
    func = 'ex_cc',
    desc = 'Display error [nr]. If [nr] is omitted, the same',
    short_desc = 'go to specific error',
  },
  {
    command = 'cclose',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_cclose',
    desc = 'Close the quickfix window.',
    short_desc = 'close quickfix window',
  },
  {
    command = 'cd',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_cd',
    desc = [[On non-Unix systems when 'cdhome' is off: Print the current directory name. Otherwise: Change the current directory to the home directory. Clear any window-local directory. Use :pwd to print the current directory on all systems. Change the current directory to {path}. If {path} is relative, it is searched for in the directories listed in 'cdpath'. Clear any window-local directory. Does not change the meaning of an already opened file, because its full path name is remembered.]],
    short_desc = 'change directory',
  },
  {
    command = 'cdo',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_QUICKFIX_VALID',
    func = 'ex_listdo',
    desc = 'Execute {cmd} in each valid entry in the quickfix list. It works like doing this: > :cfirst :{cmd} :cnext :{cmd} etc.',
    short_desc = 'execute command in each valid error list entry',
  },
  {
    command = 'center',
    flags = bit.bor(TRLBAR, RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_align',
    desc = [[Center lines in [range] between [width] columns (default 'textwidth' or 80 when 'textwidth' is 0).]],
    short_desc = 'format lines at the center',
  },
  {
    command = 'cexpr',
    flags = bit.bor(NEEDARG, WORD1, NOTRLCOM, BANG),
    addr_type = 'ADDR_NONE',
    func = 'ex_cexpr',
    desc = [[Create a quickfix list using the result of {expr} and jump to the first error. If {expr} is a String, then each newline terminated line in the String is processed using the global value of 'errorformat' and the result is added to the quickfix list. If {expr} is a List, then each String item in the list is processed and added to the quickfix list. Non String items in the List are ignored. See :cc for [!]. Examples: > :cexpr system('grep -n xyz *') :cexpr getline(1, '$')]],
    short_desc = 'read errors from expr and jump to first',
  },
  {
    command = 'cfile',
    flags = bit.bor(TRLBAR, FILE1, BANG),
    addr_type = 'ADDR_NONE',
    func = 'ex_cfile',
    desc = [[Read the error file and jump to the first error. This is done automatically when Vim is started with the -q option. You can use this command when you keep Vim running while compiling. If you give the name of the errorfile, the 'errorfile' option will be set to [errorfile]. See :cc for [!]. If the encoding of the error file differs from the 'encoding' option, you can use the 'makeencoding' option to specify the encoding.]],
    short_desc = 'read file with error messages and jump to first',
  },
  -- Even though 'cfdo' is alphabetically lower than 'cfile', it is after
  -- 'cfile' in this cmd list to support the existing ":cf" abbreviation.
  {
    command = 'cfdo',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_QUICKFIX_VALID',
    func = 'ex_listdo',
    desc = 'Execute {cmd} in each file in the quickfix list. It works like doing this: > :cfirst :{cmd} :cnfile :{cmd} etc.',
    short_desc = 'execute command in each file in error list',
  },
  {
    command = 'cfirst',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cc',
    desc = 'Same as ":crewind".',
    short_desc = 'go to the specified error, default first one',
  },
  {
    command = 'cgetfile',
    flags = bit.bor(TRLBAR, FILE1),
    addr_type = 'ADDR_NONE',
    func = 'ex_cfile',
    desc = [[Read the error file. Just like ":cfile" but don't jump to the first error. If the encoding of the error file differs from the 'encoding' option, you can use the 'makeencoding' option to specify the encoding.]],
    short_desc = 'read file with error messages',
  },
  {
    command = 'cgetbuffer',
    flags = bit.bor(RANGE, WORD1, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_cbuffer',
    desc = [[Read the error list from the current buffer. Just like ":cbuffer" but don't jump to the first error.]],
    short_desc = 'get errors from buffer',
  },
  {
    command = 'cgetexpr',
    flags = bit.bor(NEEDARG, WORD1, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_cexpr',
    desc = [[Create a quickfix list using the result of {expr}. Just like :cexpr, but don't jump to the first error.]],
    short_desc = 'get errors from expr',
  },
  {
    command = 'chdir',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_cd',
    desc = 'Same as :cd.',
    short_desc = 'change directory',
  },
  {
    command = 'changes',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_changes',
    desc = 'Print the change list. A ">" character indicates the current position. Just after a change it is below the newest entry, indicating that `g;` takes you to the newest entry position. The first column indicates the count needed to take you to this position. Example: change line col text ~ 3 9 8 bla bla bla 2 11 57 foo is a bar 1 14 54 the latest changed line > The `3g;` command takes you to line 9.',
    short_desc = 'print the change list',
  },
  {
    command = 'checkhealth',
    flags = bit.bor(EXTRA, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_checkhealth',
    desc = [[Run all healthchecks. Nvim depends on $VIMRUNTIME, 'runtimepath' and 'packpath' to find the standard "runtime files" for syntax highlighting, filetype-specific behavior, and standard plugins (including :checkhealth). If the runtime files cannot be found then those features will not work. Run healthcheck(s) for one or more plugins. E.g. to run only the standard Nvim healthcheck: >vim :checkhealth vim.health]],
    short_desc = 'run healthchecks',
  },
  {
    command = 'checkpath',
    flags = bit.bor(TRLBAR, BANG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_checkpath',
    desc = 'List all the included files that could not be found. List all the included files.',
    short_desc = 'list included files',
  },
  {
    command = 'checktime',
    flags = bit.bor(RANGE, BUFNAME, COUNT, EXTRA, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_checktime',
    desc = 'Check if any buffers were changed outside of Vim. This checks and warns you if you would end up with two versions of a file. If this is called from an autocommand, a ":global" command or is not typed the actual check is postponed until a moment the side effects (reloading the file) would be harmless. Each loaded buffer is checked for its associated file being changed. If the file was changed Vim will take action.',
    short_desc = 'check timestamp of loaded buffers',
  },
  {
    command = 'chistory',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'qf_history',
    desc = 'Show the list of error lists. The current list is marked with ">". The output looks like: > error list 1 of 3; 43 errors :make > error list 2 of 3; 0 errors :helpgrep tag error list 3 of 3; 15 errors :grep ex_help *.c',
    short_desc = 'list the error lists',
  },
  {
    command = 'clist',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'qf_list',
    desc = 'List all errors that are valid quickfix-valid. If numbers [from] and/or [to] are given, the respective range of errors is listed. A negative number counts from the last error backwards, -1 being the last error. The :filter command can be used to display only the quickfix entries matching a supplied pattern. The pattern is matched against the filename, module name, pattern and text of the entry. List the current and next {count} valid errors.',
    short_desc = 'list all errors',
  },
  {
    command = 'clast',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cc',
    desc = 'Display error [nr]. If [nr] is omitted, the LAST error is displayed. See :cc.',
    short_desc = 'go to the specified error, default last one',
  },
  {
    command = 'close',
    flags = bit.bor(BANG, RANGE, COUNT, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_WINDOWS',
    func = 'ex_close',
    desc = [[Without {count}: Close the current window. If given close the {count} window. When 'hidden' is set, or when the buffer was changed and the [!] is used, the buffer becomes hidden (unless there is another window editing it). When there is only one edit-window in the current tab page and there is another tab page, this closes the current tab page. tab-page. This command fails when: - There is only one window on the screen.]],
    short_desc = 'close current window (like )',
  },
  {
    command = 'clearjumps',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_clearjumps',
    desc = 'Clear the jump list of the current window.',
    short_desc = 'clear the jump list',
  },
  {
    command = 'cmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-c',
    short_desc = 'like ":map" but for Command-line mode',
  },
  {
    command = 'cmapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-c',
    short_desc = 'clear all mappings for Command-line mode',
  },
  {
    command = 'cmenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Command-line mode',
  },
  {
    command = 'cnext',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = [[Display the [count] next error in the list that includes a file name. If there are no file names at all, go to the [count] next error. See :cc for [!] and 'switchbuf'.]],
    short_desc = 'go to next error',
  },
  {
    command = 'cnewer',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'qf_age',
    desc = 'Go to newer error list. When [count] is given, do this [count] times. When already at the newest error list, an error message is given.',
    short_desc = 'go to newer error list',
  },
  {
    command = 'cnfile',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = [[Display the first error in the [count] next file in the list that includes a file name. If there are no file names at all or if there is no next file, go to the [count] next error. See :cc for [!] and 'switchbuf'.]],
    short_desc = 'go to first error in next file',
  },
  {
    command = 'cnoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-c',
    short_desc = 'like ":noremap" but for Command-line mode',
  },
  {
    command = 'cnoreabbrev',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'same as ":ab", but for Command-line mode only and no remapping for this {rhs}',
    short_desc = 'like ":noreabbrev" but for Command-line mode',
  },
  {
    command = 'cnoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Command-line mode',
  },
  {
    command = 'copy',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_copymove',
    desc = 'Copy the lines given by [range] to below the line given by {address}.',
    short_desc = 'copy lines',
  },
  {
    command = 'colder',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'qf_age',
    desc = 'Go to older error list. When [count] is given, do this [count] times. When already at the oldest error list, an error message is given.',
    short_desc = 'go to older error list',
  },
  {
    command = 'colorscheme',
    flags = bit.bor(WORD1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_colorscheme',
    desc = 'Output the name of the currently active color scheme. This is basically the same as > :echo g:colors_name',
    short_desc = 'load a specific color scheme',
  },
  {
    command = 'command',
    flags = bit.bor(EXTRA, BANG, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_command',
    desc = 'List all user-defined commands. When listing commands, the characters in the first columns are: ! Command has the -bang attribute " Command has the -register attribute | Command has the -bar attribute b Command is local to current buffer (see below for details on attributes) The list can be filtered on command name with :filter, e.g., to list all commands with "Pyth" in the name: > filter Pyth command List the user-defined commands that start with {cmd}',
    short_desc = 'create user-defined command',
  },
  {
    command = 'comclear',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_comclear',
    desc = 'Delete all user-defined commands.',
    short_desc = 'clear all user-defined commands',
  },
  {
    command = 'compiler',
    flags = bit.bor(BANG, TRLBAR, WORD1, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_compiler',
    desc = 'Set options to work with compiler {name}. Without the "!" options are set for the current buffer. With "!" global options are set. If you use ":compiler foo" in "file.foo" and then ":compiler! bar" in another buffer, Vim will keep on using "foo" in "file.foo".',
    short_desc = 'do settings for a specific compiler',
  },
  {
    command = 'continue',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_continue',
    desc = 'When used inside a `:while` or `:for` loop, jumps back to the start of the loop. If it is used after a `:try` inside the loop but before the matching `:finally` (if present), the commands following the `:finally` up to the matching `:endtry` are executed first. This process applies to all nested `:try`s inside the loop. The outermost `:endtry` then jumps back to the start of the loop.',
    short_desc = 'go back to :while',
  },
  {
    command = 'confirm',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = 'Execute {command}, and use a dialog when an operation has to be confirmed. Can be used on the :edit, :restart, :q, :qa and :w commands (the latter to override a read-only setting), and any commands that can fail because of unsaved changes, such as :only, :buffer, :bdelete, etc.',
    short_desc = 'prompt user when confirmation required',
  },
  {
    command = 'connect',
    flags = bit.bor(BANG, FILE1, NOTRLCOM, NEEDARG),
    addr_type = 'ADDR_NONE',
    func = 'ex_connect',
    desc = [[Detaches the UI from the server it is currently attached to and attaches it to the server at {address} instead. Note: If the current UI hasn't implemented the "connect" UI event, this command is equivalent to :detach. Same as :connect but it also stops the detached server if no other UI is currently attached to it.]],
  },
  {
    command = 'const',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_let',
    desc = 'Similar to :let, but additionally lock the variable after setting the value. This is the same as locking the variable with :lockvar just after :let, thus: > :const x = 1',
    short_desc = 'create a variable as a constant',
  },
  {
    command = 'copen',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_copen',
    desc = [[Open a window to show the current list of errors. When [height] is given, the window becomes that high (if there is room). When [height] is omitted the window is made ten lines high. If there already is a quickfix window, it will be made the current window. It is not possible to open a second quickfix window. If [height] is given the existing window will be resized to it. The window will contain a special buffer, with 'buftype' equal to "quickfix".]],
    short_desc = 'open quickfix window',
  },
  {
    command = 'cprevious',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = [[Display the [count] previous error in the list that includes a file name. If there are no file names at all, go to the [count] previous error. See :cc for [!] and 'switchbuf'.]],
    short_desc = 'go to previous error',
  },
  {
    command = 'cpfile',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_OTHER',
    func = 'ex_cnext',
    desc = [[Display the last error in the [count] previous file in the list that includes a file name. If there are no file names at all or if there is no next file, go to the [count] previous error. See :cc for [!] and 'switchbuf'.]],
    short_desc = 'go to last error in previous file',
  },
  {
    command = 'cquit',
    flags = bit.bor(RANGE, COUNT, ZEROR, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cquit',
    desc = 'Quit Vim with error code {N}. {N} defaults to one. Useful when Vim is called from another program: e.g., a compiler will not compile the same file again, `git commit` will abort the committing process, `fc` (built-in for shells like bash and zsh) will not execute the command, etc. {N} can also be zero, in which case Vim exits normally. WARNING: All changes in files are lost. It works like ":qall!" :qall, except that Nvim exits non-zero or [count].',
    short_desc = 'quit Vim with an error code',
  },
  {
    command = 'crewind',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cc',
    desc = 'Display error [nr]. If [nr] is omitted, the FIRST error is displayed. See :cc.',
    short_desc = 'go to the specified error, default first one',
  },
  {
    command = 'cunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-c',
    short_desc = 'like ":unmap" but for Command-line mode',
  },
  {
    command = 'cunabbrev',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'Same as ":una", but for Command-line mode only.',
    short_desc = 'like ":unabbrev" but for Command-line mode',
  },
  {
    command = 'cunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Command-line mode',
  },
  {
    command = 'cwindow',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_cwindow',
    desc = 'Open the quickfix window when there are recognized errors. If the window is already open and there are no recognized errors, close the window. When opening the window and [height] is given, the window becomes that high (if there is room). When [height] is omitted the window is made ten lines high.',
    short_desc = 'open or close quickfix window',
  },
  {
    command = 'delete',
    flags = bit.bor(RANGE, WHOLEFOLD, REGSTR, COUNT, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_operators',
    desc = 'Delete [range] lines (default: current line) [into register x]. Note these weird abbreviations: :dl delete and list :dell idem :delel idem :deletl idem :deletel idem :dp delete and print :dep idem :delp idem :delep idem :deletp idem :deletep idem',
    short_desc = 'delete lines',
  },
  {
    command = 'delmarks',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_delmarks',
    desc = [[Delete the specified marks. Marks that can be deleted include A-Z and 0-9. You cannot delete the ' mark. They can be specified by giving the list of mark names, or with a range, separated with a dash. Spaces are ignored. Examples: > :delmarks a deletes mark a :delmarks a b 1 deletes marks a, b and 1 :delmarks Aa deletes marks A and a :delmarks p-z deletes marks in the range p to z :delmarks ^.[] deletes marks ^ . [ ] :delmarks \" deletes mark "]],
    short_desc = 'delete marks',
  },
  {
    command = 'debug',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_debug',
    desc = 'Debugging will only be done while this command executes. Useful for debugging a specific script or user function. And for scripts and functions used by autocommands. Example: > :debug edit test.txt.gz',
    short_desc = 'run a command in debugging mode',
  },
  {
    command = 'debuggreedy',
    flags = bit.bor(RANGE, ZEROR, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_debuggreedy',
    desc = [[Read debug mode commands from the normal input stream, instead of getting them directly from the user. Only useful for test scripts. Example: > echo 'q^Mq' | vim -e -s -c debuggreedy -c 'breakadd file script.vim' -S script.vim]],
    short_desc = 'read debug mode commands from normal input',
  },
  {
    command = 'defer',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_call',
    desc = 'Call {func} when the current function is done. {args} are evaluated here.',
    short_desc = 'call function when current function is done',
  },
  {
    command = 'delcommand',
    flags = bit.bor(BANG, NEEDARG, WORD1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_delcommand',
    desc = 'Delete the user-defined command {cmd}.',
    short_desc = 'delete user-defined command',
  },
  {
    command = 'delfunction',
    flags = bit.bor(BANG, NEEDARG, WORD1, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_delfunction',
    desc = 'Delete function {name}. {name} can also be a Dictionary entry that is a Funcref: > :delfunc dict.init',
    short_desc = 'delete a user function',
  },
  {
    command = 'detach',
    flags = TRLBAR,
    addr_type = 'ADDR_NONE',
    func = 'ex_detach',
    desc = 'Detaches the current UI. Other UIs (if any) remain attached. The server (typically `nvim --embed`) continues running as a background process, and you can reattach to it later. Before detaching, you may want to note the server address: >vim :echo v:servername',
    short_desc = 'detach the current UI',
  },
  {
    command = 'display',
    flags = bit.bor(EXTRA, NOTRLCOM, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_display',
    desc = 'Same as :registers.',
    short_desc = 'display registers',
  },
  {
    command = 'diffupdate',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_diffupdate',
    desc = 'Update the diff highlighting and folds.',
    short_desc = [[update 'diff' buffers]],
  },
  {
    command = 'diffget',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_diffgetput',
    desc = 'Modify the current buffer to undo difference with another buffer. If [bufspec] is given, that buffer is used. If [bufspec] refers to the current buffer then nothing happens. Otherwise this only works if there is one other buffer in diff mode. See below for [range].',
    short_desc = 'remove differences in current buffer',
  },
  {
    command = 'diffoff',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_diffoff',
    desc = [[Switch off diff mode for the current window. Resets related options also when 'diff' was not set. Switch off diff mode for the current window and in all windows in the current tab page where 'diff' is set. Resetting related options only happens in a window that has 'diff' set, if the current window does not have 'diff' set then no options in it are changed. Hidden buffers are also removed from the list of diff'ed buffers.]],
    short_desc = 'switch off diff mode',
  },
  {
    command = 'diffpatch',
    flags = bit.bor(EXTRA, FILE1, TRLBAR, MODIFY),
    addr_type = 'ADDR_NONE',
    func = 'ex_diffpatch',
    desc = [[Use the current buffer, patch it with the diff found in {patchfile} and open a buffer on the result. This sets the options as for "nvim -d". {patchfile} can be in any format that the "patch" program understands or 'patchexpr' can handle. Note that {patchfile} should only contain a diff for one file, the current file. If {patchfile} contains diffs for other files as well, the results are unpredictable.]],
    short_desc = 'apply a patch and show differences',
  },
  {
    command = 'diffput',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_diffgetput',
    desc = [[Modify another buffer to undo difference with the current buffer. Just like ":diffget" but the other buffer is modified instead of the current one. When [bufspec] is omitted and there is more than one other buffer in diff mode where 'modifiable' is set this fails. See below for [range].]],
    short_desc = 'remove differences in other buffer',
  },
  {
    command = 'diffsplit',
    flags = bit.bor(EXTRA, FILE1, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_diffsplit',
    desc = [[Open a new window on the file {filename}. The options are set as for "nvim -d" for the current and the newly opened window. Also see 'diffexpr'.]],
    short_desc = 'show differences with another file',
  },
  {
    command = 'diffthis',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_diffthis',
    desc = 'Make the current window part of the diff windows. This sets the options as for "nvim -d".',
    short_desc = 'make current window a diff window',
  },
  {
    command = 'digraphs',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_digraphs',
    desc = 'Show currently defined digraphs. With [!] headers are used to make it a bit easier to find a specific character. Add digraph {char1}{char2} to the list. {number} is the decimal representation of the character. Normally it is the Unicode character, see digraph-encoding. Example: > :digr e: 235 a: 228',
    short_desc = 'show or enter digraphs',
  },
  {
    command = 'djump',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like "[ CTRL-D" and "] CTRL-D", but search in [range] lines (default: whole file). See :search-args for [/] and [!].',
    short_desc = 'jump to #define',
  },
  {
    command = 'dlist',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like `[D` and `]D`, but search in [range] lines (default: whole file). See :search-args for [/] and [!]. Note that `:dl` works like `:delete` with the "l" flag, not `:dlist`.',
    short_desc = 'list #defines',
  },
  {
    command = 'doautocmd',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_doautocmd',
    desc = [[Apply the autocommands matching [fname] (default: current file name) for {event} to the current buffer. You can use this when the current file name does not match the right pattern, after changing settings, or to execute autocommands for a certain event. It's possible to use this inside an autocommand too, so you can base the autocommands for one extension on another extension. Example: > :au BufEnter *.cpp so ~/.config/nvim/init_cpp.vim :au BufEnter *.cpp doau BufEnter x.c]],
    short_desc = 'apply autocommands to current buffer',
  },
  {
    command = 'doautoall',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_doautoall',
    desc = [[Like ":doautocmd", but apply the autocommands to each loaded buffer. The current buffer is done last. Note that [fname] is used to select the autocommands, not the buffers to which they are applied. Example: > augroup mine autocmd! autocmd FileType * echo expand('<amatch>') augroup END doautoall mine FileType Loaded-Buffer]],
    short_desc = 'apply autocommands for all loaded buffers',
  },
  {
    command = 'drop',
    flags = bit.bor(BANG, FILES, CMDARG, NEEDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_drop',
    desc = [[Edit the first {file} in a window. - If the file is already open in a window change to that window. - If the file is not open in a window edit the file in the current window. If the current buffer can't be abandoned, the window is split first. - Windows that are not in the argument list or are not full width will be closed if possible. The argument-list is set, like with the :next command.]],
    short_desc = 'jump to window editing file or edit file in',
  },
  {
    command = 'dsearch',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like "[d" and "]d", but search in [range] lines (default: whole file). See :search-args for [/] and [!].',
    short_desc = 'list one #define',
  },
  {
    command = 'dsplit',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like "CTRL-W d", but search in [range] lines (default: whole file). See :search-args for [/] and [!].',
    short_desc = 'split window and jump to #define',
  },
  {
    command = 'edit',
    flags = bit.bor(BANG, FILE1, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_edit',
    desc = [[Edit the current file. This is useful to re-edit the current file, when it has been changed outside of Vim. If [!] is given, unsaved changes in the current buffer are discarded. Without [!] the command fails if there are unsaved changes, unless 'autowriteall' is set and the file can be written. Also see ++opt and +cmd. Edit {file}. If [!] is given, unsaved changes in the current buffer are discarded.]],
    short_desc = 'edit a file',
  },
  {
    command = 'earlier',
    flags = bit.bor(TRLBAR, EXTRA, NOSPC, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_later',
    desc = 'Go to older text state {count} times. Go to older text state about {N} seconds before. Go to older text state about {N} minutes before. Go to older text state about {N} hours before. Go to older text state about {N} days before. Go to older text state {N} file writes before. When changes were made since the last write ":earlier 1f" will revert the text to the state when it was written. Otherwise it will go to the write before that.',
    short_desc = 'go to older change, undo',
  },
  {
    command = 'echo',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_echo',
    desc = [[Echoes each {expr1}, with a space in between. The first {expr1} starts on a new line. Also see :comment. Use "\n" to start a new line. Use "\r" to move the cursor to the first column. Uses the highlighting set by the `:echohl` command. Cannot be followed by a comment. Example: > :echo "the value of 'shell' is" &shell]],
    short_desc = 'echoes the result of expressions',
  },
  {
    command = 'echoerr',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_execute',
    desc = 'Echo the expression(s) as an error message, saving the message in the message-history. When used in a script or function the line number will be added. Spaces are placed between the arguments as with the `:echomsg` command. When used inside a try conditional, the message is raised as an error exception instead (see try-echoerr). Example: > :echoerr "This script just failed!"',
    short_desc = 'like :echo, show like an error and use history',
  },
  {
    command = 'echohl',
    flags = bit.bor(EXTRA, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_echohl',
    desc = [[Use the highlight group {name} for the following `:echo`, `:echon` and `:echomsg` commands. Also used for the `input()` prompt. Example: > :echohl WarningMsg | echo "Don't panic!" | echohl None]],
    short_desc = 'set highlighting for echo commands',
  },
  {
    command = 'echomsg',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_execute',
    desc = 'Echo the expression(s) as a true message, saving the message in the message-history. Spaces are placed between the arguments as with the `:echo` command. But unprintable characters are displayed, not interpreted. The parsing works slightly different from `:echo`, more like `:execute`. All the expressions are first evaluated and concatenated before echoing anything. If expressions does not evaluate to a Number or String, string() is used to turn it into a string.',
    short_desc = 'same as :echo, put message in history',
  },
  {
    command = 'echon',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_echo',
    desc = [[Echoes each {expr1}, without anything added. Also see :comment. Uses the highlighting set by the `:echohl` command. Cannot be followed by a comment. Example: > :echon "the value of 'shell' is " &shell]],
    short_desc = 'same as :echo, but without <EOL>',
  },
  {
    command = 'else',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_else',
    desc = 'Execute the commands until the next matching `:else` or `:endif` if they previously were not being executed.',
    short_desc = 'part of an :if command',
  },
  {
    command = 'elseif',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_else',
    desc = 'Short for `:else` `:if`, with the addition that there is no extra `:endif`.',
    short_desc = 'part of an :if command',
  },
  {
    command = 'emenu',
    flags = bit.bor(NEEDARG, EXTRA, TRLBAR, NOTRLCOM, RANGE, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_emenu',
    desc = 'Execute {menu} from the command line. The default is to execute the Normal mode menu. If a range is specified, it executes the Visual mode menu. If used from <c-o>, it executes the insert-mode menu Eg: > :emenu File.Exit',
    short_desc = 'execute a menu by name',
  },
  {
    command = 'endif',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_endif',
    desc = 'Execute the commands until the next matching `:else` or `:endif` if {expr1} evaluates to non-zero. Although the short forms work, it is recommended to always use `:endif` to avoid confusion and to make auto-indenting work properly. From Vim version 4.5 until 5.0, every Ex command in between the `:if` and `:endif` is ignored. These two commands were just to allow for future expansions in a backward compatible way. Nesting was allowed.',
    short_desc = 'end previous :if',
  },
  {
    command = 'endfunction',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_endfunction',
    desc = [[The end of a function definition. Best is to put it on a line by its own, without [argument]. [argument] can be: | command command to execute next \n command command to execute next " comment always ignored anything else ignored, warning given when 'verbose' is non-zero The support for a following command was added in Vim 8.0.0654, before that any argument was silently ignored.]],
    short_desc = 'end of a user function started with :function',
  },
  {
    command = 'endfor',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_endwhile',
    desc = 'Repeat the commands between `:for` and `:endfor` for each item in {object}. {object} can be a List, a Blob or a String. Variable {var} is set to the value of each item. When an error is detected for a command inside the loop, execution continues after the `endfor`. Changing {object} inside the loop affects what items are used. Make a copy if this is unwanted: > :for item in copy(mylist)',
    short_desc = 'end previous :for',
  },
  {
    command = 'endtry',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_endtry',
    desc = 'Change the error handling for the commands between `:try` and `:endtry` including everything being executed across `:source` commands, function calls, or autocommand invocations. When an error or interrupt is detected and there is a `:finally` command following, execution continues after the `:finally`. Otherwise, or when the `:endtry` is reached thereafter, the next (dynamically) surrounding `:try` is checked for a corresponding `:finally` etc. Then the script processing is terminated.',
    short_desc = 'end previous :try',
  },
  {
    command = 'endwhile',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_endwhile',
    desc = 'Repeat the commands between `:while` and `:endwhile`, as long as {expr1} evaluates to non-zero. When an error is detected from a command inside the loop, execution continues after the `endwhile`. Example: > :let lnum = 1 :while lnum <= line("$") :call FixLine(lnum) :let lnum = lnum + 1 :endwhile',
    short_desc = 'end previous :while',
  },
  {
    command = 'enew',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_edit',
    desc = [[Edit a new, unnamed buffer. This fails when changes have been made to the current buffer, unless 'hidden' is set or 'autowriteall' is set and the file can be written. If 'fileformats' is not empty, the first format given will be used for the new buffer. If 'fileformats' is empty, the 'fileformat' of the current buffer is used. Edit a new, unnamed buffer. Discard any changes to the current buffer. Set 'fileformat' like :enew.]],
    short_desc = 'edit a new, unnamed buffer',
  },
  {
    command = 'eval',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_eval',
    desc = [[Evaluate {expr} and discard the result. Example: > :eval Getlist()->Filter()->append('$')]],
    short_desc = 'evaluate an expression and discard the result',
  },
  {
    command = 'ex',
    flags = bit.bor(BANG, FILE1, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_edit',
    desc = 'Same as :edit.',
    short_desc = 'same as ":edit"',
  },
  {
    command = 'execute',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_execute',
    desc = 'Executes the string that results from the evaluation of {expr1} as an Ex command. Multiple arguments are concatenated, with a space in between. To avoid the extra space use the ".." operator to concatenate strings into one argument. {expr1} is used as the processed command, command line editing keys are not recognized. Cannot be followed by a comment. Examples: > :execute "buffer" nextbuf :execute "normal" count .. "w"',
    short_desc = 'execute result of expressions',
  },
  {
    command = 'exit',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, FILE1, ARGOPT, DFLALL, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_exit',
    desc = 'Same as :xit.',
    short_desc = 'same as ":xit"',
  },
  {
    command = 'exusage',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_exusage',
    desc = 'Show help on Ex commands. Added to simulate the Nvi command.',
    short_desc = 'overview of Ex commands',
  },
  {
    command = 'file',
    flags = bit.bor(RANGE, ZEROR, BANG, FILE1, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_file',
    desc = [[Prints the current file name (as typed, unless ":cd" was used), the cursor position (unless the 'ruler' option is set), and the file status (readonly, modified, read errors, new file). See the 'shortmess' option about how to make this message shorter. like :file, but don't truncate the name even when 'shortmess' indicates this.]],
    short_desc = 'show or set the current file name',
  },
  {
    command = 'files',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'buflist_list',
    desc = 'Show all buffers. Example: 1 #h "/test/text" line 1 ~ 2u "asdf" line 0 ~ 3 %a + "version.c" line 1 ~ When the [!] is included the list will show unlisted buffers (the term "unlisted" is a bit confusing then...). Each buffer has a unique number. That number will not change, thus you can always go to a specific buffer with ":buffer N" or "N CTRL-^", where N is the buffer number.',
    short_desc = 'list all files in the buffer list',
  },
  {
    command = 'filetype',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_filetype',
    desc = ':filetype on',
    short_desc = 'switch file type detection on/off',
  },
  {
    command = 'filter',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Restrict the output of {command} to lines matching with {pattern}. For example, to list only xml files: > :filter /\.xml$/ oldfiles]],
    short_desc = 'filter output of following command',
  },
  {
    command = 'find',
    flags = bit.bor(RANGE, BANG, FILE1, CMDARG, ARGOPT, TRLBAR, NEEDARG),
    addr_type = 'ADDR_OTHER',
    func = 'ex_find',
    desc = [[Find {file} in 'path' and then :edit it. See also: 'findfunc'.]],
    short_desc = [[find file in 'path' and edit it]],
  },
  {
    command = 'finally',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_finally',
    desc = 'The following commands until the matching `:endtry` are executed whenever the part between the matching `:try` and the `:finally` is left: either by falling through to the `:finally` or by a `:continue`, `:break`, `:finish`, or `:return`, or by an error or interrupt or exception (see `:throw`).',
    short_desc = 'part of a :try command',
  },
  {
    command = 'finish',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_finish',
    desc = 'Stop sourcing a script. Can only be used in a Vim script file. This is a quick way to skip the rest of the file. If it is used after a :try but before the matching :finally (if present), the commands following the ":finally" up to the matching :endtry are executed first. This process applies to all nested ":try"s in the script. The outermost ":endtry" then stops sourcing the script.',
    short_desc = 'quit sourcing a Vim script',
  },
  {
    command = 'first',
    flags = bit.bor(EXTRA, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_rewind',
    desc = 'Other name for ":rewind".',
    short_desc = 'go to the first file in the argument list',
  },
  {
    command = 'fold',
    flags = bit.bor(RANGE, WHOLEFOLD, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_fold',
    desc = 'Create a fold for the lines in {range}. Works like "zf".',
    short_desc = 'create a fold',
  },
  {
    command = 'foldclose',
    flags = bit.bor(RANGE, BANG, WHOLEFOLD, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_foldopen',
    desc = 'Close folds in {range}. When [!] is added all folds are closed. Useful to hide all the text in {range}. Without [!] one level of folds is closed.',
    short_desc = 'close folds',
  },
  {
    command = 'folddoopen',
    flags = bit.bor(RANGE, DFLALL, NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_LINES',
    func = 'ex_folddo',
    desc = 'Execute {cmd} on all lines that are not in a closed fold. When [range] is given, only these lines are used. Each time {cmd} is executed the cursor is positioned on the line it is executed for. This works like the ":global" command: First all lines that are not in a closed fold are marked. Then the {cmd} is executed for all marked lines. Thus when {cmd} changes the folds, this has no influence on where it is executed (except when lines are deleted, of course).',
    short_desc = 'execute command on lines not in a closed fold',
  },
  {
    command = 'folddoclosed',
    flags = bit.bor(RANGE, DFLALL, NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_LINES',
    func = 'ex_folddo',
    desc = 'Execute {cmd} on all lines that are in a closed fold. Otherwise like ":folddoopen".',
    short_desc = 'execute command on lines in a closed fold',
  },
  {
    command = 'foldopen',
    flags = bit.bor(RANGE, BANG, WHOLEFOLD, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_foldopen',
    desc = 'Open folds in {range}. When [!] is added all folds are opened. Useful to see all the text in {range}. Without [!] one level of folds is opened.',
    short_desc = 'open folds',
  },
  {
    command = 'for',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_while',
    desc = 'Repeat the commands between `:for` and `:endfor` for each item in {object}. {object} can be a List, a Blob or a String. Variable {var} is set to the value of each item. When an error is detected for a command inside the loop, execution continues after the `endfor`. Changing {object} inside the loop affects what items are used. Make a copy if this is unwanted: > :for item in copy(mylist)',
    short_desc = 'for loop',
  },
  {
    command = 'function',
    flags = bit.bor(EXTRA, BANG, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_function',
    desc = 'List all functions and their arguments. List function {name}, annotated with line numbers unless "!" is given. {name} may be a Dictionary Funcref entry: > :function dict.init',
    short_desc = 'define a user function',
  },
  {
    command = 'fclose',
    flags = bit.bor(BANG, RANGE, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_fclose',
    desc = [[Close [count] floating windows with the highest zindex values. '!' to close all floating windows.]],
    short_desc = 'close floating window',
  },
  {
    command = 'global',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, EXTRA, DFLALL, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_global',
    desc = 'Execute the Ex command [cmd] (default ":p") on the lines within [range] where {pattern} matches.',
    short_desc = 'execute commands for matching lines',
  },
  {
    command = 'goto',
    flags = bit.bor(RANGE, COUNT, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_goto',
    desc = [[Go to [count] byte in the buffer. exclusive motion. Default [count] is one, start of the file. When giving [range], the last number in it used as the byte count. End-of-line characters are counted depending on the current 'fileformat' setting. Also see the line2byte() function, and the 'o' option in 'statusline'.]],
    short_desc = 'go to byte in the buffer',
  },
  {
    command = 'grep',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE),
    addr_type = 'ADDR_OTHER',
    func = 'ex_make',
    desc = [[Just like ":make", but use 'grepprg' instead of 'makeprg' and 'grepformat' instead of 'errorformat'. When 'grepprg' is "internal" this works like :vimgrep. Note that the pattern needs to be enclosed in separator characters then. If the encoding of the program output differs from the 'encoding' option, you can use the 'makeencoding' option to specify the encoding.]],
    short_desc = [[run 'grepprg' and jump to first match]],
  },
  {
    command = 'grepadd',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE),
    addr_type = 'ADDR_OTHER',
    func = 'ex_make',
    desc = 'Just like ":grep", but instead of making a new list of errors the matches are appended to the current list. Example: > :call setqflist([]) :bufdo grepadd! something %',
    short_desc = 'like :grep, but append to current list',
  },
  {
    command = 'gui',
    flags = bit.bor(BANG, FILES, CMDARG, ARGOPT, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_nogui',
    desc = [[start the GUI]],
    short_desc = 'start the GUI',
  },
  {
    command = 'gvim',
    flags = bit.bor(BANG, FILES, CMDARG, ARGOPT, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_nogui',
    desc = [[start the GUI]],
    short_desc = 'start the GUI',
  },
  {
    command = 'help',
    flags = bit.bor(BANG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_help',
    desc = [[Open a window and display the help file in read-only mode. If there is a help window open already, use that one. Otherwise, if the current window uses the full width of the screen or is at least 80 characters wide, the help window will appear just above the current window. Otherwise the new window is put at the very top. The 'helplang' option is used to select a language, if the main help file is available in several languages. Type gO to see the table of contents.]],
    short_desc = 'open a help window',
  },
  {
    command = 'helpclose',
    flags = TRLBAR,
    addr_type = 'ADDR_NONE',
    func = 'ex_helpclose',
    desc = 'Close one help window, if there is one. Vim will try to restore the window layout (including cursor position) to the same layout it was before opening the help window initially. This might cause triggering several autocommands.',
    short_desc = 'close one help window',
  },
  {
    command = 'helpgrep',
    flags = bit.bor(EXTRA, NOTRLCOM, NEEDARG),
    addr_type = 'ADDR_NONE',
    func = 'ex_helpgrep',
    desc = [[Search all help text files and make a list of lines in which {pattern} matches. Jumps to the first match. The optional [@xx] specifies that only matches in the "xx" language are to be found. You can navigate through the matches with the quickfix commands, e.g., :cnext to jump to the next one. Or use :cwindow to get the list of matches in the quickfix window. {pattern} is used as a Vim regexp pattern. 'ignorecase' is not used, add "\c" to ignore case.]],
    short_desc = 'like ":grep" but searches help files',
  },
  {
    command = 'helptags',
    flags = bit.bor(NEEDARG, FILES, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_helptags',
    desc = [[Generate the help tags file(s) for directory {dir}. When {dir} is ALL then all "doc" directories in 'runtimepath' will be used. All "*.txt" and "*.??x" files in the directory and sub-directories are scanned for a help tag definition in between stars. The "*.??x" files are for translated docs, they generate the "tags-??" file, see help-translated. The generated tags files are sorted. When there are duplicates an error message is given. An existing tags file is silently overwritten.]],
    short_desc = 'generate help tags for a directory',
  },
  {
    command = 'highlight',
    flags = bit.bor(BANG, EXTRA, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_highlight',
    desc = 'language-specific groups, which are prefixed with the language name)',
    short_desc = 'specify highlighting methods',
  },
  {
    command = 'hide',
    flags = bit.bor(BANG, RANGE, COUNT, EXTRA, TRLBAR),
    addr_type = 'ADDR_WINDOWS',
    func = 'ex_hide',
    desc = [[Without {count}: Quit the current window, unless it is the last window on the screen. If {count} is given quit the {count} window. The buffer becomes hidden (unless there is another window editing it or 'bufhidden' is `unload`, `delete` or `wipe`). If the window is the last one in the current tab page the tab page is closed. tab-page The value of 'hidden' is irrelevant for this command. Changes to the buffer are not written and won't get lost, so this is a "safe" command.]],
    short_desc = 'hide current buffer for a command',
  },
  {
    command = 'history',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_history',
    desc = 'Print the history of last entered commands. List the contents of history {name} which can be: c[md] or : command-line history s[earch] or / or ? search string history e[xpr] or = expression register history i[nput] or @ input line history d[ebug] or > debug command history a[ll] all of the above If the numbers {first} and/or {last} are given, the respective range of entries from a history is listed.',
    short_desc = 'print a history list',
  },
  {
    command = 'horizontal',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = 'Execute {cmd} in a horizontal split window. Supports these commands: - `:wincmd =`: equalize windows only horizontally. - :terminal: open a terminal buffer in a split window. - :checkhealth: open a healthcheck buffer in a split window.',
    short_desc = 'following window command work horizontally',
  },
  {
    command = 'insert',
    flags = bit.bor(BANG, RANGE, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_append',
    desc = [[Insert several lines of text above the specified line. If the {range} is missing, the text will be inserted before the current line. Adding [!] toggles 'autoindent' for the time this command is executed.]],
    short_desc = 'insert text',
  },
  {
    command = 'iabbrev',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'same as ":ab", but for Insert mode only.',
    short_desc = 'like ":abbrev" but for Insert mode',
  },
  {
    command = 'iabclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abclear',
    desc = 'Remove all abbreviations for Insert mode.',
    short_desc = 'like ":abclear" but for Insert mode',
  },
  {
    command = 'if',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_if',
    desc = 'Execute the commands until the next matching `:else` or `:endif` if {expr1} evaluates to non-zero. Although the short forms work, it is recommended to always use `:endif` to avoid confusion and to make auto-indenting work properly. From Vim version 4.5 until 5.0, every Ex command in between the `:if` and `:endif` is ignored. These two commands were just to allow for future expansions in a backward compatible way. Nesting was allowed.',
    short_desc = 'execute commands when condition met',
  },
  {
    command = 'ijump',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like "[ CTRL-I" and "] CTRL-I", but search in [range] lines (default: whole file). See :search-args for [/] and [!].',
    short_desc = 'jump to definition of identifier',
  },
  {
    command = 'ilist',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like "[I" and "]I", but search in [range] lines (default: whole file). See :search-args for [/] and [!].',
    short_desc = 'list lines where identifier matches',
  },
  {
    command = 'imap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-i',
    short_desc = 'like ":map" but for Insert mode',
  },
  {
    command = 'imapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-i',
    short_desc = 'like ":mapclear" but for Insert mode',
  },
  {
    command = 'imenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Insert mode',
  },
  {
    command = 'inoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-i',
    short_desc = 'like ":noremap" but for Insert mode',
  },
  {
    command = 'inoreabbrev',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'same as ":ab", but for Insert mode only and no remapping for this {rhs}',
    short_desc = 'like ":noreabbrev" but for Insert mode',
  },
  {
    command = 'inoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Insert mode',
  },
  {
    command = 'intro',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_intro',
    desc = [[print the introductory message]],
    short_desc = 'print the introductory message',
  },
  {
    command = 'iput',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, REGSTR, TRLBAR, ZEROR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_iput',
    desc = 'Like :put, but adjust indent to the current line.',
    short_desc = 'like :put, but adjust the indent to the',
  },
  {
    command = 'isearch',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like "[i" and "]i", but search in [range] lines (default: whole file). See :search-args for [/] and [!].',
    short_desc = 'list one line where identifier matches',
  },
  {
    command = 'isplit',
    flags = bit.bor(BANG, RANGE, DFLALL, WHOLEFOLD, EXTRA),
    addr_type = 'ADDR_LINES',
    func = 'ex_findpat',
    desc = 'Like "CTRL-W i" and "CTRL-W i", but search in [range] lines (default: whole file). See :search-args for [/] and [!].',
    short_desc = 'split window and jump to definition of',
  },
  {
    command = 'iunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-i',
    short_desc = 'like ":unmap" but for Insert mode',
  },
  {
    command = 'iunabbrev',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'Same as ":una", but for insert mode only.',
    short_desc = 'like ":unabbrev" but for Insert mode',
  },
  {
    command = 'iunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Insert mode',
  },
  {
    command = 'join',
    flags = bit.bor(BANG, RANGE, WHOLEFOLD, COUNT, FLAGS, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_join',
    desc = 'Join [range] lines. Same as "J", except with [!] the join does not insert or delete any spaces. If a [range] has equal start and end values, this command does nothing. The default behavior is to join the current line with the line below it. See ex-flags for [flags].',
    short_desc = 'join lines',
  },
  {
    command = 'jumps',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_jumps',
    desc = 'Print the jump list (not a motion command).',
    short_desc = 'print the jump list',
  },
  {
    command = 'k',
    flags = bit.bor(RANGE, WORD1, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_mark',
    desc = 'Same as :mark, but the space before the mark name can be omitted.',
    short_desc = 'set a mark',
  },
  {
    command = 'keepmarks',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Currently only has effect for the filter command :range!: - When the number of lines after filtering is equal to or larger than before, all marks are kept at the same line number. - When the number of lines decreases, the marks in the lines that disappeared are deleted. In any case the marks below the filtered text have their line numbers adjusted, thus stick to the text, as usual. When the 'R' flag is missing from 'cpoptions' this has the same effect as using ":keepmarks".]],
    short_desc = 'following command keeps marks where they are',
  },
  {
    command = 'keepjumps',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Moving around in {command} does not change the '', '. and '^ marks, the jumplist or the changelist. Useful when making a change or inserting text automatically and the user doesn't want to go to this position. E.g., when updating a "Last change" timestamp in the first line: > :let lnum = line(".") :keepjumps normal gg :call SetLastChange() :keepjumps exe "normal " .. lnum .. "G"]],
    short_desc = 'following command keeps jumplist and marks',
  },
  {
    command = 'keeppatterns',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = 'Execute {command}, without adding anything to the search history and, in case of :s or :&, without modifying the last substitute pattern or substitute string.',
    short_desc = 'following command keeps search pattern history',
  },
  {
    command = 'keepalt',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = 'Execute {cmd} while keeping the current alternate file name. Note that commands invoked indirectly (e.g., with a function) may still set the alternate file name.',
    short_desc = 'following command keeps the alternate file',
  },
  {
    command = 'list',
    flags = bit.bor(RANGE, WHOLEFOLD, COUNT, FLAGS, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_print',
    desc = [[Same as :print, but show tabs as ">", trailing spaces as "-", and non-breakable space characters as "+" by default. Further changed by the 'listchars' option. See ex-flags for [flags].]],
    short_desc = 'print lines',
  },
  {
    command = 'lNext',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = 'Same as ":cNext" and ":cprevious", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to previous entry in location list',
  },
  {
    command = 'lNfile',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = 'Same as ":cNfile" and ":cpfile", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to last entry in previous file',
  },
  {
    command = 'last',
    flags = bit.bor(EXTRA, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_last',
    desc = 'Start editing the last file in the argument list. This fails when changes have been made and Vim does not want to abandon the current buffer. Also see ++opt and +cmd.',
    short_desc = 'go to the last file in the argument list',
  },
  {
    command = 'labove',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Same as ":cabove", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to location above current line',
  },
  {
    command = 'language',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_language',
    desc = 'Print the current language (aka locale). With the "messages" argument the language used for messages is printed. Technical: LC_MESSAGES. With the "ctype" argument the language used for character encoding is printed. Technical: LC_CTYPE. With the "time" argument the language used for strftime() is printed. Technical: LC_TIME. With the "collate" argument the language used for collation order is printed. Technical: LC_COLLATE.',
    short_desc = 'set the language (locale)',
  },
  {
    command = 'laddexpr',
    flags = bit.bor(NEEDARG, WORD1, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_cexpr',
    desc = 'Same as ":caddexpr", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'add locations from expr',
  },
  {
    command = 'laddbuffer',
    flags = bit.bor(RANGE, WORD1, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_cbuffer',
    desc = 'Same as ":caddbuffer", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'add locations from buffer',
  },
  {
    command = 'laddfile',
    flags = bit.bor(TRLBAR, FILE1),
    addr_type = 'ADDR_NONE',
    func = 'ex_cfile',
    desc = 'Same as ":caddfile", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'add locations to current location list',
  },
  {
    command = 'lafter',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Same as ":cafter", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to location after current cursor',
  },
  {
    command = 'later',
    flags = bit.bor(TRLBAR, EXTRA, NOSPC, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_later',
    desc = 'Go to newer text state {count} times. Go to newer text state about {N} seconds later. Go to newer text state about {N} minutes later. Go to newer text state about {N} hours later. Go to newer text state about {N} days later. Go to newer text state {N} file writes later. When at the state of the last file write, ":later 1f" will go to the newest text state.',
    short_desc = 'go to newer change, redo',
  },
  {
    command = 'lbuffer',
    flags = bit.bor(BANG, RANGE, WORD1, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_cbuffer',
    desc = 'Same as ":cbuffer", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'parse locations and jump to first location',
  },
  {
    command = 'lbefore',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Same as ":cbefore", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to location before current cursor',
  },
  {
    command = 'lbelow',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cbelow',
    desc = 'Same as ":cbelow", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to location below current line',
  },
  {
    command = 'lbottom',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_cbottom',
    desc = 'Same as ":cbottom", except use the window showing the location list for the current window.',
    short_desc = 'scroll to the bottom of the location window',
  },
  {
    command = 'lcd',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_cd',
    desc = 'Like :cd, but only set the current directory for the current window. The current directory for other windows or tabs is not changed.',
    short_desc = 'change directory locally',
  },
  {
    command = 'lchdir',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_cd',
    desc = 'Same as :lcd.',
    short_desc = 'change directory locally',
  },
  {
    command = 'lclose',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_cclose',
    desc = 'Close the window showing the location list for the current window.',
    short_desc = 'close location window',
  },
  {
    command = 'ldo',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_QUICKFIX_VALID',
    func = 'ex_listdo',
    desc = 'Execute {cmd} in each valid entry in the location list for the current window. It works like doing this: > :lfirst :{cmd} :lnext :{cmd} etc.',
    short_desc = 'execute command in valid location list entries',
  },
  {
    command = 'left',
    flags = bit.bor(TRLBAR, RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_align',
    desc = 'Left-align lines in [range]. Sets the indent in the lines to [indent] (default 0).',
    short_desc = 'left align lines',
  },
  {
    command = 'leftabove',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd}. If it contains a command that splits a window, it will be opened left (vertical split) or above (horizontal split) the current window. Overrules 'splitbelow' and 'splitright'. Doesn't work for :execute and :normal.]],
    short_desc = 'make split window appear left or above',
  },
  {
    command = 'let',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_let',
    desc = [[Set internal variable {var-name} to the result of the expression {expr1}. The variable will get the type from the {expr}. If {var-name} didn't exist yet, it is created.]],
    short_desc = 'assign a value to a variable or option',
  },
  {
    command = 'lexpr',
    flags = bit.bor(NEEDARG, WORD1, NOTRLCOM, BANG),
    addr_type = 'ADDR_NONE',
    func = 'ex_cexpr',
    desc = 'Same as :cexpr, except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'read locations from expr and jump to first',
  },
  {
    command = 'lfile',
    flags = bit.bor(TRLBAR, FILE1, BANG),
    addr_type = 'ADDR_NONE',
    func = 'ex_cfile',
    desc = 'Same as ":cfile", except the location list for the current window is used instead of the quickfix list. You can not use the -q command-line option to set the location list.',
    short_desc = 'read file with locations and jump to first',
  },
  -- Even though 'lfdo' is alphabetically lower than 'lfile', it is after
  -- 'lfile' in this cmd list to support the existing ":lf" abbreviation.
  {
    command = 'lfdo',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_QUICKFIX_VALID',
    func = 'ex_listdo',
    desc = 'Execute {cmd} in each file in the location list for the current window. It works like doing this: > :lfirst :{cmd} :lnfile :{cmd} etc.',
    short_desc = 'execute command in each file in location list',
  },
  {
    command = 'lfirst',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cc',
    desc = 'Same as ":lrewind".',
    short_desc = 'go to the specified location, default first one',
  },
  {
    command = 'lgetfile',
    flags = bit.bor(TRLBAR, FILE1),
    addr_type = 'ADDR_NONE',
    func = 'ex_cfile',
    desc = 'Same as ":cgetfile", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'read file with locations',
  },
  {
    command = 'lgetbuffer',
    flags = bit.bor(RANGE, WORD1, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_cbuffer',
    desc = 'Same as ":cgetbuffer", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'get locations from buffer',
  },
  {
    command = 'lgetexpr',
    flags = bit.bor(NEEDARG, WORD1, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_cexpr',
    desc = 'Same as :cgetexpr, except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'get locations from expr',
  },
  {
    command = 'lgrep',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE),
    addr_type = 'ADDR_OTHER',
    func = 'ex_make',
    desc = 'Same as ":grep", except the location list for the current window is used instead of the quickfix list.',
    short_desc = [[run 'grepprg' and jump to first match]],
  },
  {
    command = 'lgrepadd',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE),
    addr_type = 'ADDR_OTHER',
    func = 'ex_make',
    desc = 'Same as ":grepadd", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'like :grep, but append to current list',
  },
  {
    command = 'lhelpgrep',
    flags = bit.bor(EXTRA, NOTRLCOM, NEEDARG),
    addr_type = 'ADDR_NONE',
    func = 'ex_helpgrep',
    desc = 'Same as ":helpgrep", except the location list is used instead of the quickfix list. If the help window is already opened, then the location list for that window is used. Otherwise, a new help window is opened and the location list for that window is set. The location list for the current window is not changed then.',
    short_desc = 'like ":helpgrep" but uses location list',
  },
  {
    command = 'lhistory',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'qf_history',
    desc = 'Show the list of location lists, otherwise like `:chistory`.',
    short_desc = 'list the location lists',
  },
  {
    command = 'll',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_QUICKFIX',
    func = 'ex_cc',
    desc = 'Same as ":cc", except the location list for the',
    short_desc = 'go to specific location',
  },
  {
    command = 'llast',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cc',
    desc = 'Same as ":clast", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to the specified location, default last one',
  },
  {
    command = 'llist',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'qf_list',
    desc = 'Same as ":clist", except the location list for the current window is used instead of the quickfix list. List all the entries in the location list for the current window.',
    short_desc = 'list all locations',
  },
  {
    command = 'lmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-l',
    short_desc = 'mappings',
  },
  {
    command = 'lmapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-l',
    short_desc = 'like ":mapclear!" but includes Lang-Arg mode',
  },
  {
    command = 'lmake',
    flags = bit.bor(BANG, EXTRA, NOTRLCOM, TRLBAR, XFILE),
    addr_type = 'ADDR_NONE',
    func = 'ex_make',
    desc = 'Same as ":make", except the location list for the current window is used instead of the quickfix list.',
    short_desc = [[execute external command 'makeprg' and parse]],
  },
  {
    command = 'lnoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-l',
    short_desc = 'like ":noremap!" but includes Lang-Arg mode',
  },
  {
    command = 'lnext',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = 'Same as ":cnext", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to next location',
  },
  {
    command = 'lnewer',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'qf_age',
    desc = 'Same as `:cnewer`, except use the location list for the current window instead of the quickfix list.',
    short_desc = 'go to newer location list',
  },
  {
    command = 'lnfile',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = 'Same as ":cnfile", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to first location in next file',
  },
  {
    command = 'loadview',
    flags = bit.bor(FILE1, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_loadview',
    desc = 'Load the view for the current file. When [nr] is omitted, the view stored with ":mkview" is loaded. When [nr] is specified, the view stored with ":mkview [nr]" is loaded.',
    short_desc = 'load view for current window from a file',
  },
  {
    command = 'loadkeymap',
    flags = bit.bor(CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_loadkeymap',
    desc = '" Maintainer: name <email@address> " Last Changed: 2001 Jan 1 let b:keymap_name = "short" loadkeymap a A b B comment',
    short_desc = 'load the following keymaps until EOF',
  },
  {
    command = 'lockmarks',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {command} without adjusting marks. This is useful when changing text in a way that the line count will be the same when the change has completed. WARNING: When the line count does change, marks below the change will keep their line number, thus move to another text line. These items will not be adjusted for deleted/inserted lines: - lower case letter marks 'a - 'z - upper case letter marks 'A - 'Z - numbered marks '0 - '9 - last insert position '^ - last change position '.]],
    short_desc = 'following command keeps marks where they are',
  },
  {
    command = 'lockvar',
    flags = bit.bor(BANG, EXTRA, NEEDARG, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_lockvar',
    desc = [[Lock the internal variable {name}. Locking means that it can no longer be changed (until it is unlocked). A locked variable can be deleted: > :lockvar v :let v = 'asdf' " fails! :unlet v " works]],
    short_desc = 'lock variables',
  },
  {
    command = 'log',
    flags = bit.bor(EXTRA, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_log',
    desc = 'Open a log file that exists in `stdpath("log")`. `[logname]` is the name of the file with the extension ".log" removed. Without a provided `[logname]`, opens the log directory.',
    short_desc = 'view a log file',
  },
  {
    command = 'lolder',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_UNSIGNED',
    func = 'qf_age',
    desc = 'Same as `:colder`, except use the location list for the current window instead of the quickfix list.',
    short_desc = 'go to older location list',
  },
  {
    command = 'lopen',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_copen',
    desc = 'Open a window to show the location list for the current window. Works only when the location list for the current window is present. You can have more than one location window opened at a time. Otherwise, it acts the same as ":copen".',
    short_desc = 'open location window',
  },
  {
    command = 'lprevious',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cnext',
    desc = 'Same as ":cNext" and ":cprevious", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to previous location',
  },
  {
    command = 'lpfile',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_OTHER',
    func = 'ex_cnext',
    desc = 'Same as ":cNfile" and ":cpfile", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to last location in previous file',
  },
  {
    command = 'lrewind',
    flags = bit.bor(RANGE, COUNT, TRLBAR, BANG),
    addr_type = 'ADDR_UNSIGNED',
    func = 'ex_cc',
    desc = 'Same as ":crewind", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'go to the specified location, default first one',
  },
  {
    command = 'ltag',
    flags = bit.bor(TRLBAR, BANG, WORD1),
    addr_type = 'ADDR_NONE',
    func = 'ex_tag',
    desc = [[Jump to tag [name] and add the matching tags to a new location list for the current window. [name] can be a regexp pattern, see tag-regexp. When [name] is not given, the last tag name from the tag stack is used. The search pattern to locate the tag line is prefixed with "\V" to escape all the special characters (very nomagic). The location list showing the matching tags is independent of the tag stack. See tag-! for [!].]],
    short_desc = 'jump to tag and add matching tags to the',
  },
  {
    command = 'lunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-l',
    short_desc = 'like ":unmap!" but includes Lang-Arg mode',
  },
  {
    command = 'lua',
    flags = bit.bor(RANGE, EXTRA, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_lua',
    desc = [[Executes Lua chunk {chunk}. If {chunk} starts with "=" the rest of the chunk is evaluated as an expression and printed. `:lua =expr` and `:=expr` are equivalent to `:lua print(vim.inspect(expr))`. E5107 E5108 Examples: >vim :lua vim.api.nvim_command('echo "Hello, Nvim!"')]],
    short_desc = 'execute Lua command',
  },
  {
    command = 'luado',
    flags = bit.bor(RANGE, DFLALL, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_luado',
    desc = 'Executes Lua chunk "function(line, linenr) {body} end" for each buffer line in [range], where `line` is the current line text (without <EOL>), and `linenr` is the current line number. If the function returns a string that becomes the text of the corresponding buffer line. Default [range] is the whole file: "1,$".',
    short_desc = 'execute Lua command for each line',
  },
  {
    command = 'luafile',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_luafile',
    desc = 'Execute Lua script in {file}. The whole argument is used as the filename (like :edit), spaces do not need to be escaped. Alternatively you can :source Lua files. E5111 E5112 E5113 Examples: >vim :luafile script.lua :luafile %',
    short_desc = 'execute Lua script file',
  },
  {
    command = 'lvimgrep',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_vimgrep',
    desc = 'Same as ":vimgrep", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'search for pattern in files',
  },
  {
    command = 'lvimgrepadd',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_vimgrep',
    desc = 'Same as ":vimgrepadd", except the location list for the current window is used instead of the quickfix list.',
    short_desc = 'like :vimgrep, but append to current list',
  },
  {
    command = 'lwindow',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_cwindow',
    desc = 'Same as ":cwindow", except use the window showing the location list for the current window.',
    short_desc = 'open or close location window',
  },
  {
    command = 'ls',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'buflist_list',
    desc = 'Show all buffers. Example: 1 #h "/test/text" line 1 ~ 2u "asdf" line 0 ~ 3 %a + "version.c" line 1 ~ When the [!] is included the list will show unlisted buffers (the term "unlisted" is a bit confusing then...). Each buffer has a unique number. That number will not change, thus you can always go to a specific buffer with ":buffer N" or "N CTRL-^", where N is the buffer number.',
    short_desc = 'list all buffers',
  },
  {
    command = 'lsp',
    flags = bit.bor(NEEDARG, EXTRA),
    addr_type = 'ADDR_NONE',
    func = 'ex_lsp',
    desc = 'Activates LSP for current and future buffers. See vim.lsp.enable(). Disables LSP (and stops if running) for current and future buffers. See vim.lsp.enable().',
    short_desc = 'language server protocol',
  },
  {
    command = 'move',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_copymove',
    desc = 'Move the lines given by [range] to below the line given by {address}.',
    short_desc = 'move lines',
  },
  {
    command = 'mark',
    flags = bit.bor(RANGE, WORD1, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_mark',
    desc = [[Set mark {a-zA-Z'} at last line number in [range], column 0. Default is cursor line.]],
    short_desc = 'set a mark',
  },
  {
    command = 'make',
    flags = bit.bor(BANG, EXTRA, NOTRLCOM, TRLBAR, XFILE),
    addr_type = 'ADDR_NONE',
    func = 'ex_make',
    desc = [[1. All relevant QuickFixCmdPre autocommands are executed. 2. If the 'autowrite' option is on, write any changed buffers 3. An errorfile name is made from 'makeef'. If 'makeef' doesn't contain "##", and a file with this name already exists, it is deleted. 4. The program given with the 'makeprg' option is started (default "make") with the optional [arguments] and the output is saved in the errorfile (for Unix it is also echoed on the screen). 5. The errorfile is read using 'errorformat'. 6.]],
    short_desc = [[execute external command 'makeprg' and parse]],
  },
  {
    command = 'map',
    flags = bit.bor(BANG, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-nvo',
    short_desc = 'show or enter a mapping',
  },
  {
    command = 'mapclear',
    flags = bit.bor(EXTRA, BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-nvo',
    short_desc = 'clear all mappings for Normal and Visual mode',
  },
  {
    command = 'marks',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_marks',
    desc = [[List all the current marks (not a motion command). The '(, '), '{ and '} marks are not listed. The first column has number zero. List the marks that are mentioned in {arg} (not a motion command). For example: > :marks aB]],
    short_desc = 'list all marks',
  },
  {
    command = 'match',
    flags = bit.bor(RANGE, EXTRA, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_match',
    desc = 'Define a pattern to highlight in the current window. It will be highlighted with {group}. Example: > :highlight MyGroup ctermbg=green guibg=green :match MyGroup /TODO/',
    short_desc = 'define a match to highlight',
  },
  {
    command = 'menu',
    flags = bit.bor(RANGE, ZEROR, BANG, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = 'They are mostly like',
    short_desc = 'enter a new menu item',
  },
  {
    command = 'menutranslate',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menutranslate',
    desc = 'Clear all menu translations. Translate menu name {english} to {mylang}. All special characters like "&" and "<Tab>" need to be included. Spaces and dots need to be escaped with a backslash, just like in other :menu commands. Case in {english} is ignored.',
    short_desc = ':menut[ranslate] add a menu translation item',
  },
  {
    command = 'messages',
    flags = bit.bor(EXTRA, TRLBAR, RANGE, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_messages',
    desc = [[view previously displayed messages]],
    short_desc = 'view previously displayed messages',
  },
  {
    command = 'mkexrc',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mkrc',
    desc = 'Write current key mappings and changed options to [file] (default ".exrc" in the current directory), unless it already exists. Always write current key mappings and changed options to [file] (default ".exrc" in the current directory).',
    short_desc = 'write current mappings and settings to a file',
  },
  {
    command = 'mksession',
    flags = bit.bor(BANG, FILE1, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_mkrc',
    desc = 'Write a Vim script that restores the current editing session. When [!] is included, an existing file is overwritten. When [file] is omitted, "Session.vim" is used.',
    short_desc = 'write session info to a file',
  },
  {
    command = 'mkspell',
    flags = bit.bor(BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE),
    addr_type = 'ADDR_NONE',
    func = 'ex_mkspell',
    desc = 'Generate a Vim spell file from word lists. Example: > :mkspell /tmp/nl nl_NL.words',
    short_desc = 'produce .spl spell file',
  },
  {
    command = 'mkvimrc',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mkrc',
    desc = 'Like ":mkexrc", but the default is ".nvimrc" in the current directory. The ":version" command is also written to the file.',
    short_desc = 'write current mappings and settings to a file',
  },
  {
    command = 'mkview',
    flags = bit.bor(BANG, FILE1, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_mkrc',
    desc = [[Write a Vim script that restores the contents of the current window. When [!] is included, an existing file is overwritten. When [file] is omitted or is a number from 1 to 9, a name is generated and 'viewdir' prepended. When the last path part of 'viewdir' does not exist, this directory is created. E.g., when 'viewdir' is "$VIM/vimfiles/view" then "view" is created in "$VIM/vimfiles". An existing file is always overwritten then. Use :loadview to load this view again.]],
    short_desc = 'write view of current window to a file',
  },
  {
    command = 'mode',
    flags = bit.bor(WORD1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mode',
    desc = 'Clears and redraws the screen. See also nvim__redraw().',
    short_desc = 'show or change the screen mode',
  },
  {
    command = 'mzscheme',
    flags = bit.bor(RANGE, EXTRA, DFLALL, NEEDARG, CMDWIN, LOCK_OK, SBOXOK),
    addr_type = 'ADDR_LINES',
    func = 'ex_script_ni',
  },
  {
    command = 'mzfile',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_ni',
  },
  {
    command = 'next',
    flags = bit.bor(RANGE, BANG, FILES, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_next',
    desc = 'Edit [count] next file. This fails when changes have been made and Vim does not want to abandon the current buffer. Also see ++opt and +cmd.',
    short_desc = 'go to next file in the argument list',
  },
  {
    command = 'new',
    flags = bit.bor(BANG, FILE1, RANGE, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_splitview',
    desc = [[Create a new window and start editing an empty file in it. Make new window N high (default is to use half the existing height). Reduces the current window height to create room (and others, if the 'equalalways' option is set and 'eadirection' isn't "hor"). Also see ++opt and +cmd. If 'fileformats' is not empty, the first format given will be used for the new buffer. If 'fileformats' is empty, the 'fileformat' of the current buffer is used. This can be overridden with the ++opt argument.]],
    short_desc = 'create a new empty window',
  },
  {
    command = 'nmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-n',
    short_desc = 'like ":map" but for Normal mode',
  },
  {
    command = 'nmapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-n',
    short_desc = 'clear all mappings for Normal mode',
  },
  {
    command = 'nmenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Normal mode',
  },
  {
    command = 'nnoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-n',
    short_desc = 'like ":noremap" but for Normal mode',
  },
  {
    command = 'nnoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Normal mode',
  },
  {
    command = 'noremap',
    flags = bit.bor(BANG, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-nvo',
    short_desc = 'enter a mapping that will not be remapped',
  },
  {
    command = 'noautocmd',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[This will set 'eventignore' to "all" for the duration of the]],
    short_desc = [[following commands don't trigger autocommands]],
  },
  {
    command = 'nohlsearch',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_nohlsearch',
    desc = [[Stop the highlighting for the 'hlsearch' option. It is automatically turned back on when using a search command, or setting the 'hlsearch' option. This command doesn't work in an autocommand, because the highlighting state is saved and restored when executing autocommands autocmd-searchpat. Same thing for when invoking a user function.]],
    short_desc = [[suspend 'hlsearch' highlighting]],
  },
  {
    command = 'noreabbrev',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'same as ":ab", but no remapping for this {rhs}',
    short_desc = 'enter an abbreviation that will not be',
  },
  {
    command = 'noremenu',
    flags = bit.bor(RANGE, ZEROR, BANG, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = 'They are mostly like',
    short_desc = 'enter a menu that will not be remapped',
  },
  {
    command = 'noswapfile',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[{command} Execute {command}. If it contains a command that loads a new buffer, it will be loaded without creating a swapfile and the 'swapfile' option will be reset. If a buffer already had a swapfile it is not removed and 'swapfile' is not reset.]],
    short_desc = [[following commands don't create a swap file]],
  },
  {
    command = 'normal',
    flags = bit.bor(RANGE, BANG, EXTRA, NEEDARG, NOTRLCOM, CTRLV, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_normal',
    desc = 'Execute Normal mode commands {commands}. This makes it possible to execute Normal mode commands typed on the command-line. {commands} are executed like they are typed. For undo all commands are undone together. Execution stops when an error is encountered. If the [!] is given, mappings will not be used. Without it, when this command is called from a non-remappable mapping (:noremap), the argument can be mapped anyway. {commands} should be a complete command.',
    short_desc = 'execute Normal mode commands',
  },
  {
    command = 'number',
    flags = bit.bor(RANGE, WHOLEFOLD, COUNT, FLAGS, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_print',
    desc = [[Same as :print, but precede each line with its line number. (See also hl-LineNr and 'numberwidth'). See ex-flags for [flags].]],
    short_desc = 'print lines with line number',
  },
  {
    command = 'nunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-n',
    short_desc = 'like ":unmap" but for Normal mode',
  },
  {
    command = 'nunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Normal mode',
  },
  {
    command = 'oldfiles',
    flags = bit.bor(BANG, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_oldfiles',
    desc = [[List the files that have marks stored in the ShaDa file. This list is read on startup and only changes afterwards with `:rshada!`. Also see v:oldfiles. The number can be used with c_#<. The output can be filtered with :filter, e.g.: > filter /\.vim/ oldfiles]],
    short_desc = 'list files that have marks in the shada file',
  },
  {
    command = 'omap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-o',
    short_desc = 'like ":map" but for Operator-pending mode',
  },
  {
    command = 'omapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-o',
    short_desc = 'remove all mappings for Operator-pending mode',
  },
  {
    command = 'omenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Operator-pending mode',
  },
  {
    command = 'only',
    flags = bit.bor(BANG, RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_WINDOWS',
    func = 'ex_only',
    desc = [[Make the current window the only one on the screen. All other windows are closed. For {count} see the `:quit` command above :count_quit. When the 'hidden' option is set, all buffers in closed windows become hidden. When 'hidden' is not set, and the 'autowrite' option is set, modified buffers are written. Otherwise, windows that have buffers that are modified are not removed, unless the [!] is given, then they become hidden. But modified buffers are never abandoned, so changes cannot get lost.]],
    short_desc = 'close all but current window (like )',
  },
  {
    command = 'onoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-o',
    short_desc = 'like ":noremap" but for Operator-pending mode',
  },
  {
    command = 'onoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Operator-pending mode',
  },
  {
    command = 'options',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_options',
    desc = 'Open a window for viewing and setting all options. Options are grouped by function. Offers short help for each option. Hit <CR> on the short help to open a help window with more help for the option. Modify the value of the option and hit <CR> on the "set" line to set the new value. For window and buffer specific options, the last accessed window is used to set the option value in, unless this is a help window, in which case the window below help window is used (skipping the option-window).',
    short_desc = 'open the options-window',
  },
  {
    command = 'ounmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-o',
    short_desc = 'like ":unmap" but for Operator-pending mode',
  },
  {
    command = 'ounmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Operator-pending mode',
  },
  {
    command = 'ownsyntax',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_ownsyntax',
    desc = [[Use 'winhighlight' instead.]],
  },
  {
    command = 'print',
    flags = bit.bor(RANGE, WHOLEFOLD, COUNT, FLAGS, TRLBAR, CMDWIN, LOCK_OK, SBOXOK),
    addr_type = 'ADDR_LINES',
    func = 'ex_print',
    desc = 'Print [range] lines (default current line). Can also be spelled `:[range]|` due to Vi compatibility (see :bar). Gives an error in an empty buffer. In the GUI you can use the File.Print menu entry. See ex-flags for [flags]. The :filter command can be used to only show lines matching a pattern.',
    short_desc = 'print lines',
  },
  {
    command = 'packadd',
    flags = bit.bor(BANG, FILE1, NEEDARG, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_packadd',
    desc = [[pack-add Search for an optional plugin directory in 'packpath', source any plugin files found, and add it to 'runtimepath'. The directory must match: > pack/*/opt/{name}]],
    short_desc = [[add a plugin from 'packpath']],
  },
  {
    command = 'packloadall',
    flags = bit.bor(BANG, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_packloadall',
    desc = [[Load all packages in the "start" directory under each entry in 'packpath'. First all the directories found are added to 'runtimepath', then the plugins found in the directories are sourced. This allows for a plugin to depend on something of another plugin, e.g. an "autoload" directory. See packload-two-steps for how this can be useful. This is normally done automatically during startup, after loading your vimrc file. With this command it can be done earlier. Packages will be loaded only once.]],
    short_desc = [[load all packages under 'packpath']],
  },
  {
    command = 'pbuffer',
    flags = bit.bor(BANG, RANGE, BUFNAME, BUFUNL, COUNT, EXTRA, CMDARG, TRLBAR),
    addr_type = 'ADDR_BUFFERS',
    func = 'ex_pbuffer',
    desc = [[Edit buffer [N] from the buffer list in the preview window. If [N] is not given, the current buffer remains being edited. See :buffer-! for [!]. This will also edit a buffer that is not in the buffer list, without setting the 'buflisted' flag. Also see +cmd.]],
    short_desc = 'edit buffer in the preview window',
  },
  {
    command = 'pclose',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_pclose',
    desc = [[Close any "Preview" window currently open. When the 'hidden' option is set, or when the buffer was changed and the [!] is used, the buffer becomes hidden (unless there is another window editing it). The command fails if any "Preview" buffer cannot be closed. See also :close.]],
    short_desc = 'close preview window',
  },
  {
    command = 'perl',
    flags = bit.bor(RANGE, EXTRA, DFLALL, NEEDARG, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_perl',
    desc = 'Execute perl statement {stmt}. The current package is "main". A simple check if the `:perl` command is working: > :perl print "Hello"',
    short_desc = 'execute perl command',
  },
  {
    command = 'perldo',
    flags = bit.bor(RANGE, EXTRA, DFLALL, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_perldo',
    desc = 'Execute perl command {cmd} for each line in the[range], with $_ being set to the test of each line in turn, without a trailing <EOL>. In addition to $_, $line and $linenr is also set to the line content and line number respectively. Setting $_ will change the text, but note that it is not possible to add or delete lines using this command. The default for [range] is the whole file: "1,$".',
    short_desc = 'execute perl command for each line',
  },
  {
    command = 'perlfile',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_perlfile',
    desc = 'Execute the perl script in {file}. The whole argument is used as a single file name.',
    short_desc = 'execute perl script file',
  },
  {
    command = 'pedit',
    flags = bit.bor(BANG, FILE1, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_pedit',
    desc = [[Edit {file} in the preview window. The preview window is opened like with :ptag. The current window and cursor position isn't changed. Useful example: > :pedit +/fputc /usr/include/stdio.h]],
    short_desc = 'edit file in the preview window',
  },
  {
    command = 'pop',
    flags = bit.bor(RANGE, BANG, COUNT, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_tag',
    desc = 'Jump to [count] older entry in tag stack (default 1). See tag-! for [!].',
    short_desc = 'jump to older entry in tag stack',
  },
  {
    command = 'popup',
    flags = bit.bor(NEEDARG, EXTRA, BANG, TRLBAR, NOTRLCOM, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_popup',
    desc = 'Popup the menu {name}. The menu named must have at least one subentry, but need not appear on the menu-bar (see hidden-menus). Like above, but use the position of the mouse pointer instead of the cursor.',
    short_desc = 'popup a menu by name',
  },
  {
    command = 'ppop',
    flags = bit.bor(RANGE, BANG, COUNT, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_ptag',
    desc = 'Does ":[count]pop[!]" in the preview window. See :pop and :ptag.',
    short_desc = '":pop" in preview window',
  },
  {
    command = 'preserve',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_preserve',
    desc = 'Write all text for the current buffer into its swap file. The original file is no longer needed for recovery.',
    short_desc = 'write all text to swap file',
  },
  {
    command = 'previous',
    flags = bit.bor(EXTRA, RANGE, COUNT, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_previous',
    desc = 'Same as :Next. Also see ++opt and +cmd.',
    short_desc = 'go to previous file in argument list',
  },
  {
    command = 'profile',
    flags = bit.bor(BANG, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_profile',
    desc = 'Start profiling, write the output in {fname} upon exit or when a `:profile stop` or `:profile dump` command is invoked. "~/" and environment variables in {fname} will be expanded. If {fname} already exists it will be silently overwritten. The variable v:profiling is set to one. Write the collected profiling information to the logfile and stop profiling. You can use the `:profile start` command to clear the profiling statistics and start profiling again.',
    short_desc = 'profiling functions and scripts',
  },
  {
    command = 'profdel',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_breakdel',
    desc = 'Stop profiling for the arguments specified. See :breakdel for the arguments. Examples: > profdel func MyFunc profdel file MyScript.vim profdel here',
    short_desc = 'stop profiling a function or script',
  },
  {
    command = 'psearch',
    flags = bit.bor(BANG, RANGE, WHOLEFOLD, DFLALL, EXTRA),
    addr_type = 'ADDR_LINES',
    func = 'ex_psearch',
    desc = [[Works like :ijump but shows the found match in the preview window. The preview window is opened like with :ptag. The current window and cursor position isn't changed. Useful example: > :psearch popen]],
    short_desc = 'like ":ijump" but shows match in preview window',
  },
  {
    command = 'ptag',
    flags = bit.bor(RANGE, BANG, WORD1, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_ptag',
    desc = [[Does ":tag[!] [tagname]" and shows the found tag in a "Preview" window without changing the current buffer or cursor position. If a "Preview" window already exists, it is re-used (like a help window is). If a new one is opened, 'previewheight' is used for the height of the window. See also :tag. See below for an example. CursorHold-example Small difference from :tag: When [tagname] is equal to the already displayed tag, the position in the matching tag list is not reset.]],
    short_desc = 'show tag in preview window',
  },
  {
    command = 'ptNext',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_ptag',
    desc = 'Same as ":ptprevious".',
    short_desc = ':tNext in preview window',
  },
  {
    command = 'ptfirst',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_ptag',
    desc = 'Same as ":ptrewind".',
    short_desc = ':trewind in preview window',
  },
  {
    command = 'ptjump',
    flags = bit.bor(BANG, TRLBAR, WORD1),
    addr_type = 'ADDR_NONE',
    func = 'ex_ptag',
    desc = 'Does ":tjump[!] [name]" and shows the new tag in a "Preview" window. See :ptag for more info.',
    short_desc = 'to the tag under the cursor',
  },
  {
    command = 'ptlast',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_ptag',
    desc = '":tlast" in the preview window. See :ptag.',
    short_desc = ':tlast in preview window',
  },
  {
    command = 'ptnext',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_ptag',
    desc = '":tnext" in the preview window. See :ptag.',
    short_desc = ':tnext in preview window',
  },
  {
    command = 'ptprevious',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_ptag',
    desc = '":tprevious" in the preview window. See :ptag.',
    short_desc = ':tprevious in preview window',
  },
  {
    command = 'ptrewind',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_ptag',
    desc = '":trewind" in the preview window. See :ptag.',
    short_desc = ':trewind in preview window',
  },
  {
    command = 'ptselect',
    flags = bit.bor(BANG, TRLBAR, WORD1),
    addr_type = 'ADDR_NONE',
    func = 'ex_ptag',
    desc = 'Does ":tselect[!] [name]" and shows the new tag in a "Preview" window. See :ptag for more info.',
    short_desc = ':tselect and show tag in preview window',
  },
  {
    command = 'put',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, REGSTR, TRLBAR, ZEROR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_put',
    desc = [[Put the text [from register x] after [line] (default current line). This always works linewise, thus this command can be used to put a yanked block as new lines. If no register is specified, it depends on the 'clipboard' option: If 'clipboard' contains "unnamedplus", paste from the + register quoteplus. Otherwise, if 'clipboard' contains "unnamed", paste from the * register quotestar. Otherwise, paste from the unnamed register quote_quote.]],
    short_desc = 'like , but adjust the indent to the',
  },
  {
    command = 'pwd',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_pwd',
    desc = [[Print the current directory name. Also see getcwd(). When 'verbose' is non-zero, :pwd will also display what scope the current directory was set. Example: > " Set by :cd :verbose pwd [global] /path/to/current " Set by :lcd :verbose pwd [window] /path/to/current " Set by :tcd :verbose pwd [tabpage] /path/to/current]],
    short_desc = 'print current directory',
  },
  {
    command = 'python',
    flags = bit.bor(RANGE, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_python3',
    desc = 'Execute Python statement {stmt}. A simple check if the `:python` command is working: >vim :python print("Hello")',
    short_desc = 'execute Python command',
  },
  {
    command = 'pydo',
    flags = bit.bor(RANGE, DFLALL, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_pydo3',
    desc = 'Execute Python function "def _vim_pydo(line, linenr): {body}" for each line in the [range], with the function arguments being set to the text of each line in turn, without a trailing <EOL>, and the current line number. The function should return a string or None. If a string is returned, it becomes the text of the line in the current turn. The default for [range] is the whole file: "1,$".',
    short_desc = 'execute Python command for each line',
  },
  {
    command = 'pyfile',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_py3file',
    desc = 'Execute the Python script in {file}. The whole argument is used as a single file name.',
    short_desc = 'execute Python script file',
  },
  {
    command = 'py3',
    flags = bit.bor(RANGE, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_python3',
    desc = 'The `:py3` and `:python3` commands work similar to `:python`. A simple check if the `:py3` command is working: >vim :py3 print("Hello")',
    short_desc = 'execute Python 3 command',
  },
  {
    command = 'py3do',
    flags = bit.bor(RANGE, DFLALL, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_pydo3',
    desc = 'The `:py3do` command works similar to `:pydo`.',
    short_desc = 'execute Python 3 command for each line',
  },
  {
    command = 'python3',
    flags = bit.bor(RANGE, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_python3',
    desc = 'The `:py3` and `:python3` commands work similar to `:python`. A simple check if the `:py3` command is working: >vim :py3 print("Hello")',
    short_desc = 'same as :py3',
  },
  {
    command = 'py3file',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_py3file',
    desc = 'The `:py3file` command works similar to `:pyfile`.',
    short_desc = 'execute Python 3 script file',
  },
  {
    command = 'pyx',
    flags = bit.bor(RANGE, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_python3',
    desc = 'To check if `:pyx` works: >vim :pyx print("Hello")',
    short_desc = 'execute python_x command',
  },
  {
    command = 'pyxdo',
    flags = bit.bor(RANGE, DFLALL, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_pydo3',
    desc = [[if has('pythonx') echo 'pyx* commands are available. (Python ' .. &pyx .. ')' endif]],
    short_desc = 'execute python_x command for each line',
  },
  {
    command = 'pythonx',
    flags = bit.bor(RANGE, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_python3',
    desc = 'To check if `:pyx` works: >vim :pyx print("Hello")',
    short_desc = 'same as :pyx',
  },
  {
    command = 'pyxfile',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_py3file',
    desc = [[if has('pythonx') echo 'pyx* commands are available. (Python ' .. &pyx .. ')' endif]],
    short_desc = 'execute python_x script file',
  },
  {
    command = 'quit',
    flags = bit.bor(BANG, RANGE, COUNT, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_WINDOWS',
    func = 'ex_quit',
    desc = 'Quit the current window. Quit Vim if this is the last edit-window. This fails when changes have been made and Vim refuses to abandon the current buffer, and when the last file in the argument list has not been edited. If there are other tab pages and quitting the last window in the current tab page the current tab page is closed tab-page. Triggers the QuitPre autocommand event. See CTRL-W_q for quitting another window.',
    short_desc = 'quit current window (like )',
  },
  {
    command = 'quitall',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_quitall',
    desc = 'Same as ":qall".',
    short_desc = 'quit Vim',
  },
  {
    command = 'qall',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_quitall',
    desc = [[Exit Vim, unless there are some buffers which have been changed. (Use ":bmod" to go to the next modified buffer). When 'autowriteall' is set all changed buffers will be written, like :wqall.]],
    short_desc = 'quit Vim',
  },
  {
    command = 'read',
    flags = bit.bor(BANG, RANGE, WHOLEFOLD, FILE1, ARGOPT, TRLBAR, ZEROR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_read',
    desc = 'Insert the file [name] (default: current file) below the cursor. See ++opt for the possible values of [++opt].',
    short_desc = 'read file into the text',
  },
  {
    command = 'recover',
    flags = bit.bor(BANG, FILE1, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_recover',
    desc = 'Try to recover [file] from the swap file. If [file] is not given use the file name for the current buffer. The current contents of the buffer are lost. This command fails if the buffer was modified. Like ":recover", but any changes in the current buffer are lost.',
    short_desc = 'recover a file from a swap file',
  },
  {
    command = 'redo',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_redo',
    desc = 'Redo one change which was undone.',
    short_desc = 'redo one undone change',
  },
  {
    command = 'redir',
    flags = bit.bor(BANG, FILES, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_redir',
    desc = 'Redirect messages to file {file}. The messages which are the output of commands are written to that file, until redirection ends. The messages are also still shown on the screen. When [!] is included, an existing file is overwritten. When [!] is omitted, and {file} exists, this command fails. Only one ":redir" can be active at a time. Calls to ":redir" will close any active redirection before starting redirection to the new target. For recursive use check out execute().',
    short_desc = 'redirect messages to a file or register',
  },
  {
    command = 'redraw',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_redraw',
    desc = [[Redraws pending screen updates now, or the entire screen if "!" is included. To CLEAR the screen use :mode or CTRL-L. It can be used to redraw the screen in a script or function (or a mapping if 'lazyredraw' is set). See also nvim__redraw().]],
    short_desc = 'force a redraw of the display',
  },
  {
    command = 'redrawstatus',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_redrawstatus',
    desc = [[Redraws the status line and window bar of the current window, or all status lines and window bars if "!" is included. Redraws the commandline instead if it contains the 'ruler'. Useful if 'statusline' or 'winbar' includes an item that doesn't cause automatic updating. See also nvim__redraw().]],
    short_desc = 'force a redraw of the status line(s) and',
  },
  {
    command = 'redrawtabline',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_redrawtabline',
    desc = [[Redraw the tabline. Useful to update the tabline when 'tabline' includes an item that doesn't trigger automatic updating. See also nvim__redraw().]],
    short_desc = 'force a redraw of the tabline',
  },
  {
    command = 'registers',
    flags = bit.bor(EXTRA, NOTRLCOM, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_display',
    desc = 'Display the type and contents of all numbered and named registers. If a register is written to for :redir it will not be listed. Type can be one of: "c" for characterwise text "l" for linewise text "b" for blockwise-visual text',
    short_desc = 'display the contents of registers',
  },
  {
    command = 'resize',
    flags = bit.bor(RANGE, TRLBAR, WORD1, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_resize',
    desc = 'Decrease current window height by N (default 1). If used after :vertical: decrease width by N.',
    short_desc = 'change current window height',
  },
  {
    command = 'restart',
    flags = bit.bor(CMDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_restart',
    desc = 'Restarts Nvim. Sets v:exitreason. See also ZR. 1. Stops Nvim using `:qall` (or +cmd, if given). 2. Starts a new Nvim server using the same v:argv (except `-- [file…]` files). 3. Attaches all UIs to the new Nvim server and runs `[command]` on it. Example: discard changes and stop with `:qall!`, then restart: > :restart +qall!',
    short_desc = 'restart the Nvim server',
  },
  {
    command = 'retab',
    flags = bit.bor(TRLBAR, RANGE, WHOLEFOLD, DFLALL, BANG, WORD1, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_retab',
    desc = [[Replace all sequences of white-space containing a <Tab> with new strings of white-space using {new-tabstop}. If you do not specify {new-tabstop} or it is zero, Vim uses the current value of 'tabstop'. The current value of 'tabstop' is always used to compute the width of existing tabs. With !, Vim also replaces strings of only normal spaces with tabs where appropriate. With 'expandtab' on, Vim replaces all tabs with the appropriate number of spaces.]],
    short_desc = 'change tab size',
  },
  {
    command = 'return',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_return',
    desc = [[Return from a function. When [expr] is given, it is evaluated and returned as the result of the function. If [expr] is not given, the number 0 is returned. When a function ends without an explicit ":return", the number 0 is returned. Note that there is no check for unreachable lines, thus there is no warning if commands follow ":return". Also, there is no check if the following line contains a valid command. Forgetting the line continuation backslash may go unnoticed: > return 'some text' ..]],
    short_desc = 'return from a user function',
  },
  {
    command = 'rewind',
    flags = bit.bor(EXTRA, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_rewind',
    desc = 'Start editing the first file in the argument list. This fails when changes have been made and Vim does not want to abandon the current buffer. Also see ++opt and +cmd.',
    short_desc = 'go to the first file in the argument list',
  },
  {
    command = 'right',
    flags = bit.bor(TRLBAR, RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_align',
    desc = [[Right-align lines in [range] at [width] columns (default 'textwidth' or 80 when 'textwidth' is 0).]],
    short_desc = 'right align text',
  },
  {
    command = 'rightbelow',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd}. If it contains a command that splits a window, it will be opened right (vertical split) or below (horizontal split) the current window. Overrules 'splitbelow' and 'splitright'. Doesn't work for :execute and :normal.]],
    short_desc = 'make split window appear right or below',
  },
  {
    command = 'rshada',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_shada',
    desc = 'Read from ShaDa file [file] (default: see above). If [!] is given, then any information that is already set (registers, marks, v:oldfiles, etc.) will be overwritten.',
    short_desc = 'read from shada file',
  },
  {
    command = 'runtime',
    flags = bit.bor(BANG, NEEDARG, FILES, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_runtime',
    desc = [[Sources Ex commands or Lua code (".lua" files) read from {file} (a relative path) in each directory given by 'runtimepath' and/or 'packpath'. Ignores non-existing files. Example: > :runtime syntax/c.vim :runtime syntax/c.lua]],
    short_desc = [[source vim scripts in 'runtimepath']],
  },
  {
    command = 'rundo',
    flags = bit.bor(NEEDARG, FILE1),
    addr_type = 'ADDR_NONE',
    func = 'ex_rundo',
    desc = 'Read undo history from {file}.',
    short_desc = 'read undo information from a file',
  },
  {
    command = 'ruby',
    flags = bit.bor(RANGE, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_ruby',
    desc = 'Execute Ruby command {cmd}. A command to try it out: > :ruby print "Hello"',
    short_desc = 'execute Ruby command',
  },
  {
    command = 'rubydo',
    flags = bit.bor(RANGE, DFLALL, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_rubydo',
    desc = 'Evaluate Ruby command {cmd} for each line in the [range], with $_ being set to the text of each line in turn, without a trailing <EOL>. Setting $_ will change the text, but note that it is not possible to add or delete lines using this command. The default for [range] is the whole file: "1,$".',
    short_desc = 'execute Ruby command for each line',
  },
  {
    command = 'rubyfile',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_rubyfile',
    desc = [[Execute the Ruby script in {file}. This is the same as `:ruby load 'file'`, but allows file name completion.]],
    short_desc = 'execute Ruby script file',
  },
  {
    command = 'rviminfo',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_shada',
    desc = 'Deprecated alias to :rshada command.',
  },
  {
    command = 'substitute',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, PREVIEW),
    addr_type = 'ADDR_LINES',
    func = 'ex_substitute',
    preview_func = 'ex_substitute_preview',
    desc = 'For each line in [range] replace a match of {pattern} with {string}. For the {pattern} see pattern. {string} can be a literal string, or something special; see sub-replace-special. When [range] and [count] are omitted, replace in the current line only. When [count] is given, replace in [count] lines, starting with the last line in [range]. When [range] is omitted start in the current line. [count] must be a positive number (max 2147483647) Also see cmdline-ranges. See :s_flags for [flags].',
    short_desc = 'find and replace text',
  },
  {
    command = 'sNext',
    flags = bit.bor(EXTRA, RANGE, COUNT, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_previous',
    desc = 'Short for ":split | [N]Next": split window and go to Nth previous argument. But when there is no previous file, the window is not split. Also see ++opt and +cmd.',
    short_desc = 'split window and go to previous file in',
  },
  {
    command = 'sargument',
    flags = bit.bor(BANG, RANGE, COUNT, EXTRA, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_ARGUMENTS',
    func = 'ex_argument',
    desc = 'Short for ":split | argument [N]": split window and go to Nth argument. But when there is no such argument, the window is not split. Also see ++opt and +cmd.',
    short_desc = 'split window and go to specific file in',
  },
  {
    command = 'sall',
    flags = bit.bor(BANG, RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_all',
    desc = [[Rearrange the screen to open one window for each argument. All other windows are closed. When a count is given, this is the maximum number of windows to open. With the :tab modifier open a tab page for each argument. When there are more arguments than 'tabpagemax' further ones become split windows in the last tab page. When the 'hidden' option is set, all buffers in closed windows become hidden. When 'hidden' is not set, and the 'autowrite' option is set, modified buffers are written.]],
    short_desc = 'open a window for each file in argument list',
  },
  {
    command = 'sandbox',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd} in the sandbox. Useful to evaluate an option that may have been set from a modeline, e.g. 'foldexpr'.]],
    short_desc = 'execute a command in the sandbox',
  },
  {
    command = 'saveas',
    flags = bit.bor(BANG, FILE1, ARGOPT, CMDWIN, LOCK_OK, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_write',
    desc = [[Save the current buffer under the name {file} and set the filename of the current buffer to {file}. The previous name is used for the alternate file name. The [!] is needed to overwrite an existing file. When 'filetype' is empty filetype detection is done with the new name, before the file is written. When the write was successful 'readonly' is reset.]],
    short_desc = 'save file under another name.',
  },
  {
    command = 'sbuffer',
    flags = bit.bor(BANG, RANGE, BUFNAME, BUFUNL, COUNT, EXTRA, CMDARG, TRLBAR),
    addr_type = 'ADDR_BUFFERS',
    func = 'ex_buffer',
    desc = [[Split window and edit buffer [N] from the buffer list. If [N] is not given, the current buffer is edited. Respects the "useopen" setting of 'switchbuf' when splitting. This will also edit a buffer that is not in the buffer list, without setting the 'buflisted' flag. Also see +cmd.]],
    short_desc = 'split window and go to specific file in the',
  },
  {
    command = 'sbNext',
    flags = bit.bor(RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bprevious',
    desc = [[Split window and go to [N]th previous buffer in buffer list. Wraps around the start of the buffer list. Uses 'switchbuf'. Also see +cmd.]],
    short_desc = 'split window and go to previous file in the',
  },
  {
    command = 'sball',
    flags = bit.bor(RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_buffer_all',
    desc = [[Rearrange the screen to open one window for each buffer in the buffer list. When a count is given, this is the maximum number of windows to open. 'winheight' also limits the number of windows opened ('winwidth' if :vertical was prepended). Buf/Win Enter/Leave autocommands are not executed for the new windows here, that's only done when they are really entered. When the :tab modifier is used new windows are opened in a new tab, up to 'tabpagemax'.]],
    short_desc = 'open a window for each file in the buffer list',
  },
  {
    command = 'sbfirst',
    flags = bit.bor(CMDARG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_brewind',
    desc = 'Same as ":sbrewind".',
    short_desc = 'split window and go to first file in the',
  },
  {
    command = 'sblast',
    flags = bit.bor(CMDARG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_blast',
    desc = [[Split window and go to last buffer in buffer list. If the buffer list is empty, go to the last unlisted buffer. Respects 'switchbuf' option.]],
    short_desc = 'split window and go to last file in buffer',
  },
  {
    command = 'sbmodified',
    flags = bit.bor(RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bmodified',
    desc = [[Split window and go to [N]th next modified buffer. Respects 'switchbuf' option. Note: this command also finds buffers not in the buffer list.]],
    short_desc = 'split window and go to modified file in the',
  },
  {
    command = 'sbnext',
    flags = bit.bor(RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bnext',
    desc = [[Split window and go to [N]th next buffer in buffer list. Wraps around the end of the buffer list. Uses 'switchbuf' Also see +cmd.]],
    short_desc = 'split window and go to next file in the buffer',
  },
  {
    command = 'sbprevious',
    flags = bit.bor(RANGE, COUNT, CMDARG, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_bprevious',
    desc = [[Split window and go to [N]th previous buffer in buffer list. Wraps around the start of the buffer list. Uses 'switchbuf'. Also see +cmd.]],
    short_desc = 'split window and go to previous file in the',
  },
  {
    command = 'sbrewind',
    flags = bit.bor(CMDARG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_brewind',
    desc = [[Split window and go to first buffer in buffer list. If the buffer list is empty, go to the first unlisted buffer. Respects the 'switchbuf' option. Also see +cmd.]],
    short_desc = 'split window and go to first file in the',
  },
  {
    command = 'scriptnames',
    flags = bit.bor(BANG, FILES, RANGE, COUNT, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_scriptnames',
    desc = 'List all sourced script names, in the order they were first sourced. The number is used for the script ID <SID>. Also see `getscriptinfo()`.',
    short_desc = 'list names of all sourced Vim scripts',
  },
  {
    command = 'scriptencoding',
    flags = bit.bor(WORD1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_scriptencoding',
    desc = [[Specify the character encoding used in the script. The following lines will be converted from [encoding] to the value of the 'encoding' option, if they are different. Examples: > scriptencoding iso-8859-5 scriptencoding cp932]],
    short_desc = 'encoding used in sourced Vim script',
  },
  {
    command = 'set',
    flags = bit.bor(BANG, TRLBAR, EXTRA, CMDWIN, LOCK_OK, SBOXOK),
    addr_type = 'ADDR_NONE',
    func = 'ex_set',
    desc = 'Show all options that differ from their default value. When [!] is present every option is on a separate line. Show all options. When [!] is present every option is on a separate line. Show value of {option}. NOTE: some legacy options were removed. nvim-removed Toggle option: set, switch it on. Number option: show value. String option: show value. Toggle option: Reset, switch it off. Toggle option: Invert value. Reset option to its default value. Reset option to its Vi default value.',
    short_desc = 'show or set options',
  },
  {
    command = 'setfiletype',
    flags = bit.bor(TRLBAR, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_setfiletype',
    desc = [[Set the 'filetype' option to {filetype}, but only if not done yet in a sequence of (nested) autocommands. This is short for: > :if !did_filetype() : setlocal filetype={filetype} :endif]],
    short_desc = [[set 'filetype', unless it was set already]],
  },
  {
    command = 'setglobal',
    flags = bit.bor(BANG, TRLBAR, EXTRA, CMDWIN, LOCK_OK, SBOXOK),
    addr_type = 'ADDR_NONE',
    func = 'ex_set',
    desc = 'Like ":set" but set only the global value for a local option without changing the local value. When displaying an option, the global value is shown. With the "all" argument: display global values for all local options. Without argument: display global values for all local options which are different from the default.',
    short_desc = 'show global values of options',
  },
  {
    command = 'setlocal',
    flags = bit.bor(BANG, TRLBAR, EXTRA, CMDWIN, LOCK_OK, SBOXOK),
    addr_type = 'ADDR_NONE',
    func = 'ex_set',
    desc = 'Like ":set" but set only the value local to the current buffer or window. Not all options have a local value. If the option does not have a local value the global value is set. With the "all" argument: display local values for all local options. Without argument: Display local values for all local options which are different from the default. When displaying a specific local option, show the local value.',
    short_desc = 'show or set options locally',
  },
  {
    command = 'sfind',
    flags = bit.bor(BANG, FILE1, RANGE, CMDARG, ARGOPT, TRLBAR, NEEDARG),
    addr_type = 'ADDR_OTHER',
    func = 'ex_splitview',
    desc = [[Same as ":split", but search for {file} in 'path' like in :find. Doesn't split if {file} is not found.]],
    short_desc = [[split current window and edit file in 'path']],
  },
  {
    command = 'sfirst',
    flags = bit.bor(EXTRA, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_rewind',
    desc = 'Same as ":srewind".',
    short_desc = 'split window and go to first file in the',
  },
  {
    command = 'simalt',
    flags = bit.bor(NEEDARG, WORD1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_ni',
  },
  {
    command = 'sign',
    flags = bit.bor(NEEDARG, RANGE, EXTRA, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_sign',
    desc = ':sign define piet text=>> texthl=Search :exe ":sign place 2 line=23 name=piet file=" .. expand("%:p")',
    short_desc = 'manipulate signs',
  },
  {
    command = 'silent',
    flags = bit.bor(NEEDARG, EXTRA, BANG, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = 'Execute {command} silently. Normal messages will not be given or added to the message history. When [!] is added, error messages will also be skipped, and commands and mappings will not be aborted when an error is detected. v:errmsg is still set. When [!] is not used, an error message will cause further messages to be displayed normally. Redirection, started with :redir, will continue as usual, although there might be small differences.',
    short_desc = 'run a command silently',
  },
  {
    command = 'sleep',
    flags = bit.bor(BANG, RANGE, COUNT, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_sleep',
    desc = 'Do nothing for [N] seconds, or [N] milliseconds if [m] was given. "gs" always uses seconds. Default is one second. > :sleep "sleep for one second :5sleep "sleep for five seconds :sleep 100m "sleep for 100 milliseconds 10gs "sleep for ten seconds',
    short_desc = 'do nothing for a few seconds',
  },
  {
    command = 'slast',
    flags = bit.bor(EXTRA, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_last',
    desc = 'Short for ":split | last": split window and go to last argument. But when there is no argument list, the window is not split. Also see ++opt and +cmd.',
    short_desc = 'split window and go to last file in the',
  },
  {
    command = 'smagic',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, PREVIEW),
    addr_type = 'ADDR_LINES',
    func = 'ex_submagic',
    preview_func = 'ex_submagic_preview',
    desc = [[Same as `:substitute`, but always use 'magic'.]],
    short_desc = [[:substitute with 'magic']],
  },
  {
    command = 'smap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-s',
    short_desc = 'like ":map" but for Select mode',
  },
  {
    command = 'smapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-s',
    short_desc = 'remove all mappings for Select mode',
  },
  {
    command = 'smenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Select mode',
  },
  {
    command = 'snext',
    flags = bit.bor(RANGE, BANG, FILES, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_next',
    desc = 'Short for ":split | [N]next": split window and go to Nth next argument. But when there is no next file, the window is not split. Also see ++opt and +cmd.',
    short_desc = 'split window and go to next file in the',
  },
  {
    command = 'snomagic',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, PREVIEW),
    addr_type = 'ADDR_LINES',
    func = 'ex_submagic',
    preview_func = 'ex_submagic_preview',
    desc = [[Same as `:substitute`, but always use 'nomagic'.]],
    short_desc = [[:substitute with 'nomagic']],
  },
  {
    command = 'snoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-s',
    short_desc = 'like ":noremap" but for Select mode',
  },
  {
    command = 'snoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Select mode',
  },
  {
    command = 'source',
    flags = bit.bor(RANGE, DFLALL, BANG, FILE1, TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_source',
    desc = 'Runs Ex-commands or Lua code (".lua" files) from {file}. Triggers the SourcePre autocommand.',
    short_desc = 'read Vim or Ex commands from a file',
  },
  {
    command = 'sort',
    flags = bit.bor(RANGE, DFLALL, WHOLEFOLD, BANG, EXTRA, NOTRLCOM, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_sort',
    desc = 'Sort lines in [range]. When no range is given all lines are sorted. With [!] the order is reversed. With [i] case is ignored. With [l] sort uses the current collation locale. Implementation details: strcoll() is used to compare strings. See :language to check or set the collation locale. Example: > :language collate en_US.UTF-8 :%sort l',
    short_desc = 'sort lines',
  },
  {
    command = 'split',
    flags = bit.bor(BANG, FILE1, RANGE, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_splitview',
    desc = [[Split current window in two. The result is two viewports on the same file. Make the new window N high (default is to use half the height of the current window). Reduces the current window height to create room (and others, if the 'equalalways' option is set, 'eadirection' isn't "hor", and one of them is higher than the current or the new window). Note: CTRL-S does not work on all terminals and might block further input, use CTRL-Q to get going again. Also see ++opt and +cmd.]],
    short_desc = 'split current window',
  },
  {
    command = 'spellgood',
    flags = bit.bor(BANG, RANGE, NEEDARG, EXTRA, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_spell',
    desc = [[Add {word} as a good word to 'spellfile', like with zg. Without count the first name is used, with a count of two the second entry, etc. Add {word} as a good word to the internal word list, like with zG.]],
    short_desc = 'add good word for spelling',
  },
  {
    command = 'spelldump',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_spelldump',
    desc = 'Open a new window and fill it with all currently valid words. Compound words are not included. Note: For some languages the result may be enormous, causing Vim to run out of memory. Like ":spelldump" and include the word count. This is the number of times the word was found while updating the screen. Words that are in COMMON items get a starting count of 10.',
    short_desc = 'split window and fill with all correct words',
  },
  {
    command = 'spellinfo',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_spellinfo',
    desc = 'Display the information for the spell file(s) used for the current buffer.',
    short_desc = 'show info about loaded spell files',
  },
  {
    command = 'spellrepall',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_spellrepall',
    desc = 'Repeat the replacement done by z= for all matches with the replaced word in the current window.',
    short_desc = 'replace all bad words like last z=',
  },
  {
    command = 'spellrare',
    flags = bit.bor(BANG, RANGE, NEEDARG, EXTRA, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_spell',
    desc = [[Add {word} as a rare word to 'spellfile', similar to zw. Without count the first name is used, with a count of two the second entry, etc. There are no normal mode commands to mark words as rare as this is a fairly uncommon command and all intuitive commands for this are already taken. If you want you can add mappings with e.g.: > nnoremap z? :exe ':spellrare ' .. expand('<cWORD>')<CR> nnoremap z/ :exe ':spellrare! ' .. expand('<cWORD>')<CR>]],
    short_desc = 'add rare word for spelling',
  },
  {
    command = 'spellundo',
    flags = bit.bor(BANG, RANGE, NEEDARG, EXTRA, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_spell',
    desc = 'Like zuw. [count] used as with :spellgood. Like zuW. [count] used as with :spellgood.',
    short_desc = 'remove good or bad word',
  },
  {
    command = 'spellwrong',
    flags = bit.bor(BANG, RANGE, NEEDARG, EXTRA, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_spell',
    desc = [[Add {word} as a wrong (bad) word to 'spellfile', as with zw. Without count the first name is used, with a count of two the second entry, etc. Add {word} as a wrong (bad) word to the internal word list, like with zW.]],
    short_desc = 'add spelling mistake',
  },
  {
    command = 'sprevious',
    flags = bit.bor(EXTRA, RANGE, COUNT, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_previous',
    desc = 'Short for ":split | [N]Next": split window and go to Nth previous argument. But when there is no previous file, the window is not split. Also see ++opt and +cmd.',
    short_desc = 'split window and go to previous file in the',
  },
  {
    command = 'srewind',
    flags = bit.bor(EXTRA, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_rewind',
    desc = 'Short for ":split | rewind": split window and go to first argument. But when there is no argument list, the window is not split. Also see ++opt and +cmd.',
    short_desc = 'split window and go to first file in the',
  },
  {
    command = 'stop',
    flags = bit.bor(TRLBAR, BANG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_stop',
    desc = [[Suspend Nvim using OS "job control"; it will continue if you make it the foreground job again. Triggers VimSuspend before suspending and VimResume when resumed. If "!" is not given and 'autowrite' is set, every buffer with changes and a file name is written out. If "!" is given or 'autowrite' is not set, changed buffers are not written, don't forget to bring Nvim back to the foreground later!]],
    short_desc = 'suspend the editor or escape to a shell',
  },
  {
    command = 'stag',
    flags = bit.bor(RANGE, BANG, WORD1, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_stag',
    desc = [[Does ":tag[!] [tagname]" and splits the window for the found tag. Refer to 'switchbuf' to jump to a tag in a vertically split window or a new tab page. See also :tag.]],
    short_desc = 'split window and jump to a tag',
  },
  {
    command = 'startinsert',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_startinsert',
    desc = 'Start Insert mode (or Terminal-mode in a terminal buffer) just after executing this command. Works like typing "i" in Normal mode. When the ! is included it works like "A", append to the line. Otherwise insertion starts at the cursor position. Note that when using this command in a function or script, the insertion only starts after the function or script is finished. This command does not work from :normal.',
    short_desc = 'start Insert mode',
  },
  {
    command = 'startgreplace',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_startinsert',
    desc = 'Just like :startreplace, but use Virtual Replace mode, like with gR.',
    short_desc = ':startg[replace] start Virtual Replace mode',
  },
  {
    command = 'startreplace',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_startinsert',
    desc = 'Start Replace mode just after executing this command. Works just like typing "R" in Normal mode. When the ! is included it acts just like "$R" had been typed (ie. begin replace mode at the end-of-line). Other- wise replacement begins at the cursor position. Note that when using this command in a function or script that the replacement will only start after the function or script is finished.',
    short_desc = 'start Replace mode',
  },
  {
    command = 'stopinsert',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_stopinsert',
    desc = 'Stop Insert mode or Terminal-mode as soon as possible. Works like typing <Esc> in Insert mode. Can be used in an autocommand, example: > :au BufEnter scratch stopinsert',
    short_desc = 'stop Insert mode',
  },
  {
    command = 'stjump',
    flags = bit.bor(BANG, TRLBAR, WORD1),
    addr_type = 'ADDR_NONE',
    func = 'ex_stag',
    desc = 'Does ":tjump[!] [name]" and splits the window for the selected tag.',
    short_desc = 'do ":tjump" and split window',
  },
  {
    command = 'stselect',
    flags = bit.bor(BANG, TRLBAR, WORD1),
    addr_type = 'ADDR_NONE',
    func = 'ex_stag',
    desc = 'Does ":tselect[!] [name]" and splits the window for the selected tag.',
    short_desc = 'do ":tselect" and split window',
  },
  {
    command = 'sunhide',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_buffer_all',
    desc = 'Rearrange the screen to open one window for each loaded buffer in the buffer list. When a count is given, this is the maximum number of windows to open.',
    short_desc = 'same as ":unhide"',
  },
  {
    command = 'sunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-s',
    short_desc = 'like ":unmap" but for Select mode',
  },
  {
    command = 'sunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Select mode',
  },
  {
    command = 'suspend',
    flags = bit.bor(TRLBAR, BANG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_stop',
    desc = [[Suspend Nvim using OS "job control"; it will continue if you make it the foreground job again. Triggers VimSuspend before suspending and VimResume when resumed. If "!" is not given and 'autowrite' is set, every buffer with changes and a file name is written out. If "!" is given or 'autowrite' is not set, changed buffers are not written, don't forget to bring Nvim back to the foreground later!]],
    short_desc = 'same as ":stop"',
  },
  {
    command = 'sview',
    flags = bit.bor(BANG, FILE1, RANGE, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_splitview',
    desc = [[Same as ":split", but set 'readonly' option for this buffer.]],
    short_desc = 'split window and edit file read-only',
  },
  {
    command = 'swapname',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_swapname',
    desc = ':sw[apname]',
    short_desc = 'show the name of the current swap file',
  },
  {
    command = 'syntax',
    flags = bit.bor(EXTRA, NOTRLCOM, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_syntax',
    desc = ':sy[ntax] [list]',
    short_desc = 'syntax highlighting',
  },
  {
    command = 'syntime',
    flags = bit.bor(NEEDARG, WORD1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_syntime',
    desc = 'To see slowness switch on some features that usually interfere, such',
    short_desc = 'measure syntax highlighting speed',
  },
  {
    command = 'syncbind',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_syncbind',
    desc = [[Force all 'scrollbind' windows to have the same relative offset. I.e., when any of the 'scrollbind' windows is scrolled to the top of its buffer, all of the 'scrollbind' windows will also be at the top of their buffers.]],
    short_desc = 'sync scroll binding',
  },
  {
    command = 't',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_copymove',
    desc = 'Synonym for copy.',
    short_desc = 'same as ":copy"',
  },
  {
    command = 'tcd',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_cd',
    desc = 'Like :cd, but only set the directory for the current tab. The current window will also use this directory. The current directory is not changed for windows in other tabs and for windows in the current tab that have their own window-local directory. Change to the previous current directory (before the previous ":tcd {path}" command).',
    short_desc = 'change directory for tab page',
  },
  {
    command = 'tchdir',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_cd',
    desc = 'Same as :tcd.',
    short_desc = 'change directory for tab page',
  },
  {
    command = 'tNext',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_tag',
    desc = 'Same as ":tprevious".',
    short_desc = 'in preview window',
  },
  {
    command = 'tag',
    flags = bit.bor(RANGE, BANG, WORD1, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_tag',
    desc = 'Jump to the definition of {name}, using the information in the tags file(s). Put {name} in the tag stack. See tag-! for [!]. {name} can be a regexp pattern, see tag-regexp. When there are several matching tags for {name}, jump to the [count] one. When [count] is omitted the first one is jumped to. See tag-matchlist for jumping to other matching tags.',
    short_desc = 'jump to tag',
  },
  {
    command = 'tags',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'do_tags',
    desc = [[Show the contents of the tag stack. The active entry is marked with a '>'.]],
    short_desc = 'show the contents of the tag stack',
  },
  {
    command = 'tab',
    flags = bit.bor(RANGE, ZEROR, NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_TABS',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd} and when it opens a new window open a new tab page instead. Doesn't work for :diffsplit, :diffpatch, :execute and :normal. If [count] is given the new tab page appears after the tab page [count] otherwise the new tab page will appear after the current one.]],
    short_desc = 'create new tab when opening new window',
  },
  {
    command = 'tabclose',
    flags = bit.bor(BANG, RANGE, ZEROR, EXTRA, NOSPC, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_TABS',
    func = 'ex_tabclose',
    desc = [[Close current tab page. This command fails when: - There is only one tab page on the screen. - When 'hidden' is not set, [!] is not used, a buffer has changes, and there is no other window on this buffer. Changes to the buffer are not written and won't get lost, so this is a "safe" command. > :tabclose " close the current tab page]],
    short_desc = 'close current tab page',
  },
  {
    command = 'tabdo',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_TABS',
    func = 'ex_listdo',
    desc = 'Execute {cmd} in each tab page or, if [range] is given, only in tabpages which tab page number is in the [range]. It works like doing this: > :tabfirst :{cmd} :tabnext :{cmd} etc.',
    short_desc = 'execute command in each tab page',
  },
  {
    command = 'tabedit',
    flags = bit.bor(BANG, FILE1, RANGE, ZEROR, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_TABS',
    func = 'ex_splitview',
    desc = 'Open a new tab page with an empty window, after the current tab page. If [count] is given the new tab page appears after the tabpage [count] otherwise the new tab page will appear after the current one. > :tabnew " opens tabpage after the current one :.tabnew " as above :+tabnew " opens tabpage after the next tab page " note: it is one further than :tabnew :-tabnew " opens tabpage before the current :0tabnew " opens tabpage before the first one :$tabnew " opens tabpage after the last one',
    short_desc = 'edit a file in a new tab page',
  },
  {
    command = 'tabfind',
    flags = bit.bor(BANG, FILE1, RANGE, ZEROR, CMDARG, ARGOPT, NEEDARG, TRLBAR),
    addr_type = 'ADDR_TABS',
    func = 'ex_splitview',
    desc = [[Open a new tab page and edit {file} in 'path', like with :find. For [count] see :tabnew above.]],
    short_desc = [[find file in 'path', edit it in a new tab page]],
  },
  {
    command = 'tabfirst',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_tabnext',
    desc = 'Go to the first tab page.',
    short_desc = 'go to first tab page',
  },
  {
    command = 'tabmove',
    flags = bit.bor(RANGE, ZEROR, EXTRA, NOSPC, TRLBAR),
    addr_type = 'ADDR_TABS',
    func = 'ex_tabmove',
    desc = 'Move the current tab page to after tab page N. Use zero to make the current tab page the first one. N is counted before the move, thus if the second tab page is the current one, `:tabmove 1` and `:tabmove 2` have no effect. Without N the tab page is made the last one.',
    short_desc = 'move tab page to other position',
  },
  {
    command = 'tablast',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_tabnext',
    desc = 'Go to the last tab page.',
    short_desc = 'go to last tab page',
  },
  {
    command = 'tabnext',
    flags = bit.bor(RANGE, ZEROR, EXTRA, NOSPC, TRLBAR),
    addr_type = 'ADDR_TABS',
    func = 'ex_tabnext',
    desc = 'Go to the next tab page. Wraps around from the last to the first one.',
    short_desc = 'go to next tab page',
  },
  {
    command = 'tabnew',
    flags = bit.bor(BANG, FILE1, RANGE, ZEROR, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_TABS',
    func = 'ex_splitview',
    desc = 'Open a new tab page with an empty window, after the current tab page. If [count] is given the new tab page appears after the tabpage [count] otherwise the new tab page will appear after the current one. > :tabnew " opens tabpage after the current one :.tabnew " as above :+tabnew " opens tabpage after the next tab page " note: it is one further than :tabnew :-tabnew " opens tabpage before the current :0tabnew " opens tabpage before the first one :$tabnew " opens tabpage after the last one',
    short_desc = 'edit a file in a new tab page',
  },
  {
    command = 'tabonly',
    flags = bit.bor(BANG, RANGE, ZEROR, EXTRA, NOSPC, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_TABS',
    func = 'ex_tabonly',
    desc = [[Close all other tab pages. When the 'hidden' option is set, all buffers in closed windows become hidden. When 'hidden' is not set, and the 'autowrite' option is set, modified buffers are written. Otherwise, windows that have buffers that are modified are not removed, unless the [!] is given, then they become hidden. But modified buffers are never abandoned, so changes cannot get lost. > :tabonly " close all tab pages except the current one Close all tab pages except {count} one.]],
    short_desc = 'close all tab pages except the current one',
  },
  {
    command = 'tabprevious',
    flags = bit.bor(RANGE, ZEROR, EXTRA, NOSPC, TRLBAR),
    addr_type = 'ADDR_TABS_RELATIVE',
    func = 'ex_tabnext',
    desc = 'Go to the previous tab page. Wraps around from the first one to the last one.',
    short_desc = 'go to previous tab page',
  },
  {
    command = 'tabNext',
    flags = bit.bor(RANGE, ZEROR, EXTRA, NOSPC, TRLBAR),
    addr_type = 'ADDR_TABS_RELATIVE',
    func = 'ex_tabnext',
    desc = 'Go to the previous tab page. Wraps around from the first one to the last one.',
    short_desc = 'go to previous tab page',
  },
  {
    command = 'tabrewind',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_tabnext',
    desc = 'Go to the first tab page.',
    short_desc = 'go to first tab page',
  },
  {
    command = 'tabs',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_tabs',
    desc = 'List the tab pages and the windows they contain. Shows a ">" for the current window. Shows a "+" for modified buffers. For example: Tab page 1 ~ + tabpage.txt ~ ex_docmd.c ~ Tab page 2 ~ > main.c ~',
    short_desc = 'list the tab pages and what they contain',
  },
  {
    command = 'tcl',
    flags = bit.bor(RANGE, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_script_ni',
  },
  {
    command = 'tcldo',
    flags = bit.bor(RANGE, DFLALL, EXTRA, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_ni',
  },
  {
    command = 'tclfile',
    flags = bit.bor(RANGE, FILE1, NEEDARG, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_ni',
  },
  {
    command = 'terminal',
    flags = bit.bor(BANG, FILES, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_terminal',
    desc = [[Run [cmd] in a non-interactive 'shell' in a new terminal-emulator buffer. Without [cmd], start an interactive 'shell'. By default the current window is used. To open in a split window, use :horizontal or :vertical: > :hor te :vert te]],
    short_desc = 'open a terminal buffer',
  },
  {
    command = 'tfirst',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_tag',
    desc = 'Same as ":trewind".',
    short_desc = 'jump to first matching tag',
  },
  {
    command = 'throw',
    flags = bit.bor(EXTRA, NEEDARG, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_throw',
    desc = 'The {expr1} is evaluated and thrown as an exception. If the `:throw` is used after a `:try` but before the first corresponding `:catch`, commands are skipped until the first `:catch` matching {expr1} is reached. If there is no such `:catch` or if the `:throw` is used after a `:catch` but before the `:finally`, the commands following the `:finally` (if present) up to the matching `:endtry` are executed. If the `:throw` is after the `:finally`, commands up to the `:endtry` are skipped.',
    short_desc = 'throw an exception',
  },
  {
    command = 'tjump',
    flags = bit.bor(BANG, TRLBAR, WORD1),
    addr_type = 'ADDR_NONE',
    func = 'ex_tag',
    desc = 'Like ":tselect", but jump to the tag directly when there is only one match.',
    short_desc = 'to tag under',
  },
  {
    command = 'tlast',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_tag',
    desc = 'Jump to last matching tag. See tag-! for [!].',
    short_desc = 'in preview window',
  },
  {
    command = 'tlmenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Terminal-mode',
  },
  {
    command = 'tlnoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Terminal-mode',
  },
  {
    command = 'tlunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Terminal-mode',
  },
  {
    command = 'tmenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = 'Define a tip for a menu or tool. (only in X11 and Win32 GUI) List menu tips. (only in X11 and Win32 GUI)',
    short_desc = 'define menu tooltip',
  },
  {
    command = 'tmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-t Map the key sequence {lhs} to {rhs} for the modes where the map command applies. The result, including {rhs}, is then further scanned for mappings. This allows for nested and recursive use of mappings. Note: Trailing spaces are included in the {rhs}, because space is a valid Normal mode command. See map-trailing-white.',
    short_desc = 'like ":map" but for Terminal-mode',
  },
  {
    command = 'tmapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-t Remove ALL mappings for the modes where the map command applies. Use the <buffer> argument to remove buffer-local mappings :map-<buffer> Warning: This also removes the default-mappings.',
    short_desc = 'remove all mappings for Terminal-mode',
  },
  {
    command = 'tnext',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_tag',
    desc = 'Jump to [count] next matching tag (default 1). See tag-! for [!].',
    short_desc = 'in preview window',
  },
  {
    command = 'tnoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = [[mapmode-t Map the key sequence {lhs} to {rhs} for the modes where the map command applies. Disallow mapping of {rhs}, to avoid nested and recursive mappings. Often used to redefine a command. Note: Keys in {rhs} also won't trigger abbreviation, with the exception of i_CTRL-] and c_CTRL-]. Note: When <Plug> appears in the {rhs} this part is always applied even if remapping is disallowed.]],
    short_desc = 'like ":noremap" but for Terminal-mode',
  },
  {
    command = 'topleft',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd}. If it contains a command that splits a window, it will appear at the top and occupy the full width of the Vim window. When the split is vertical the window appears at the far left and occupies the full height of the Vim window. Doesn't work for :execute and :normal.]],
    short_desc = 'make split window appear at top or far left',
  },
  {
    command = 'tprevious',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_tag',
    desc = 'Jump to [count] previous matching tag (default 1). See tag-! for [!].',
    short_desc = 'in preview window',
  },
  {
    command = 'trewind',
    flags = bit.bor(RANGE, BANG, TRLBAR, ZEROR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_tag',
    desc = 'Jump to first matching tag. If [count] is given, jump to [count]th matching tag. See tag-! for [!].',
    short_desc = 'in preview window',
  },
  {
    command = 'trust',
    flags = bit.bor(EXTRA, FILE1, TRLBAR, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_trust',
    desc = [[Manage trusted files. Without ++ options, :trust marks the current buffer (or [file], but see WARNING below) as trusted, keyed on a hash of its contents. The trust list is stored on disk, Nvim will re-use it after restarting. WARNING: `:trust [file]` has a TOCTOU risk, so for security you should view the file, inspect its contents, and run `:trust` (without [file] arg). [++deny] marks the current buffer or [file] as untrusted: it will never be executed, 'exrc' will ignore it.]],
    short_desc = 'add or remove file from trust database',
  },
  {
    command = 'try',
    flags = bit.bor(TRLBAR, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_try',
    desc = 'Change the error handling for the commands between `:try` and `:endtry` including everything being executed across `:source` commands, function calls, or autocommand invocations. When an error or interrupt is detected and there is a `:finally` command following, execution continues after the `:finally`. Otherwise, or when the `:endtry` is reached thereafter, the next (dynamically) surrounding `:try` is checked for a corresponding `:finally` etc. Then the script processing is terminated.',
    short_desc = 'execute commands, abort on error or exception',
  },
  {
    command = 'tselect',
    flags = bit.bor(BANG, TRLBAR, WORD1),
    addr_type = 'ADDR_NONE',
    func = 'ex_tag',
    desc = [[List the tags that match [name], using the information in the tags file(s). When [name] is not given, the last tag name from the tag stack is used. See tag-! for [!]. With a '>' in the first column is indicated which is the current position in the list (if there is one). [name] can be a regexp pattern, see tag-regexp. See tag-priority for the priorities used in the listing. Example output:]],
    short_desc = 'for tag',
  },
  {
    command = 'tunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = 'Remove a tip for a menu or tool. (only in X11 and Win32 GUI)',
    short_desc = 'remove menu tooltip',
  },
  {
    command = 'tunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-t Remove the mapping of {lhs} for the modes where the map command applies. The mapping may remain defined for other modes where it applies. It also works when {lhs} matches the {rhs} of a mapping. This is for when an abbreviation applied. Note: Trailing spaces are included in the {lhs}. See map-trailing-white.',
    short_desc = 'like ":unmap" but for Terminal-mode',
  },
  {
    command = 'undo',
    flags = bit.bor(BANG, RANGE, COUNT, ZEROR, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_undo',
    desc = 'Undo one change. Jump to after change number {N}. See undo-branches for the meaning of {N}. Undo one change and remove it from undo history. Like ":u[ndo] {N}", but forget all changes in the current undo branch up until {N}. You may only use ":undo! {N}" to move backwards in the same undo branch, not to redo or switch to a different undo branch.',
    short_desc = 'undo last change(s)',
  },
  {
    command = 'undojoin',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_undojoin',
    desc = [[Join further changes with the previous undo block. Warning: Use with care, it may prevent the user from properly undoing changes. Don't use this after undo or redo.]],
    short_desc = 'join next change with previous undo block',
  },
  {
    command = 'undolist',
    flags = bit.bor(TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_undolist',
    desc = 'List the leafs in the tree of changes. Example: number changes when saved ~ 88 88 2010/01/04 14:25:53 108 107 08/07 12:47:51 136 46 13:33:01 7 166 164 3 seconds ago The "number" column is the change number. This number continuously increases and can be used to identify a specific undo-able change, see :undo. The "changes" column is the number of changes to this leaf from the root of the tree. The "when" column is the date and time when this change was made.',
    short_desc = 'list leafs of the undo tree',
  },
  {
    command = 'unabbreviate',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_abbreviate',
    desc = 'Remove abbreviation for {lhs} from the list. If none is found, remove abbreviations in which {lhs} matches with the {rhs}. This is done so that you can even remove abbreviations after expansion. To avoid expansion insert a CTRL-V (type it twice).',
    short_desc = 'remove abbreviation',
  },
  {
    command = 'unhide',
    flags = bit.bor(RANGE, COUNT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_buffer_all',
    desc = 'Rearrange the screen to open one window for each loaded buffer in the buffer list. When a count is given, this is the maximum number of windows to open.',
    short_desc = 'open a window for each loaded file in the',
  },
  {
    command = 'uniq',
    flags = bit.bor(RANGE, DFLALL, WHOLEFOLD, BANG, EXTRA, NOTRLCOM, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_uniq',
    desc = 'Remove duplicate lines that are adjacent to each other in [range]. When no range is given, all lines are processed. With [i] case is ignored when comparing lines. With [l] comparison uses the current collation locale. See :sort-l for more details. With [r] comparison is done on the text that matches /{pattern}/ instead of the full line. With [u] only keep lines that do not repeat (i.e., are not immediately followed by the same line).',
    short_desc = 'uniq lines',
  },
  {
    command = 'unlet',
    flags = bit.bor(BANG, EXTRA, NEEDARG, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unlet',
    desc = 'Remove the internal variable {name}. Several variable names can be given, they are all removed. The name may also be a List or Dictionary item. With [!] no error message is given for non-existing variables. One or more items from a List can be removed: > :unlet list[3] " remove fourth item :unlet list[3:] " remove fourth item to last',
    short_desc = 'delete variable',
  },
  {
    command = 'unlockvar',
    flags = bit.bor(BANG, EXTRA, NEEDARG, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_lockvar',
    desc = 'Unlock the internal variable {name}. Does the opposite of :lockvar. No error is given if {name} does not exist.',
    short_desc = 'unlock variables',
  },
  {
    command = 'unmap',
    flags = bit.bor(BANG, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-nvo',
    short_desc = 'remove mapping',
  },
  {
    command = 'unmenu',
    flags = bit.bor(BANG, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = 'Eg: > :unmenu! Edit.Paste',
    short_desc = 'remove menu',
  },
  {
    command = 'unsilent',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {command} not silently. Only makes a difference when :silent was used to get to this command. Use this for giving a message even when :silent was used. In this example :silent is used to avoid the message about reading the file and :unsilent to be able to list the first line of each file. > :silent argdo unsilent echo expand('%') .. ": " .. getline(1)]],
    short_desc = 'run a command not silently',
  },
  {
    command = 'update',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, FILE1, ARGOPT, DFLALL, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_update',
    desc = [[Like ":write", but only write when the buffer has been modified, or when the buffer represents a new file that doesn't exist on disk.]],
    short_desc = 'write buffer if modified',
  },
  {
    command = 'uptime',
    flags = bit.bor(CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_uptime',
    desc = [[Shows Nvim's uptime. See also v:starttime.]],
    short_desc = [[show Nvim's uptime]],
  },
  {
    command = 'vglobal',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, DFLALL, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_global',
    desc = 'Same as :g!.',
    short_desc = 'execute commands for not matching lines',
  },
  {
    command = 'version',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_version',
    desc = 'Print editor version and build information. See also feature-compile.',
    short_desc = 'print version number and other info',
  },
  {
    command = 'verbose',
    flags = bit.bor(NEEDARG, RANGE, EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_wrongmodifier',
    desc = [[Execute {command} with 'verbose' set to [count]. If [count] is omitted one is used. ":0verbose" can be used to set 'verbose' to zero. The additional use of ":silent" makes messages generated but not displayed. The combination of ":silent" and ":verbose" can be used to generate messages and check them with v:statusmsg and friends. For example: > :let v:statusmsg = "" :silent verbose runtime foobar.vim :if v:statusmsg != "" : " foobar.vim could not be found :endif]],
    short_desc = [[execute command with 'verbose' set]],
  },
  {
    command = 'vertical',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM),
    addr_type = 'ADDR_NONE',
    func = 'ex_wrongmodifier',
    desc = [[Execute {cmd}. If it contains a command that splits a window, it will be split vertically. For `vertical wincmd =` windows will be equalized only vertically. Doesn't work for :execute and :normal.]],
    short_desc = 'make following command split vertically',
  },
  {
    command = 'visual',
    flags = bit.bor(BANG, FILE1, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_edit',
    desc = 'When used in Ex mode: Leave Ex-mode, go back to Normal mode. Otherwise same as :edit.',
    short_desc = 'same as ":edit", but turns off "Ex" mode',
  },
  {
    command = 'view',
    flags = bit.bor(BANG, FILE1, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_edit',
    desc = [[When used in Ex mode: Leave Ex-mode, go back to Normal mode. Otherwise same as :edit, but set 'readonly' option for this buffer.]],
    short_desc = 'edit a file read-only',
  },
  {
    command = 'vimgrep',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_vimgrep',
    desc = [[Search for {pattern} in the files {file} ... and set the error list to the matches. Files matching 'wildignore' are ignored; files in 'suffixes' are searched last. {pattern} is a Vim search pattern. Instead of enclosing it in / any non-ID character (see 'isident') can be used, so long as it does not appear in {pattern}. 'ignorecase' applies. To overrule it put /\c in the pattern to ignore case or /\C to match case. 'smartcase' is not used. If {pattern} is empty (e.g.]],
    short_desc = 'search for pattern in files',
  },
  {
    command = 'vimgrepadd',
    flags = bit.bor(RANGE, BANG, NEEDARG, EXTRA, NOTRLCOM, TRLBAR, XFILE, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_vimgrep',
    desc = 'Just like ":vimgrep", but instead of making a new list of errors the matches are appended to the current list.',
    short_desc = 'like :vimgrep, but append to current list',
  },
  {
    command = 'viusage',
    flags = bit.bor(TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_viusage',
    desc = 'Show help on Normal mode commands. Added to simulate the Nvi command.',
    short_desc = 'overview of Normal mode commands',
  },
  {
    command = 'vmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-v',
    short_desc = 'like ":map" but for Visual+Select mode',
  },
  {
    command = 'vmapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-v',
    short_desc = 'remove all mappings for Visual+Select mode',
  },
  {
    command = 'vmenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Visual+Select mode',
  },
  {
    command = 'vnoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-v',
    short_desc = 'like ":noremap" but for Visual+Select mode',
  },
  {
    command = 'vnew',
    flags = bit.bor(BANG, FILE1, RANGE, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_splitview',
    desc = [[Like :new, but split vertically. If 'equalalways' is set and 'eadirection' isn't "ver" the windows will be spread out horizontally, unless a width was specified.]],
    short_desc = 'create a new empty window, vertically split',
  },
  {
    command = 'vnoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Visual+Select mode',
  },
  {
    command = 'vsplit',
    flags = bit.bor(BANG, FILE1, RANGE, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_splitview',
    desc = [[Like :split, but split vertically. The windows will be spread out horizontally if 1. a width was not specified, 2. 'equalalways' is set, 3. 'eadirection' isn't "ver", and 4. one of the other windows is wider than the current or new window. If N was given make the new window N columns wide, if possible. Note: In other places CTRL-Q does the same as CTRL-V, but here it doesn't!]],
    short_desc = 'split current window vertically',
  },
  {
    command = 'vunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-v',
    short_desc = 'like ":unmap" but for Visual+Select mode',
  },
  {
    command = 'vunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Visual+Select mode',
  },
  {
    command = 'write',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, FILE1, ARGOPT, DFLALL, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_write',
    desc = [[Write the whole buffer to the current file. This is the normal way to save changes to a file. Fails when 'readonly' is set or when there is another reason why the file can't be written, such as when the parent directory doesn't exist (use ++p to avoid that). For ++opt see ++opt, but only ++p, ++bin, ++nobin, ++ff and ++enc are effective.]],
    short_desc = 'write to a file',
  },
  {
    command = 'wNext',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, FILE1, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_wnext',
    desc = 'Same as :wnext, but go to previous file instead of next.',
    short_desc = 'write to a file and go to previous file in',
  },
  {
    command = 'wall',
    flags = bit.bor(BANG, TRLBAR, CMDWIN, LOCK_OK, ARGOPT),
    addr_type = 'ADDR_NONE',
    func = 'do_wqall',
    desc = '[++opt] Write all changed buffers. Buffers without a file name cause an error message. Buffers which are readonly are not written. For ++opt see ++opt, but only ++p is effective, and applies to each written file. Write all changed buffers, even the ones that are readonly. Buffers without a file name are not written and cause an error message.',
    short_desc = 'write all (changed) buffers',
  },
  {
    command = 'while',
    flags = bit.bor(EXTRA, NOTRLCOM, SBOXOK, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_while',
    desc = 'Repeat the commands between `:while` and `:endwhile`, as long as {expr1} evaluates to non-zero. When an error is detected from a command inside the loop, execution continues after the `endwhile`. Example: > :let lnum = 1 :while lnum <= line("$") :call FixLine(lnum) :let lnum = lnum + 1 :endwhile',
    short_desc = 'execute loop for as long as condition met',
  },
  {
    command = 'winsize',
    flags = bit.bor(EXTRA, NEEDARG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'ex_winsize',
    desc = 'Set the window height to {width} by {height} characters. Obsolete, use ":set lines=11 columns=22".',
    short_desc = 'get or set window size (obsolete)',
  },
  {
    command = 'wincmd',
    flags = bit.bor(NEEDARG, WORD1, RANGE, COUNT, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_wincmd',
    desc = 'Like executing CTRL-W [count] {arg}. Example: > :wincmd j',
    short_desc = 'execute a Window (CTRL-W) command',
  },
  {
    command = 'windo',
    flags = bit.bor(NEEDARG, EXTRA, NOTRLCOM, RANGE, DFLALL),
    addr_type = 'ADDR_WINDOWS',
    func = 'ex_listdo',
    desc = 'Execute {cmd} in each focusable window, or only for windows in a given [range] of window numbers. It works like doing this: > CTRL-W t :{cmd} CTRL-W w :{cmd} etc.',
    short_desc = 'execute command in each window',
  },
  {
    command = 'winpos',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_ni',
    desc = 'Display current position of the top left corner of the GUI vim window in pixels. Does not work in all versions. Also see getwinpos(), getwinposx() and getwinposy().',
    short_desc = 'get or set window position',
  },
  {
    command = 'wnext',
    flags = bit.bor(RANGE, BANG, FILE1, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_wnext',
    desc = 'Write current file and start editing the [count] next file. Also see ++opt and +cmd.',
    short_desc = 'write to a file and go to next file in',
  },
  {
    command = 'wprevious',
    flags = bit.bor(RANGE, BANG, FILE1, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_wnext',
    desc = 'Same as :wnext, but go to previous file instead of next.',
    short_desc = 'write to a file and go to previous file in',
  },
  {
    command = 'wq',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, FILE1, ARGOPT, DFLALL, TRLBAR),
    addr_type = 'ADDR_LINES',
    func = 'ex_exit',
    desc = 'Write the current file and close the window. If this was the last edit-window Vim quits. Writing fails when the file is read-only or the buffer does not have a name. Quitting fails when the last file in the argument list has not been edited. Write the current file and close the window. If this was the last edit-window Vim quits. Writing fails when the current buffer does not have a name. Write to {file} and close the window. If this was the last edit-window Vim quits.',
    short_desc = 'write to a file and quit window or Vim',
  },
  {
    command = 'wqall',
    flags = bit.bor(BANG, FILE1, ARGOPT, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'do_wqall',
    desc = 'Write all changed buffers and exit Vim. If there are buffers without a file name, which are readonly or which cannot be written for another reason, Vim will not quit.',
    short_desc = 'write all changed buffers and quit Vim',
  },
  {
    command = 'wshada',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_shada',
    desc = [[Write to ShaDa file [file] (default: see above). The information in the file is first read in to make a merge between old and new info. When [!] is used, the old information is not read first, only the internal info is written (also disables safety checks described in shada-error-handling). If 'shada' is empty, marks for up to 100 files will be written. When you get error "E929: All .tmp.X files exist, cannot write ShaDa file!", check that no old temp files were left behind (e.g.]],
    short_desc = 'write to ShaDa file',
  },
  {
    command = 'wundo',
    flags = bit.bor(BANG, NEEDARG, FILE1),
    addr_type = 'ADDR_NONE',
    func = 'ex_wundo',
    desc = 'Write undo history to {file}. When {file} exists and it does not look like an undo file (the magic number at the start of the file is wrong), then this fails, unless the ! was added. If it exists and does look like an undo file it is overwritten. If there is no undo-history, nothing will be written. Implementation detail: Overwriting happens by first deleting the existing file and then creating a new file with the same name.',
    short_desc = 'write undo information to a file',
  },
  {
    command = 'wviminfo',
    flags = bit.bor(BANG, FILE1, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_shada',
    desc = 'Deprecated alias to :wshada command.',
  },
  {
    command = 'xit',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, FILE1, ARGOPT, DFLALL, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_exit',
    desc = [[Like ":wq", but write only when changes have been made. When 'hidden' is set and there are more windows, the current buffer becomes hidden, after writing the file.]],
    short_desc = 'write if buffer changed and close window',
  },
  {
    command = 'xall',
    flags = bit.bor(BANG, TRLBAR),
    addr_type = 'ADDR_NONE',
    func = 'do_wqall',
    desc = 'Write all changed buffers and exit Vim. If there are buffers without a file name, which are readonly or which cannot be written for another reason, Vim will not quit.',
    short_desc = 'same as ":wqall"',
  },
  {
    command = 'xmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = '{lhs} {rhs} mapmode-x',
    short_desc = 'like ":map" but for Visual mode',
  },
  {
    command = 'xmapclear',
    flags = bit.bor(EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_mapclear',
    desc = 'mapmode-x',
    short_desc = 'remove all mappings for Visual mode',
  },
  {
    command = 'xmenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'add menu for Visual mode',
  },
  {
    command = 'xnoremap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_map',
    desc = 'mapmode-x',
    short_desc = 'like ":noremap" but for Visual mode',
  },
  {
    command = 'xnoremenu',
    flags = bit.bor(RANGE, ZEROR, EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_OTHER',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'like ":noremenu" but for Visual mode',
  },
  {
    command = 'xunmap',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_unmap',
    desc = 'mapmode-x',
    short_desc = 'like ":unmap" but for Visual mode',
  },
  {
    command = 'xunmenu',
    flags = bit.bor(EXTRA, TRLBAR, NOTRLCOM, CTRLV, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_NONE',
    func = 'ex_menu',
    desc = [[Example: > :menu <silent> Settings.Ignore\ case :set ic<CR>]],
    short_desc = 'remove menu for Visual mode',
  },
  {
    command = 'yank',
    flags = bit.bor(RANGE, WHOLEFOLD, REGSTR, COUNT, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_operators',
    desc = 'Yank [range] lines [into register x].',
    short_desc = 'yank lines into a register',
  },
  {
    command = 'z',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, EXTRA, FLAGS, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_z',
    desc = [[Display several lines of text surrounding the line specified with [range], or around the current line if there is no [range]. If there is a [count], that's how many lines you'll see; if there is no [count] and only one window then twice the value of the 'scroll' option is used, otherwise the current window height minus 3 is used. This is the value of "scr" in the table below. If there is a [count] the 'window' option is set to its value.]],
    short_desc = 'print some lines',
  },
  -- commands that don't start with a letter
  {
    command = '!',
    enum = 'CMD_bang',
    flags = bit.bor(RANGE, WHOLEFOLD, BANG, FILES, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_bang',
    short_desc = 'filter lines or execute an external command',
    desc = [[Execute {cmd} with 'shell'. See also :terminal. For the filter command, see :range!. The command runs in a non-interactive shell connected to a pipe (not a terminal). Use :terminal to run an interactive shell connected to a terminal. Backgrounded ("&") commands must not write to stdout or stderr, the streams are closed immediately. E5677 Use jobstart() instead. > :call jobstart('foo', {'detach':1})]],
  },
  {
    command = '#',
    enum = 'CMD_pound',
    flags = bit.bor(RANGE, WHOLEFOLD, COUNT, FLAGS, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_print',
    short_desc = 'same as ":number"',
    desc = 'synonym for :number.',
  },
  {
    command = '&',
    enum = 'CMD_and',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_substitute',
    short_desc = 'repeat last ":substitute"',
    desc = [[Repeat last :substitute with same search pattern and substitute string, but without the same flags. You may add [flags], see :s_flags. Note that after `:substitute` the '&' and '#' flags can't be used, they're recognized as a pattern separator. The space between `:substitute` and the 'c', 'g', 'i', 'I' and 'r' flags isn't required, but in scripts it's a good idea to keep it to avoid confusion. Also see the two and three letter commands to repeat :substitute below :substitute-repeat.]],
  },
  {
    command = '<',
    enum = 'CMD_lshift',
    flags = bit.bor(RANGE, WHOLEFOLD, COUNT, FLAGS, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_operators',
    short_desc = [[shift lines one 'shiftwidth' left]],
    desc = [[Shift [range] lines one 'shiftwidth' left. Repeat '<' for shifting multiple 'shiftwidth's.]],
  },
  {
    command = '=',
    enum = 'CMD_equal',
    flags = bit.bor(RANGE, EXTRA, DFLALL, ARGOPT, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_equal',
    short_desc = 'print the last line number',
    desc = 'Without [args]: prints the last line number. With [args]: equivalent to `:lua ={expr}`. see :lua',
  },
  {
    command = '>',
    enum = 'CMD_rshift',
    flags = bit.bor(RANGE, WHOLEFOLD, COUNT, FLAGS, TRLBAR, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_operators',
    short_desc = [[shift lines one 'shiftwidth' right]],
    desc = [[Shift [range] lines one 'shiftwidth' right. Repeat '>' for shifting multiple 'shiftwidth's. See ex-flags for [flags].]],
  },
  {
    command = '@',
    enum = 'CMD_at',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, TRLBAR, CMDWIN, LOCK_OK),
    addr_type = 'ADDR_LINES',
    func = 'ex_at',
    short_desc = 'execute contents of a register',
    desc = [[Execute the contents of register {0-9a-z".=*+} as an Ex command. First set cursor at line [addr] (default is current line). When the last line in the register does not have a <CR> it will be added automatically when the 'e' flag is present in 'cpoptions'. For ":@=" the last used expression is used. The result of evaluating the expression is executed as an Ex command. Mappings are not recognized in these commands.]],
  },
  {
    command = '~',
    enum = 'CMD_tilde',
    flags = bit.bor(RANGE, WHOLEFOLD, EXTRA, CMDWIN, LOCK_OK, MODIFY),
    addr_type = 'ADDR_LINES',
    func = 'ex_substitute',
    short_desc = 'repeat last ":substitute"',
    desc = 'Repeat last substitute with same substitute string but with last used search pattern. This is like `:&r`. See :s_flags for [flags].',
  },
  -- commands that start with an uppercase letter
  {
    command = 'Next',
    flags = bit.bor(EXTRA, RANGE, COUNT, BANG, CMDARG, ARGOPT, TRLBAR),
    addr_type = 'ADDR_OTHER',
    func = 'ex_previous',
    desc = 'Edit [count] previous file in argument list. This fails when changes have been made and Vim does not want to abandon the current buffer. Also see ++opt and +cmd.',
    short_desc = 'go to previous file in the argument list',
  },
}

return M
