language: c
compiler:
  - clang-asan
  - gcc-gcov
  - gcc-unittest
before_install:
  - sudo ./scripts/travis-setup.sh 
script:
  - ./scripts/travis.sh
after_success:
  - coveralls --encoding iso-8859-1
