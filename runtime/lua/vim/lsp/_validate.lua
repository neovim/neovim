--[[
THIS FILE IS GENERATED by src/gen/gen_lsp.lua
DO NOT EDIT MANUALLY

Validates non-nullable fields on incoming LSP server→client messages (M)
and on results returned to client→server requests (validate_result).
Null (vim.NIL) values in fields that the spec does not allow to be null
are repaired (set to nil) and a warning is logged to the LSP log.

Regenerate:
  nvim -l src/gen/gen_lsp.lua
--]]

local NIL = vim.NIL
local M = {}
local validate_result = {}

local _log = require('vim.lsp.log')
local _client_name = '?'
---@type table<string, true>
local _warned = {}
---@param msg string
local function warn(msg)
  local name = _client_name
  local key = (msg:gsub('%[%d+%]', '[]'))
  if _warned[name .. key] then
    return
  end
  _warned[name .. key] = true
  _log.warn(('LSP[%s] %s'):format(name, key))
end

---@param v lsp.Registration|vim.NIL|nil
---@param ctx string
local validate_Registration = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['id'] == NIL then
    v['id'] = nil
    warn(ctx .. '.id must not be null')
  end
  if v['method'] == NIL then
    v['method'] = nil
    warn(ctx .. '.method must not be null')
  end
end

---@param v lsp.RegistrationParams|vim.NIL|nil
---@param ctx string
local validate_RegistrationParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['registrations'] == NIL then
    v['registrations'] = nil
    warn(ctx .. '.registrations must not be null')
  end
  if type(v['registrations']) == 'table' then
    for i, x in ipairs(v['registrations']) do
      validate_Registration(x, ctx .. '.registrations[' .. i .. ']')
    end
  end
end

---@param v lsp.Unregistration|vim.NIL|nil
---@param ctx string
local validate_Unregistration = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['id'] == NIL then
    v['id'] = nil
    warn(ctx .. '.id must not be null')
  end
  if v['method'] == NIL then
    v['method'] = nil
    warn(ctx .. '.method must not be null')
  end
end

---@param v lsp.UnregistrationParams|vim.NIL|nil
---@param ctx string
local validate_UnregistrationParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['unregisterations'] == NIL then
    v['unregisterations'] = nil
    warn(ctx .. '.unregisterations must not be null')
  end
  if type(v['unregisterations']) == 'table' then
    for i, x in ipairs(v['unregisterations']) do
      validate_Unregistration(x, ctx .. '.unregisterations[' .. i .. ']')
    end
  end
end

---@param v lsp.CancelParams|vim.NIL|nil
---@param ctx string
local validate_CancelParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['id'] == NIL then
    v['id'] = nil
    warn(ctx .. '.id must not be null')
  end
end

---@param v lsp.LogTraceParams|vim.NIL|nil
---@param ctx string
local validate_LogTraceParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['message'] == NIL then
    v['message'] = nil
    warn(ctx .. '.message must not be null')
  end
  if v['verbose'] == NIL then
    v['verbose'] = nil
    warn(ctx .. '.verbose must not be null')
  end
end

---@param v lsp.ProgressParams|vim.NIL|nil
---@param ctx string
local validate_ProgressParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['token'] == NIL then
    v['token'] = nil
    warn(ctx .. '.token must not be null')
  end
end

---@param v lsp.Range|vim.NIL|nil
---@param ctx string
local validate_Range = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['start'] == NIL then
    v['start'] = nil
    warn(ctx .. '.start must not be null')
  end
  if v['end'] == NIL then
    v['end'] = nil
    warn(ctx .. '.end must not be null')
  end
end

---@param v lsp.CodeDescription|vim.NIL|nil
---@param ctx string
local validate_CodeDescription = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['href'] == NIL then
    v['href'] = nil
    warn(ctx .. '.href must not be null')
  end
end

---@param v lsp.DiagnosticRelatedInformation|vim.NIL|nil
---@param ctx string
local validate_DiagnosticRelatedInformation = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['location'] == NIL then
    v['location'] = nil
    warn(ctx .. '.location must not be null')
  end
  if v['message'] == NIL then
    v['message'] = nil
    warn(ctx .. '.message must not be null')
  end
end

---@param v lsp.Diagnostic|vim.NIL|nil
---@param ctx string
local validate_Diagnostic = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['severity'] == NIL then
    v['severity'] = nil
    warn(ctx .. '.severity must not be null')
  end
  if v['code'] == NIL then
    v['code'] = nil
    warn(ctx .. '.code must not be null')
  end
  if v['codeDescription'] == NIL then
    v['codeDescription'] = nil
    warn(ctx .. '.codeDescription must not be null')
  end
  validate_CodeDescription(v['codeDescription'], ctx .. '.codeDescription')
  if v['source'] == NIL then
    v['source'] = nil
    warn(ctx .. '.source must not be null')
  end
  if v['message'] == NIL then
    v['message'] = nil
    warn(ctx .. '.message must not be null')
  end
  if v['tags'] == NIL then
    v['tags'] = nil
    warn(ctx .. '.tags must not be null')
  end
  if v['relatedInformation'] == NIL then
    v['relatedInformation'] = nil
    warn(ctx .. '.relatedInformation must not be null')
  end
  if type(v['relatedInformation']) == 'table' then
    for i, x in ipairs(v['relatedInformation']) do
      validate_DiagnosticRelatedInformation(x, ctx .. '.relatedInformation[' .. i .. ']')
    end
  end
end

---@param v lsp.PublishDiagnosticsParams|vim.NIL|nil
---@param ctx string
local validate_PublishDiagnosticsParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
  if v['version'] == NIL then
    v['version'] = nil
    warn(ctx .. '.version must not be null')
  end
  if v['diagnostics'] == NIL then
    v['diagnostics'] = nil
    warn(ctx .. '.diagnostics must not be null')
  end
  if type(v['diagnostics']) == 'table' then
    for i, x in ipairs(v['diagnostics']) do
      validate_Diagnostic(x, ctx .. '.diagnostics[' .. i .. ']')
    end
  end
end

---@param v lsp.LogMessageParams|vim.NIL|nil
---@param ctx string
local validate_LogMessageParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['type'] == NIL then
    v['type'] = nil
    warn(ctx .. '.type must not be null')
  end
  if v['message'] == NIL then
    v['message'] = nil
    warn(ctx .. '.message must not be null')
  end
end

---@param v lsp.ShowDocumentParams|vim.NIL|nil
---@param ctx string
local validate_ShowDocumentParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
  if v['external'] == NIL then
    v['external'] = nil
    warn(ctx .. '.external must not be null')
  end
  if v['takeFocus'] == NIL then
    v['takeFocus'] = nil
    warn(ctx .. '.takeFocus must not be null')
  end
  if v['selection'] == NIL then
    v['selection'] = nil
    warn(ctx .. '.selection must not be null')
  end
  validate_Range(v['selection'], ctx .. '.selection')
end

---@param v lsp.ShowMessageParams|vim.NIL|nil
---@param ctx string
local validate_ShowMessageParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['type'] == NIL then
    v['type'] = nil
    warn(ctx .. '.type must not be null')
  end
  if v['message'] == NIL then
    v['message'] = nil
    warn(ctx .. '.message must not be null')
  end
end

---@param v lsp.MessageActionItem|vim.NIL|nil
---@param ctx string
local validate_MessageActionItem = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['title'] == NIL then
    v['title'] = nil
    warn(ctx .. '.title must not be null')
  end
end

---@param v lsp.ShowMessageRequestParams|vim.NIL|nil
---@param ctx string
local validate_ShowMessageRequestParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['type'] == NIL then
    v['type'] = nil
    warn(ctx .. '.type must not be null')
  end
  if v['message'] == NIL then
    v['message'] = nil
    warn(ctx .. '.message must not be null')
  end
  if v['actions'] == NIL then
    v['actions'] = nil
    warn(ctx .. '.actions must not be null')
  end
  if type(v['actions']) == 'table' then
    for i, x in ipairs(v['actions']) do
      validate_MessageActionItem(x, ctx .. '.actions[' .. i .. ']')
    end
  end
end

---@param v lsp.WorkDoneProgressCreateParams|vim.NIL|nil
---@param ctx string
local validate_WorkDoneProgressCreateParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['token'] == NIL then
    v['token'] = nil
    warn(ctx .. '.token must not be null')
  end
end

---@param v lsp.TextDocumentEdit|vim.NIL|nil
---@param ctx string
local validate_TextDocumentEdit = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['textDocument'] == NIL then
    v['textDocument'] = nil
    warn(ctx .. '.textDocument must not be null')
  end
  if v['edits'] == NIL then
    v['edits'] = nil
    warn(ctx .. '.edits must not be null')
  end
end

---@param v lsp.CreateFile|vim.NIL|nil
---@param ctx string
local validate_CreateFile = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
  if v['options'] == NIL then
    v['options'] = nil
    warn(ctx .. '.options must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['annotationId'] == NIL then
    v['annotationId'] = nil
    warn(ctx .. '.annotationId must not be null')
  end
end

---@param v lsp.RenameFile|vim.NIL|nil
---@param ctx string
local validate_RenameFile = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['oldUri'] == NIL then
    v['oldUri'] = nil
    warn(ctx .. '.oldUri must not be null')
  end
  if v['newUri'] == NIL then
    v['newUri'] = nil
    warn(ctx .. '.newUri must not be null')
  end
  if v['options'] == NIL then
    v['options'] = nil
    warn(ctx .. '.options must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['annotationId'] == NIL then
    v['annotationId'] = nil
    warn(ctx .. '.annotationId must not be null')
  end
end

---@param v lsp.DeleteFile|vim.NIL|nil
---@param ctx string
local validate_DeleteFile = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
  if v['options'] == NIL then
    v['options'] = nil
    warn(ctx .. '.options must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['annotationId'] == NIL then
    v['annotationId'] = nil
    warn(ctx .. '.annotationId must not be null')
  end
end

---@param v lsp.WorkspaceEdit|vim.NIL|nil
---@param ctx string
local validate_WorkspaceEdit = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['changes'] == NIL then
    v['changes'] = nil
    warn(ctx .. '.changes must not be null')
  end
  if v['documentChanges'] == NIL then
    v['documentChanges'] = nil
    warn(ctx .. '.documentChanges must not be null')
  end
  if type(v['documentChanges']) == 'table' then
    for i, x in ipairs(v['documentChanges']) do
      validate_TextDocumentEdit(
        x --[[@as lsp.TextDocumentEdit|vim.NIL|nil]],
        ctx .. '.documentChanges[' .. i .. ']'
      )
    end
  end
  if type(v['documentChanges']) == 'table' then
    for i, x in ipairs(v['documentChanges']) do
      validate_CreateFile(
        x --[[@as lsp.CreateFile|vim.NIL|nil]],
        ctx .. '.documentChanges[' .. i .. ']'
      )
    end
  end
  if type(v['documentChanges']) == 'table' then
    for i, x in ipairs(v['documentChanges']) do
      validate_RenameFile(
        x --[[@as lsp.RenameFile|vim.NIL|nil]],
        ctx .. '.documentChanges[' .. i .. ']'
      )
    end
  end
  if type(v['documentChanges']) == 'table' then
    for i, x in ipairs(v['documentChanges']) do
      validate_DeleteFile(
        x --[[@as lsp.DeleteFile|vim.NIL|nil]],
        ctx .. '.documentChanges[' .. i .. ']'
      )
    end
  end
  if v['changeAnnotations'] == NIL then
    v['changeAnnotations'] = nil
    warn(ctx .. '.changeAnnotations must not be null')
  end
end

---@param v lsp.WorkspaceEditMetadata|vim.NIL|nil
---@param ctx string
local validate_WorkspaceEditMetadata = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['isRefactoring'] == NIL then
    v['isRefactoring'] = nil
    warn(ctx .. '.isRefactoring must not be null')
  end
end

---@param v lsp.ApplyWorkspaceEditParams|vim.NIL|nil
---@param ctx string
local validate_ApplyWorkspaceEditParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['label'] == NIL then
    v['label'] = nil
    warn(ctx .. '.label must not be null')
  end
  if v['edit'] == NIL then
    v['edit'] = nil
    warn(ctx .. '.edit must not be null')
  end
  validate_WorkspaceEdit(v['edit'], ctx .. '.edit')
  if v['metadata'] == NIL then
    v['metadata'] = nil
    warn(ctx .. '.metadata must not be null')
  end
  validate_WorkspaceEditMetadata(v['metadata'], ctx .. '.metadata')
end

---@param v lsp.ConfigurationItem|vim.NIL|nil
---@param ctx string
local validate_ConfigurationItem = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['scopeUri'] == NIL then
    v['scopeUri'] = nil
    warn(ctx .. '.scopeUri must not be null')
  end
  if v['section'] == NIL then
    v['section'] = nil
    warn(ctx .. '.section must not be null')
  end
end

---@param v lsp.ConfigurationParams|vim.NIL|nil
---@param ctx string
local validate_ConfigurationParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['items'] == NIL then
    v['items'] = nil
    warn(ctx .. '.items must not be null')
  end
  if type(v['items']) == 'table' then
    for i, x in ipairs(v['items']) do
      validate_ConfigurationItem(x, ctx .. '.items[' .. i .. ']')
    end
  end
end

---@param v lsp.TextDocumentContentRefreshParams|vim.NIL|nil
---@param ctx string
local validate_TextDocumentContentRefreshParams = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
end

---@param params lsp.RegistrationParams?
M['client/registerCapability'] = function(params)
  validate_RegistrationParams(params, 'client/registerCapability params')
end

---@param params lsp.UnregistrationParams?
M['client/unregisterCapability'] = function(params)
  validate_UnregistrationParams(params, 'client/unregisterCapability params')
end

---@param params lsp.CancelParams?
M['$/cancelRequest'] = function(params)
  validate_CancelParams(params, '$/cancelRequest params')
end

---@param params lsp.LogTraceParams?
M['$/logTrace'] = function(params)
  validate_LogTraceParams(params, '$/logTrace params')
end

---@param params lsp.ProgressParams?
M['$/progress'] = function(params)
  validate_ProgressParams(params, '$/progress params')
end

---@param params lsp.PublishDiagnosticsParams?
M['textDocument/publishDiagnostics'] = function(params)
  validate_PublishDiagnosticsParams(params, 'textDocument/publishDiagnostics params')
end

---@param params lsp.LogMessageParams?
M['window/logMessage'] = function(params)
  validate_LogMessageParams(params, 'window/logMessage params')
end

---@param params lsp.ShowDocumentParams?
M['window/showDocument'] = function(params)
  validate_ShowDocumentParams(params, 'window/showDocument params')
end

---@param params lsp.ShowMessageParams?
M['window/showMessage'] = function(params)
  validate_ShowMessageParams(params, 'window/showMessage params')
end

---@param params lsp.ShowMessageRequestParams?
M['window/showMessageRequest'] = function(params)
  validate_ShowMessageRequestParams(params, 'window/showMessageRequest params')
end

---@param params lsp.WorkDoneProgressCreateParams?
M['window/workDoneProgress/create'] = function(params)
  validate_WorkDoneProgressCreateParams(params, 'window/workDoneProgress/create params')
end

---@param params lsp.ApplyWorkspaceEditParams?
M['workspace/applyEdit'] = function(params)
  validate_ApplyWorkspaceEditParams(params, 'workspace/applyEdit params')
end

---@param params lsp.ConfigurationParams?
M['workspace/configuration'] = function(params)
  validate_ConfigurationParams(params, 'workspace/configuration params')
end

---@param params lsp.TextDocumentContentRefreshParams?
M['workspace/textDocumentContent/refresh'] = function(params)
  validate_TextDocumentContentRefreshParams(params, 'workspace/textDocumentContent/refresh params')
end

---@param v lsp.CallHierarchyItem|vim.NIL|nil
---@param ctx string
local validate_CallHierarchyItem = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['name'] == NIL then
    v['name'] = nil
    warn(ctx .. '.name must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['tags'] == NIL then
    v['tags'] = nil
    warn(ctx .. '.tags must not be null')
  end
  if v['detail'] == NIL then
    v['detail'] = nil
    warn(ctx .. '.detail must not be null')
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['selectionRange'] == NIL then
    v['selectionRange'] = nil
    warn(ctx .. '.selectionRange must not be null')
  end
  validate_Range(v['selectionRange'], ctx .. '.selectionRange')
end

---@param v lsp.CallHierarchyIncomingCall|vim.NIL|nil
---@param ctx string
local validate_CallHierarchyIncomingCall = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['from'] == NIL then
    v['from'] = nil
    warn(ctx .. '.from must not be null')
  end
  validate_CallHierarchyItem(v['from'], ctx .. '.from')
  if v['fromRanges'] == NIL then
    v['fromRanges'] = nil
    warn(ctx .. '.fromRanges must not be null')
  end
  if type(v['fromRanges']) == 'table' then
    for i, x in ipairs(v['fromRanges']) do
      validate_Range(x, ctx .. '.fromRanges[' .. i .. ']')
    end
  end
end

---@param v lsp.CallHierarchyOutgoingCall|vim.NIL|nil
---@param ctx string
local validate_CallHierarchyOutgoingCall = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['to'] == NIL then
    v['to'] = nil
    warn(ctx .. '.to must not be null')
  end
  validate_CallHierarchyItem(v['to'], ctx .. '.to')
  if v['fromRanges'] == NIL then
    v['fromRanges'] = nil
    warn(ctx .. '.fromRanges must not be null')
  end
  if type(v['fromRanges']) == 'table' then
    for i, x in ipairs(v['fromRanges']) do
      validate_Range(x, ctx .. '.fromRanges[' .. i .. ']')
    end
  end
end

---@param v lsp.CodeActionDisabled|vim.NIL|nil
---@param ctx string
local validate_CodeActionDisabled = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['reason'] == NIL then
    v['reason'] = nil
    warn(ctx .. '.reason must not be null')
  end
end

---@param v lsp.Command|vim.NIL|nil
---@param ctx string
local validate_Command = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['title'] == NIL then
    v['title'] = nil
    warn(ctx .. '.title must not be null')
  end
  if v['tooltip'] == NIL then
    v['tooltip'] = nil
    warn(ctx .. '.tooltip must not be null')
  end
  if v['command'] == NIL then
    v['command'] = nil
    warn(ctx .. '.command must not be null')
  end
  if v['arguments'] == NIL then
    v['arguments'] = nil
    warn(ctx .. '.arguments must not be null')
  end
end

---@param v lsp.CodeAction|vim.NIL|nil
---@param ctx string
local validate_CodeAction = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['title'] == NIL then
    v['title'] = nil
    warn(ctx .. '.title must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['diagnostics'] == NIL then
    v['diagnostics'] = nil
    warn(ctx .. '.diagnostics must not be null')
  end
  if type(v['diagnostics']) == 'table' then
    for i, x in ipairs(v['diagnostics']) do
      validate_Diagnostic(x, ctx .. '.diagnostics[' .. i .. ']')
    end
  end
  if v['isPreferred'] == NIL then
    v['isPreferred'] = nil
    warn(ctx .. '.isPreferred must not be null')
  end
  if v['disabled'] == NIL then
    v['disabled'] = nil
    warn(ctx .. '.disabled must not be null')
  end
  validate_CodeActionDisabled(v['disabled'], ctx .. '.disabled')
  if v['edit'] == NIL then
    v['edit'] = nil
    warn(ctx .. '.edit must not be null')
  end
  validate_WorkspaceEdit(v['edit'], ctx .. '.edit')
  if v['command'] == NIL then
    v['command'] = nil
    warn(ctx .. '.command must not be null')
  end
  validate_Command(v['command'], ctx .. '.command')
  if v['tags'] == NIL then
    v['tags'] = nil
    warn(ctx .. '.tags must not be null')
  end
end

---@param v lsp.CodeLens|vim.NIL|nil
---@param ctx string
local validate_CodeLens = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['command'] == NIL then
    v['command'] = nil
    warn(ctx .. '.command must not be null')
  end
  validate_Command(v['command'], ctx .. '.command')
end

---@param v lsp.CompletionItemLabelDetails|vim.NIL|nil
---@param ctx string
local validate_CompletionItemLabelDetails = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['detail'] == NIL then
    v['detail'] = nil
    warn(ctx .. '.detail must not be null')
  end
  if v['description'] == NIL then
    v['description'] = nil
    warn(ctx .. '.description must not be null')
  end
end

---@param v lsp.TextEdit|vim.NIL|nil
---@param ctx string
local validate_TextEdit = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['newText'] == NIL then
    v['newText'] = nil
    warn(ctx .. '.newText must not be null')
  end
end

---@param v lsp.InsertReplaceEdit|vim.NIL|nil
---@param ctx string
local validate_InsertReplaceEdit = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['newText'] == NIL then
    v['newText'] = nil
    warn(ctx .. '.newText must not be null')
  end
  if v['insert'] == NIL then
    v['insert'] = nil
    warn(ctx .. '.insert must not be null')
  end
  validate_Range(v['insert'], ctx .. '.insert')
  if v['replace'] == NIL then
    v['replace'] = nil
    warn(ctx .. '.replace must not be null')
  end
  validate_Range(v['replace'], ctx .. '.replace')
end

---@param v lsp.CompletionItem|vim.NIL|nil
---@param ctx string
local validate_CompletionItem = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['label'] == NIL then
    v['label'] = nil
    warn(ctx .. '.label must not be null')
  end
  if v['labelDetails'] == NIL then
    v['labelDetails'] = nil
    warn(ctx .. '.labelDetails must not be null')
  end
  validate_CompletionItemLabelDetails(v['labelDetails'], ctx .. '.labelDetails')
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['tags'] == NIL then
    v['tags'] = nil
    warn(ctx .. '.tags must not be null')
  end
  if v['detail'] == NIL then
    v['detail'] = nil
    warn(ctx .. '.detail must not be null')
  end
  if v['documentation'] == NIL then
    v['documentation'] = nil
    warn(ctx .. '.documentation must not be null')
  end
  if v['deprecated'] == NIL then
    v['deprecated'] = nil
    warn(ctx .. '.deprecated must not be null')
  end
  if v['preselect'] == NIL then
    v['preselect'] = nil
    warn(ctx .. '.preselect must not be null')
  end
  if v['sortText'] == NIL then
    v['sortText'] = nil
    warn(ctx .. '.sortText must not be null')
  end
  if v['filterText'] == NIL then
    v['filterText'] = nil
    warn(ctx .. '.filterText must not be null')
  end
  if v['insertText'] == NIL then
    v['insertText'] = nil
    warn(ctx .. '.insertText must not be null')
  end
  if v['insertTextFormat'] == NIL then
    v['insertTextFormat'] = nil
    warn(ctx .. '.insertTextFormat must not be null')
  end
  if v['insertTextMode'] == NIL then
    v['insertTextMode'] = nil
    warn(ctx .. '.insertTextMode must not be null')
  end
  if v['textEdit'] == NIL then
    v['textEdit'] = nil
    warn(ctx .. '.textEdit must not be null')
  end
  validate_TextEdit(v['textEdit'] --[[@as lsp.TextEdit|vim.NIL|nil]], ctx .. '.textEdit')
  validate_InsertReplaceEdit(
    v['textEdit'] --[[@as lsp.InsertReplaceEdit|vim.NIL|nil]],
    ctx .. '.textEdit'
  )
  if v['textEditText'] == NIL then
    v['textEditText'] = nil
    warn(ctx .. '.textEditText must not be null')
  end
  if v['additionalTextEdits'] == NIL then
    v['additionalTextEdits'] = nil
    warn(ctx .. '.additionalTextEdits must not be null')
  end
  if type(v['additionalTextEdits']) == 'table' then
    for i, x in ipairs(v['additionalTextEdits']) do
      validate_TextEdit(x, ctx .. '.additionalTextEdits[' .. i .. ']')
    end
  end
  if v['commitCharacters'] == NIL then
    v['commitCharacters'] = nil
    warn(ctx .. '.commitCharacters must not be null')
  end
  if v['command'] == NIL then
    v['command'] = nil
    warn(ctx .. '.command must not be null')
  end
  validate_Command(v['command'], ctx .. '.command')
end

---@param v lsp.DocumentLink|vim.NIL|nil
---@param ctx string
local validate_DocumentLink = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['target'] == NIL then
    v['target'] = nil
    warn(ctx .. '.target must not be null')
  end
  if v['tooltip'] == NIL then
    v['tooltip'] = nil
    warn(ctx .. '.tooltip must not be null')
  end
end

---@param v lsp.NotebookDocumentSyncOptions|vim.NIL|nil
---@param ctx string
local validate_NotebookDocumentSyncOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['notebookSelector'] == NIL then
    v['notebookSelector'] = nil
    warn(ctx .. '.notebookSelector must not be null')
  end
  if v['save'] == NIL then
    v['save'] = nil
    warn(ctx .. '.save must not be null')
  end
end

---@param v lsp.NotebookDocumentSyncRegistrationOptions|vim.NIL|nil
---@param ctx string
local validate_NotebookDocumentSyncRegistrationOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['notebookSelector'] == NIL then
    v['notebookSelector'] = nil
    warn(ctx .. '.notebookSelector must not be null')
  end
  if v['save'] == NIL then
    v['save'] = nil
    warn(ctx .. '.save must not be null')
  end
  if v['id'] == NIL then
    v['id'] = nil
    warn(ctx .. '.id must not be null')
  end
end

---@param v lsp.CompletionOptions|vim.NIL|nil
---@param ctx string
local validate_CompletionOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['triggerCharacters'] == NIL then
    v['triggerCharacters'] = nil
    warn(ctx .. '.triggerCharacters must not be null')
  end
  if v['allCommitCharacters'] == NIL then
    v['allCommitCharacters'] = nil
    warn(ctx .. '.allCommitCharacters must not be null')
  end
  if v['resolveProvider'] == NIL then
    v['resolveProvider'] = nil
    warn(ctx .. '.resolveProvider must not be null')
  end
  if v['completionItem'] == NIL then
    v['completionItem'] = nil
    warn(ctx .. '.completionItem must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
end

---@param v lsp.SignatureHelpOptions|vim.NIL|nil
---@param ctx string
local validate_SignatureHelpOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['triggerCharacters'] == NIL then
    v['triggerCharacters'] = nil
    warn(ctx .. '.triggerCharacters must not be null')
  end
  if v['retriggerCharacters'] == NIL then
    v['retriggerCharacters'] = nil
    warn(ctx .. '.retriggerCharacters must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
end

---@param v lsp.CodeLensOptions|vim.NIL|nil
---@param ctx string
local validate_CodeLensOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['resolveProvider'] == NIL then
    v['resolveProvider'] = nil
    warn(ctx .. '.resolveProvider must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
end

---@param v lsp.DocumentLinkOptions|vim.NIL|nil
---@param ctx string
local validate_DocumentLinkOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['resolveProvider'] == NIL then
    v['resolveProvider'] = nil
    warn(ctx .. '.resolveProvider must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
end

---@param v lsp.DocumentOnTypeFormattingOptions|vim.NIL|nil
---@param ctx string
local validate_DocumentOnTypeFormattingOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['firstTriggerCharacter'] == NIL then
    v['firstTriggerCharacter'] = nil
    warn(ctx .. '.firstTriggerCharacter must not be null')
  end
  if v['moreTriggerCharacter'] == NIL then
    v['moreTriggerCharacter'] = nil
    warn(ctx .. '.moreTriggerCharacter must not be null')
  end
end

---@param v lsp.ExecuteCommandOptions|vim.NIL|nil
---@param ctx string
local validate_ExecuteCommandOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['commands'] == NIL then
    v['commands'] = nil
    warn(ctx .. '.commands must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
end

---@param v lsp.SemanticTokensOptions|vim.NIL|nil
---@param ctx string
local validate_SemanticTokensOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['legend'] == NIL then
    v['legend'] = nil
    warn(ctx .. '.legend must not be null')
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  if v['full'] == NIL then
    v['full'] = nil
    warn(ctx .. '.full must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
end

---@param v lsp.SemanticTokensRegistrationOptions|vim.NIL|nil
---@param ctx string
local validate_SemanticTokensRegistrationOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['legend'] == NIL then
    v['legend'] = nil
    warn(ctx .. '.legend must not be null')
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  if v['full'] == NIL then
    v['full'] = nil
    warn(ctx .. '.full must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
  if v['id'] == NIL then
    v['id'] = nil
    warn(ctx .. '.id must not be null')
  end
end

---@param v lsp.DiagnosticOptions|vim.NIL|nil
---@param ctx string
local validate_DiagnosticOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['identifier'] == NIL then
    v['identifier'] = nil
    warn(ctx .. '.identifier must not be null')
  end
  if v['interFileDependencies'] == NIL then
    v['interFileDependencies'] = nil
    warn(ctx .. '.interFileDependencies must not be null')
  end
  if v['workspaceDiagnostics'] == NIL then
    v['workspaceDiagnostics'] = nil
    warn(ctx .. '.workspaceDiagnostics must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
end

---@param v lsp.DiagnosticRegistrationOptions|vim.NIL|nil
---@param ctx string
local validate_DiagnosticRegistrationOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['identifier'] == NIL then
    v['identifier'] = nil
    warn(ctx .. '.identifier must not be null')
  end
  if v['interFileDependencies'] == NIL then
    v['interFileDependencies'] = nil
    warn(ctx .. '.interFileDependencies must not be null')
  end
  if v['workspaceDiagnostics'] == NIL then
    v['workspaceDiagnostics'] = nil
    warn(ctx .. '.workspaceDiagnostics must not be null')
  end
  if v['workDoneProgress'] == NIL then
    v['workDoneProgress'] = nil
    warn(ctx .. '.workDoneProgress must not be null')
  end
  if v['id'] == NIL then
    v['id'] = nil
    warn(ctx .. '.id must not be null')
  end
end

---@param v lsp.WorkspaceOptions|vim.NIL|nil
---@param ctx string
local validate_WorkspaceOptions = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['workspaceFolders'] == NIL then
    v['workspaceFolders'] = nil
    warn(ctx .. '.workspaceFolders must not be null')
  end
  if v['fileOperations'] == NIL then
    v['fileOperations'] = nil
    warn(ctx .. '.fileOperations must not be null')
  end
  if v['textDocumentContent'] == NIL then
    v['textDocumentContent'] = nil
    warn(ctx .. '.textDocumentContent must not be null')
  end
end

---@param v lsp.ServerCapabilities|vim.NIL|nil
---@param ctx string
local validate_ServerCapabilities = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['positionEncoding'] == NIL then
    v['positionEncoding'] = nil
    warn(ctx .. '.positionEncoding must not be null')
  end
  if v['textDocumentSync'] == NIL then
    v['textDocumentSync'] = nil
    warn(ctx .. '.textDocumentSync must not be null')
  end
  if v['notebookDocumentSync'] == NIL then
    v['notebookDocumentSync'] = nil
    warn(ctx .. '.notebookDocumentSync must not be null')
  end
  validate_NotebookDocumentSyncOptions(
    v['notebookDocumentSync'] --[[@as lsp.NotebookDocumentSyncOptions|vim.NIL|nil]],
    ctx .. '.notebookDocumentSync'
  )
  validate_NotebookDocumentSyncRegistrationOptions(
    v['notebookDocumentSync'] --[[@as lsp.NotebookDocumentSyncRegistrationOptions|vim.NIL|nil]],
    ctx .. '.notebookDocumentSync'
  )
  if v['completionProvider'] == NIL then
    v['completionProvider'] = nil
    warn(ctx .. '.completionProvider must not be null')
  end
  validate_CompletionOptions(v['completionProvider'], ctx .. '.completionProvider')
  if v['hoverProvider'] == NIL then
    v['hoverProvider'] = nil
    warn(ctx .. '.hoverProvider must not be null')
  end
  if v['signatureHelpProvider'] == NIL then
    v['signatureHelpProvider'] = nil
    warn(ctx .. '.signatureHelpProvider must not be null')
  end
  validate_SignatureHelpOptions(v['signatureHelpProvider'], ctx .. '.signatureHelpProvider')
  if v['declarationProvider'] == NIL then
    v['declarationProvider'] = nil
    warn(ctx .. '.declarationProvider must not be null')
  end
  if v['definitionProvider'] == NIL then
    v['definitionProvider'] = nil
    warn(ctx .. '.definitionProvider must not be null')
  end
  if v['typeDefinitionProvider'] == NIL then
    v['typeDefinitionProvider'] = nil
    warn(ctx .. '.typeDefinitionProvider must not be null')
  end
  if v['implementationProvider'] == NIL then
    v['implementationProvider'] = nil
    warn(ctx .. '.implementationProvider must not be null')
  end
  if v['referencesProvider'] == NIL then
    v['referencesProvider'] = nil
    warn(ctx .. '.referencesProvider must not be null')
  end
  if v['documentHighlightProvider'] == NIL then
    v['documentHighlightProvider'] = nil
    warn(ctx .. '.documentHighlightProvider must not be null')
  end
  if v['documentSymbolProvider'] == NIL then
    v['documentSymbolProvider'] = nil
    warn(ctx .. '.documentSymbolProvider must not be null')
  end
  if v['codeActionProvider'] == NIL then
    v['codeActionProvider'] = nil
    warn(ctx .. '.codeActionProvider must not be null')
  end
  if v['codeLensProvider'] == NIL then
    v['codeLensProvider'] = nil
    warn(ctx .. '.codeLensProvider must not be null')
  end
  validate_CodeLensOptions(v['codeLensProvider'], ctx .. '.codeLensProvider')
  if v['documentLinkProvider'] == NIL then
    v['documentLinkProvider'] = nil
    warn(ctx .. '.documentLinkProvider must not be null')
  end
  validate_DocumentLinkOptions(v['documentLinkProvider'], ctx .. '.documentLinkProvider')
  if v['colorProvider'] == NIL then
    v['colorProvider'] = nil
    warn(ctx .. '.colorProvider must not be null')
  end
  if v['workspaceSymbolProvider'] == NIL then
    v['workspaceSymbolProvider'] = nil
    warn(ctx .. '.workspaceSymbolProvider must not be null')
  end
  if v['documentFormattingProvider'] == NIL then
    v['documentFormattingProvider'] = nil
    warn(ctx .. '.documentFormattingProvider must not be null')
  end
  if v['documentRangeFormattingProvider'] == NIL then
    v['documentRangeFormattingProvider'] = nil
    warn(ctx .. '.documentRangeFormattingProvider must not be null')
  end
  if v['documentOnTypeFormattingProvider'] == NIL then
    v['documentOnTypeFormattingProvider'] = nil
    warn(ctx .. '.documentOnTypeFormattingProvider must not be null')
  end
  validate_DocumentOnTypeFormattingOptions(
    v['documentOnTypeFormattingProvider'],
    ctx .. '.documentOnTypeFormattingProvider'
  )
  if v['renameProvider'] == NIL then
    v['renameProvider'] = nil
    warn(ctx .. '.renameProvider must not be null')
  end
  if v['foldingRangeProvider'] == NIL then
    v['foldingRangeProvider'] = nil
    warn(ctx .. '.foldingRangeProvider must not be null')
  end
  if v['selectionRangeProvider'] == NIL then
    v['selectionRangeProvider'] = nil
    warn(ctx .. '.selectionRangeProvider must not be null')
  end
  if v['executeCommandProvider'] == NIL then
    v['executeCommandProvider'] = nil
    warn(ctx .. '.executeCommandProvider must not be null')
  end
  validate_ExecuteCommandOptions(v['executeCommandProvider'], ctx .. '.executeCommandProvider')
  if v['callHierarchyProvider'] == NIL then
    v['callHierarchyProvider'] = nil
    warn(ctx .. '.callHierarchyProvider must not be null')
  end
  if v['linkedEditingRangeProvider'] == NIL then
    v['linkedEditingRangeProvider'] = nil
    warn(ctx .. '.linkedEditingRangeProvider must not be null')
  end
  if v['semanticTokensProvider'] == NIL then
    v['semanticTokensProvider'] = nil
    warn(ctx .. '.semanticTokensProvider must not be null')
  end
  validate_SemanticTokensOptions(
    v['semanticTokensProvider'] --[[@as lsp.SemanticTokensOptions|vim.NIL|nil]],
    ctx .. '.semanticTokensProvider'
  )
  validate_SemanticTokensRegistrationOptions(
    v['semanticTokensProvider'] --[[@as lsp.SemanticTokensRegistrationOptions|vim.NIL|nil]],
    ctx .. '.semanticTokensProvider'
  )
  if v['monikerProvider'] == NIL then
    v['monikerProvider'] = nil
    warn(ctx .. '.monikerProvider must not be null')
  end
  if v['typeHierarchyProvider'] == NIL then
    v['typeHierarchyProvider'] = nil
    warn(ctx .. '.typeHierarchyProvider must not be null')
  end
  if v['inlineValueProvider'] == NIL then
    v['inlineValueProvider'] = nil
    warn(ctx .. '.inlineValueProvider must not be null')
  end
  if v['inlayHintProvider'] == NIL then
    v['inlayHintProvider'] = nil
    warn(ctx .. '.inlayHintProvider must not be null')
  end
  if v['diagnosticProvider'] == NIL then
    v['diagnosticProvider'] = nil
    warn(ctx .. '.diagnosticProvider must not be null')
  end
  validate_DiagnosticOptions(
    v['diagnosticProvider'] --[[@as lsp.DiagnosticOptions|vim.NIL|nil]],
    ctx .. '.diagnosticProvider'
  )
  validate_DiagnosticRegistrationOptions(
    v['diagnosticProvider'] --[[@as lsp.DiagnosticRegistrationOptions|vim.NIL|nil]],
    ctx .. '.diagnosticProvider'
  )
  if v['inlineCompletionProvider'] == NIL then
    v['inlineCompletionProvider'] = nil
    warn(ctx .. '.inlineCompletionProvider must not be null')
  end
  if v['workspace'] == NIL then
    v['workspace'] = nil
    warn(ctx .. '.workspace must not be null')
  end
  validate_WorkspaceOptions(v['workspace'], ctx .. '.workspace')
end

---@param v lsp.ServerInfo|vim.NIL|nil
---@param ctx string
local validate_ServerInfo = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['name'] == NIL then
    v['name'] = nil
    warn(ctx .. '.name must not be null')
  end
  if v['version'] == NIL then
    v['version'] = nil
    warn(ctx .. '.version must not be null')
  end
end

---@param v lsp.InitializeResult|vim.NIL|nil
---@param ctx string
local validate_InitializeResult = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['capabilities'] == NIL then
    v['capabilities'] = nil
    warn(ctx .. '.capabilities must not be null')
  end
  validate_ServerCapabilities(v['capabilities'], ctx .. '.capabilities')
  if v['serverInfo'] == NIL then
    v['serverInfo'] = nil
    warn(ctx .. '.serverInfo must not be null')
  end
  validate_ServerInfo(v['serverInfo'], ctx .. '.serverInfo')
end

---@param v lsp.Position|vim.NIL|nil
---@param ctx string
local validate_Position = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['line'] == NIL then
    v['line'] = nil
    warn(ctx .. '.line must not be null')
  end
  if v['character'] == NIL then
    v['character'] = nil
    warn(ctx .. '.character must not be null')
  end
end

---@param v lsp.InlayHint|vim.NIL|nil
---@param ctx string
local validate_InlayHint = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['position'] == NIL then
    v['position'] = nil
    warn(ctx .. '.position must not be null')
  end
  validate_Position(v['position'], ctx .. '.position')
  if v['label'] == NIL then
    v['label'] = nil
    warn(ctx .. '.label must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['textEdits'] == NIL then
    v['textEdits'] = nil
    warn(ctx .. '.textEdits must not be null')
  end
  if type(v['textEdits']) == 'table' then
    for i, x in ipairs(v['textEdits']) do
      validate_TextEdit(x, ctx .. '.textEdits[' .. i .. ']')
    end
  end
  if v['tooltip'] == NIL then
    v['tooltip'] = nil
    warn(ctx .. '.tooltip must not be null')
  end
  if v['paddingLeft'] == NIL then
    v['paddingLeft'] = nil
    warn(ctx .. '.paddingLeft must not be null')
  end
  if v['paddingRight'] == NIL then
    v['paddingRight'] = nil
    warn(ctx .. '.paddingRight must not be null')
  end
end

---@param v lsp.ColorPresentation|vim.NIL|nil
---@param ctx string
local validate_ColorPresentation = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['label'] == NIL then
    v['label'] = nil
    warn(ctx .. '.label must not be null')
  end
  if v['textEdit'] == NIL then
    v['textEdit'] = nil
    warn(ctx .. '.textEdit must not be null')
  end
  validate_TextEdit(v['textEdit'], ctx .. '.textEdit')
  if v['additionalTextEdits'] == NIL then
    v['additionalTextEdits'] = nil
    warn(ctx .. '.additionalTextEdits must not be null')
  end
  if type(v['additionalTextEdits']) == 'table' then
    for i, x in ipairs(v['additionalTextEdits']) do
      validate_TextEdit(x, ctx .. '.additionalTextEdits[' .. i .. ']')
    end
  end
end

---@param v lsp.Color|vim.NIL|nil
---@param ctx string
local validate_Color = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['red'] == NIL then
    v['red'] = nil
    warn(ctx .. '.red must not be null')
  end
  if v['green'] == NIL then
    v['green'] = nil
    warn(ctx .. '.green must not be null')
  end
  if v['blue'] == NIL then
    v['blue'] = nil
    warn(ctx .. '.blue must not be null')
  end
  if v['alpha'] == NIL then
    v['alpha'] = nil
    warn(ctx .. '.alpha must not be null')
  end
end

---@param v lsp.ColorInformation|vim.NIL|nil
---@param ctx string
local validate_ColorInformation = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['color'] == NIL then
    v['color'] = nil
    warn(ctx .. '.color must not be null')
  end
  validate_Color(v['color'], ctx .. '.color')
end

---@param v lsp.DocumentHighlight|vim.NIL|nil
---@param ctx string
local validate_DocumentHighlight = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
end

---@param v lsp.FoldingRange|vim.NIL|nil
---@param ctx string
local validate_FoldingRange = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['startLine'] == NIL then
    v['startLine'] = nil
    warn(ctx .. '.startLine must not be null')
  end
  if v['startCharacter'] == NIL then
    v['startCharacter'] = nil
    warn(ctx .. '.startCharacter must not be null')
  end
  if v['endLine'] == NIL then
    v['endLine'] = nil
    warn(ctx .. '.endLine must not be null')
  end
  if v['endCharacter'] == NIL then
    v['endCharacter'] = nil
    warn(ctx .. '.endCharacter must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['collapsedText'] == NIL then
    v['collapsedText'] = nil
    warn(ctx .. '.collapsedText must not be null')
  end
end

---@param v lsp.Hover|vim.NIL|nil
---@param ctx string
local validate_Hover = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['contents'] == NIL then
    v['contents'] = nil
    warn(ctx .. '.contents must not be null')
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
end

---@param v lsp.InlineCompletionItem|vim.NIL|nil
---@param ctx string
local validate_InlineCompletionItem = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['insertText'] == NIL then
    v['insertText'] = nil
    warn(ctx .. '.insertText must not be null')
  end
  if v['filterText'] == NIL then
    v['filterText'] = nil
    warn(ctx .. '.filterText must not be null')
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['command'] == NIL then
    v['command'] = nil
    warn(ctx .. '.command must not be null')
  end
  validate_Command(v['command'], ctx .. '.command')
end

---@param v lsp.LinkedEditingRanges|vim.NIL|nil
---@param ctx string
local validate_LinkedEditingRanges = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['ranges'] == NIL then
    v['ranges'] = nil
    warn(ctx .. '.ranges must not be null')
  end
  if type(v['ranges']) == 'table' then
    for i, x in ipairs(v['ranges']) do
      validate_Range(x, ctx .. '.ranges[' .. i .. ']')
    end
  end
  if v['wordPattern'] == NIL then
    v['wordPattern'] = nil
    warn(ctx .. '.wordPattern must not be null')
  end
end

---@param v lsp.Moniker|vim.NIL|nil
---@param ctx string
local validate_Moniker = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['scheme'] == NIL then
    v['scheme'] = nil
    warn(ctx .. '.scheme must not be null')
  end
  if v['identifier'] == NIL then
    v['identifier'] = nil
    warn(ctx .. '.identifier must not be null')
  end
  if v['unique'] == NIL then
    v['unique'] = nil
    warn(ctx .. '.unique must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
end

---@param v lsp.TypeHierarchyItem|vim.NIL|nil
---@param ctx string
local validate_TypeHierarchyItem = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['name'] == NIL then
    v['name'] = nil
    warn(ctx .. '.name must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['tags'] == NIL then
    v['tags'] = nil
    warn(ctx .. '.tags must not be null')
  end
  if v['detail'] == NIL then
    v['detail'] = nil
    warn(ctx .. '.detail must not be null')
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['selectionRange'] == NIL then
    v['selectionRange'] = nil
    warn(ctx .. '.selectionRange must not be null')
  end
  validate_Range(v['selectionRange'], ctx .. '.selectionRange')
end

---@param v lsp.Location|vim.NIL|nil
---@param ctx string
local validate_Location = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
end

---@param v lsp.SelectionRange|vim.NIL|nil
---@param ctx string
local validate_SelectionRange = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['range'] == NIL then
    v['range'] = nil
    warn(ctx .. '.range must not be null')
  end
  validate_Range(v['range'], ctx .. '.range')
  if v['parent'] == NIL then
    v['parent'] = nil
    warn(ctx .. '.parent must not be null')
  end
end

---@param v lsp.SemanticTokens|vim.NIL|nil
---@param ctx string
local validate_SemanticTokens = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['resultId'] == NIL then
    v['resultId'] = nil
    warn(ctx .. '.resultId must not be null')
  end
  if v['data'] == NIL then
    v['data'] = nil
    warn(ctx .. '.data must not be null')
  end
end

---@param v lsp.ParameterInformation|vim.NIL|nil
---@param ctx string
local validate_ParameterInformation = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['label'] == NIL then
    v['label'] = nil
    warn(ctx .. '.label must not be null')
  end
  if v['documentation'] == NIL then
    v['documentation'] = nil
    warn(ctx .. '.documentation must not be null')
  end
end

---@param v lsp.SignatureInformation|vim.NIL|nil
---@param ctx string
local validate_SignatureInformation = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['label'] == NIL then
    v['label'] = nil
    warn(ctx .. '.label must not be null')
  end
  if v['documentation'] == NIL then
    v['documentation'] = nil
    warn(ctx .. '.documentation must not be null')
  end
  if v['parameters'] == NIL then
    v['parameters'] = nil
    warn(ctx .. '.parameters must not be null')
  end
  if type(v['parameters']) == 'table' then
    for i, x in ipairs(v['parameters']) do
      validate_ParameterInformation(x, ctx .. '.parameters[' .. i .. ']')
    end
  end
end

---@param v lsp.SignatureHelp|vim.NIL|nil
---@param ctx string
local validate_SignatureHelp = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['signatures'] == NIL then
    v['signatures'] = nil
    warn(ctx .. '.signatures must not be null')
  end
  if type(v['signatures']) == 'table' then
    for i, x in ipairs(v['signatures']) do
      validate_SignatureInformation(x, ctx .. '.signatures[' .. i .. ']')
    end
  end
  if v['activeSignature'] == NIL then
    v['activeSignature'] = nil
    warn(ctx .. '.activeSignature must not be null')
  end
end

---@param v lsp.LocationUriOnly|vim.NIL|nil
---@param ctx string
local validate_LocationUriOnly = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['uri'] == NIL then
    v['uri'] = nil
    warn(ctx .. '.uri must not be null')
  end
end

---@param v lsp.WorkspaceSymbol|vim.NIL|nil
---@param ctx string
local validate_WorkspaceSymbol = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['location'] == NIL then
    v['location'] = nil
    warn(ctx .. '.location must not be null')
  end
  validate_Location(v['location'] --[[@as lsp.Location|vim.NIL|nil]], ctx .. '.location')
  validate_LocationUriOnly(
    v['location'] --[[@as lsp.LocationUriOnly|vim.NIL|nil]],
    ctx .. '.location'
  )
  if v['name'] == NIL then
    v['name'] = nil
    warn(ctx .. '.name must not be null')
  end
  if v['kind'] == NIL then
    v['kind'] = nil
    warn(ctx .. '.kind must not be null')
  end
  if v['tags'] == NIL then
    v['tags'] = nil
    warn(ctx .. '.tags must not be null')
  end
  if v['containerName'] == NIL then
    v['containerName'] = nil
    warn(ctx .. '.containerName must not be null')
  end
end

---@param v lsp.WorkspaceDiagnosticReport|vim.NIL|nil
---@param ctx string
local validate_WorkspaceDiagnosticReport = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['items'] == NIL then
    v['items'] = nil
    warn(ctx .. '.items must not be null')
  end
end

---@param v lsp.TextDocumentContentResult|vim.NIL|nil
---@param ctx string
local validate_TextDocumentContentResult = function(v, ctx)
  if v == nil or v == NIL then
    return
  end
  if v['text'] == NIL then
    v['text'] = nil
    warn(ctx .. '.text must not be null')
  end
end

---@param result lsp.CallHierarchyIncomingCall[]?
validate_result['callHierarchy/incomingCalls'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_CallHierarchyIncomingCall(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.CallHierarchyOutgoingCall[]?
validate_result['callHierarchy/outgoingCalls'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_CallHierarchyOutgoingCall(x, 'result[' .. i .. ']')
  end
end

validate_result['codeAction/resolve'] = function(result)
  if result == nil then
    return
  end
  validate_CodeAction(result, 'result')
end

validate_result['codeLens/resolve'] = function(result)
  if result == nil then
    return
  end
  validate_CodeLens(result, 'result')
end

validate_result['completionItem/resolve'] = function(result)
  if result == nil then
    return
  end
  validate_CompletionItem(result, 'result')
end

validate_result['documentLink/resolve'] = function(result)
  if result == nil then
    return
  end
  validate_DocumentLink(result, 'result')
end

validate_result['initialize'] = function(result)
  if result == nil then
    return
  end
  validate_InitializeResult(result, 'result')
end

validate_result['inlayHint/resolve'] = function(result)
  if result == nil then
    return
  end
  validate_InlayHint(result, 'result')
end

-- TODO: textDocument/codeAction result type "array" not supported
---@param result lsp.CodeLens[]?
validate_result['textDocument/codeLens'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_CodeLens(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.ColorPresentation[]?
validate_result['textDocument/colorPresentation'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_ColorPresentation(x, 'result[' .. i .. ']')
  end
end

validate_result['textDocument/completion'] = function(result)
  if result == nil then
    return
  end
  ---@type lsp.CompletionItem[]?
  local items
  if vim.islist(result) then
    items = result
  elseif type(result) == 'table' then
    items = result.items ~= NIL and result.items or nil
  end
  if type(items) == 'table' then
    for i, x in ipairs(items) do
      validate_CompletionItem(x, 'result[' .. i .. ']')
    end
  end
end

-- TODO: textDocument/declaration has union result, cannot resolve to single item struct
-- TODO: textDocument/definition has union result, cannot resolve to single item struct
-- TODO: textDocument/diagnostic result type "reference" not supported
---@param result lsp.ColorInformation[]?
validate_result['textDocument/documentColor'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_ColorInformation(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.DocumentHighlight[]?
validate_result['textDocument/documentHighlight'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_DocumentHighlight(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.DocumentLink[]?
validate_result['textDocument/documentLink'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_DocumentLink(x, 'result[' .. i .. ']')
  end
end

-- TODO: textDocument/documentSymbol has union result, cannot resolve to single item struct
---@param result lsp.FoldingRange[]?
validate_result['textDocument/foldingRange'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_FoldingRange(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.TextEdit[]?
validate_result['textDocument/formatting'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TextEdit(x, 'result[' .. i .. ']')
  end
end

validate_result['textDocument/hover'] = function(result)
  if result == nil then
    return
  end
  validate_Hover(result, 'result')
end

-- TODO: textDocument/implementation has union result, cannot resolve to single item struct
---@param result lsp.InlayHint[]?
validate_result['textDocument/inlayHint'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_InlayHint(x, 'result[' .. i .. ']')
  end
end

validate_result['textDocument/inlineCompletion'] = function(result)
  if result == nil then
    return
  end
  ---@type lsp.InlineCompletionItem[]?
  local items
  if vim.islist(result) then
    items = result
  elseif type(result) == 'table' then
    items = result.items ~= NIL and result.items or nil
  end
  if type(items) == 'table' then
    for i, x in ipairs(items) do
      validate_InlineCompletionItem(x, 'result[' .. i .. ']')
    end
  end
end

-- TODO: textDocument/inlineValue result type "array" not supported
validate_result['textDocument/linkedEditingRange'] = function(result)
  if result == nil then
    return
  end
  validate_LinkedEditingRanges(result, 'result')
end

---@param result lsp.Moniker[]?
validate_result['textDocument/moniker'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_Moniker(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.TextEdit[]?
validate_result['textDocument/onTypeFormatting'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TextEdit(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.CallHierarchyItem[]?
validate_result['textDocument/prepareCallHierarchy'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_CallHierarchyItem(x, 'result[' .. i .. ']')
  end
end

-- TODO: textDocument/prepareRename result type "reference" not supported
---@param result lsp.TypeHierarchyItem[]?
validate_result['textDocument/prepareTypeHierarchy'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TypeHierarchyItem(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.TextEdit[]?
validate_result['textDocument/rangeFormatting'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TextEdit(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.TextEdit[]?
validate_result['textDocument/rangesFormatting'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TextEdit(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.Location[]?
validate_result['textDocument/references'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_Location(x, 'result[' .. i .. ']')
  end
end

validate_result['textDocument/rename'] = function(result)
  if result == nil then
    return
  end
  validate_WorkspaceEdit(result, 'result')
end

---@param result lsp.SelectionRange[]?
validate_result['textDocument/selectionRange'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_SelectionRange(x, 'result[' .. i .. ']')
  end
end

validate_result['textDocument/semanticTokens/full'] = function(result)
  if result == nil then
    return
  end
  validate_SemanticTokens(result, 'result')
end

-- TODO: textDocument/semanticTokens/full/delta has union result, cannot resolve to single item struct
validate_result['textDocument/semanticTokens/range'] = function(result)
  if result == nil then
    return
  end
  validate_SemanticTokens(result, 'result')
end

validate_result['textDocument/signatureHelp'] = function(result)
  if result == nil then
    return
  end
  validate_SignatureHelp(result, 'result')
end

-- TODO: textDocument/typeDefinition has union result, cannot resolve to single item struct
---@param result lsp.TextEdit[]?
validate_result['textDocument/willSaveWaitUntil'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TextEdit(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.TypeHierarchyItem[]?
validate_result['typeHierarchy/subtypes'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TypeHierarchyItem(x, 'result[' .. i .. ']')
  end
end

---@param result lsp.TypeHierarchyItem[]?
validate_result['typeHierarchy/supertypes'] = function(result)
  if result == nil or not vim.islist(result) then
    return
  end
  for i, x in ipairs(result) do
    validate_TypeHierarchyItem(x, 'result[' .. i .. ']')
  end
end

validate_result['workspaceSymbol/resolve'] = function(result)
  if result == nil then
    return
  end
  validate_WorkspaceSymbol(result, 'result')
end

validate_result['workspace/diagnostic'] = function(result)
  if result == nil then
    return
  end
  validate_WorkspaceDiagnosticReport(result, 'result')
end

-- TODO: workspace/executeCommand result type "reference" not supported
-- TODO: workspace/symbol has union result, cannot resolve to single item struct
validate_result['workspace/textDocumentContent'] = function(result)
  if result == nil then
    return
  end
  validate_TextDocumentContentResult(result, 'result')
end

validate_result['workspace/willCreateFiles'] = function(result)
  if result == nil then
    return
  end
  validate_WorkspaceEdit(result, 'result')
end

validate_result['workspace/willDeleteFiles'] = function(result)
  if result == nil then
    return
  end
  validate_WorkspaceEdit(result, 'result')
end

validate_result['workspace/willRenameFiles'] = function(result)
  if result == nil then
    return
  end
  validate_WorkspaceEdit(result, 'result')
end

return {
  params = M,
  result = validate_result,
  set_client_name = function(name)
    _client_name = name
  end,
}
