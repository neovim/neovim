{
  "// description": "Library modules: the three core modules (CommonJS, later UMD-wrapped) + .d.ts declarations. Emitted to dist-lib/, then assembled into dist/ by build-ts.sh.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist-lib",
    "rootDir": "./src",
    "lib": ["ES2020", "DOM", "DOM.Iterable"]
  },
  "files": [
    "src/neovim.ts",
    "src/neovim-ui.ts",
    "src/neovim-ui-pre.ts"
  ]
}
